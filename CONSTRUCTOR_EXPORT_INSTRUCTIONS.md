# 📝 Инструкции для экспорта интерфейса из Constructor

> **Дата:** 15 ноября 2025  
> **Для проекта:** proto-3 (PWA-клиент Cleverence)

---

## 🎯 Цель

Настроить экспорт интерфейса в проекте **constructor** так, чтобы экспортируемый JSON 
был готов к импорту в **proto-3** без дополнительных манипуляций.

---

## 📋 Запрос для Cursor Sonnet в проекте constructor

Скопируйте и вставьте этот запрос в Cursor:

```
Нужно исправить экспорт интерфейса, чтобы при импорте в принимающее приложение 
всё работало корректно без дополнительных манипуляций.

КРИТИЧЕСКИЕ ИЗМЕНЕНИЯ В ЭКСПОРТЕ:

1. НЕ ЭКСПОРТИРОВАТЬ:
   - Header (шапку интерфейса)
   - Debug информацию
   - Любые декоративные элементы вне кнопок
   - Только экспортируем: metadata, grid, buttons

2. ИСПРАВИТЬ КООРДИНАТЫ КНОПОК:
   Текущий экспорт использует 0-based координаты, но CSS Grid начинается с 1.
   
   Было (неправильно):
   position: {
     startRow: 0,
     endRow: 2,
     startCol: 0,
     endCol: 2
   }
   
   Должно быть (правильно):
   position: {
     startRow: 0,    // Остается как есть
     endRow: 1,      // endRow = startRow + высота - 1
     startCol: 0,    // Остается как есть
     endCol: 1       // endCol = startCol + ширина - 1
   }
   
   В принимающем приложении используется формула:
   gridColumn: `${startCol + 1} / ${endCol + 2}`
   gridRow: `${startRow + 1} / ${endRow + 2}`

3. ТИПЫ ДЕЙСТВИЙ (action):
   Используй новые типы вместо старых:
   
   Старые (НЕ использовать):
   - 'scan_barcode'
   - 'open_form'
   - 'navigate'
   
   Новые (использовать):
   - 'RECEIVING'        // Приёмка
   - 'ORDER_PICKING'    // Подбор заказа
   - 'SHIPPING'         // Отгрузка
   - 'INVENTORY'        // Инвентаризация
   - 'PLACEMENT'        // Размещение
   - 'RETURN'           // Возврат
   - 'TRANSFER'         // Перемещение
   - 'MARKING'          // Маркировка
   - 'none'             // Нет действия

4. ДОБАВИТЬ МАРШРУТЫ (опционально):
   Каждая кнопка может иметь прямой маршрут:
   
   route: '/docs/PrihodNaSklad'      // для RECEIVING
   route: '/docs/PodborZakaza'       // для ORDER_PICKING
   route: '/docs/Otgruzka'           // для SHIPPING
   route: '/docs/Inventarizaciya'    // для INVENTORY
   route: '/docs/RazmeshhenieVYachejki'  // для PLACEMENT
   route: '/docs/Vozvrat'            // для RETURN
   route: '/docs/Peremeshenie'       // для TRANSFER
   route: '/docs/Markirovka'         // для MARKING

5. СТРУКТУРА ЭКСПОРТА:
   {
     "version": "1.0",  // опционально, версия схемы (top-level)
     "metadata": {
       "name": "Название интерфейса",
       "description": "Описание",  // опционально
       "version": "1.0",  // опционально, версия конфигурации
       "created": "2025-11-15T13:49:14.347Z",  // опционально
       "updated": "2025-11-15T13:49:14.347Z"   // опционально
     },
     "grid": {
       "rows": 6,      // Количество строк
       "columns": 4    // Количество колонок (обычно 4)
     },
     "buttons": [
       {
         "id": "unique-id",
         "label": "Приход на склад",
         "action": "RECEIVING",
         "route": "/docs/PrihodNaSklad",  // опционально
         "position": {
           "startRow": 0,
           "endRow": 1,    // startRow + высота - 1
           "startCol": 0,
           "endCol": 1     // startCol + ширина - 1
         },
         "style": "light",  // или "dark"
         "color": "#F3A36A"  // опционально
       }
     ]
   }

6. СТИЛИ КНОПОК:
   - style: "light" → светлая кнопка (#F3A36A фон, #8B5931 текст)
   - style: "dark"  → темная кнопка (#3E515C фон, #FFFFFF текст)

7. ВАЛИДАЦИЯ ПЕРЕД ЭКСПОРТОМ:
   - Проверить, что все endRow = startRow + height - 1
   - Проверить, что все endCol = startCol + width - 1
   - Проверить, что используются новые типы действий
   - Удалить header и debug элементы

ПРИМЕР ПРАВИЛЬНОГО ЭКСПОРТА кнопки 2×2:
{
  "id": "btn-receiving",
  "label": "Приход на склад",
  "action": "RECEIVING",
  "route": "/docs/PrihodNaSklad",
  "position": {
    "startRow": 0,
    "endRow": 1,     // 0 + 2 - 1 = 1
    "startCol": 0,
    "endCol": 1      // 0 + 2 - 1 = 1
  },
  "style": "light"
}

ЗАДАЧА:
Найди функцию экспорта интерфейса и исправь её согласно этим требованиям.
Убедись, что экспортируется ТОЛЬКО чистая схема без UI-элементов конструктора.
```

---

## ✅ Что было сделано в proto-3

### 1. Упрощен компонент `DynamicGridInterface.tsx`

- Удалены все console.log и избыточные комментарии
- Оставлена только чистая логика работы с JSON-схемой
- Убран встроенный QRScanner (теперь только через меню)
- Оптимизирована загрузка схемы из localStorage

### 2. Формула позиционирования кнопок

В proto-3 используется следующая формула для преобразования координат:

```typescript
gridColumn: `${startCol + 1} / ${endCol + 2}`
gridRow: `${startRow + 1} / ${endRow + 2}`
```

**Пример:**
- Кнопка 2×2 в левом верхнем углу:
  - JSON: `{ startRow: 0, endRow: 1, startCol: 0, endCol: 1 }`
  - CSS Grid: `gridColumn: "1 / 3"`, `gridRow: "1 / 3"`
  - Результат: Занимает ячейки (1,1), (1,2), (2,1), (2,2) = 2×2 ✅

- Кнопка 2×1 в правом верхнем углу:
  - JSON: `{ startRow: 0, endRow: 0, startCol: 2, endCol: 3 }`
  - CSS Grid: `gridColumn: "3 / 5"`, `gridRow: "1 / 2"`
  - Результат: Занимает ячейки (1,3), (1,4) = 2×1 ✅

### 3. Квадратная сетка

Расчет размеров для квадратных кнопок:

```typescript
const gap = 2; // px - минимальный отступ
const containerPadding = 32; // px (16px слева + 16px справа от Layout)
const availableWidth = `100vw - ${containerPadding}px`;
const cellSize = `calc((${availableWidth} - ${gap * (columns - 1)}px) / ${columns})`;

gridTemplateColumns: `repeat(${columns}, 1fr)`  // Колонки делят доступное пространство
gridTemplateRows: `repeat(${rows}, ${cellSize})`  // Строки квадратные
```

### 4. Приоритет навигации

1. **Прямой маршрут** (`button.route`) - высший приоритет
2. **Маршрут через ACTION_ROUTES** (`ACTION_ROUTES[button.action]`)
3. **Legacy actions** (для обратной совместимости)

### 5. Счетчики документов

- Автоматически загружаются из API через `documentCounter.getAllCounts()`
- Обновляются каждую минуту
- Отображаются в правом нижнем углу кнопки
- Fallback на mock-данные если API недоступен

---

## 📦 Пример корректного экспорта

Вот пример JSON, который корректно импортируется в proto-3:

```json
{
  "version": "1.0",
  "metadata": {
    "name": "Custom Interface",
    "description": "Созданный интерфейс для ТСД",
    "created": "2025-11-15T13:49:14.347Z",
    "updated": "2025-11-15T13:49:14.347Z",
    "version": "1.0"
  },
  "grid": {
    "columns": 4,
    "rows": 6
  },
  "buttons": [
    {
      "id": "btn-1",
      "position": {
        "startCol": 0,
        "endCol": 1,
        "startRow": 0,
        "endRow": 1
      },
      "label": "Приход на склад",
      "action": "RECEIVING",
      "style": "light",
      "documentCount": 58,
      "route": "/docs/PrihodNaSklad"
    },
    {
      "id": "btn-2",
      "position": {
        "startCol": 2,
        "endCol": 3,
        "startRow": 0,
        "endRow": 1
      },
      "label": "Подбор заказа",
      "action": "ORDER_PICKING",
      "style": "dark",
      "documentCount": 57,
      "route": "/docs/PodborZakaza"
    }
  ]
}
```

---

## 🎨 Стили кнопок

### Light (светлая)
```json
{
  "style": "light",
  "color": "#F3A36A"  // опционально, переопределяет стандартный
}
```
- Фон: `#F3A36A` (светлый персиковый)
- Текст: `#8B5931` (темно-коричневый)

### Dark (темная)
```json
{
  "style": "dark"
}
```
- Фон: `#3E515C` (темно-серый)
- Текст: `#FFFFFF` (белый)
- Граница: `1px solid rgba(255, 255, 255, 0.2)`

---

## 🔑 Ключевые моменты

1. **Координаты включительные**: `endRow` и `endCol` - это последняя занятая ячейка
2. **Формула для 2×2 кнопки**: `endRow = startRow + 1`, `endCol = startCol + 1`
3. **Формула для 2×1 кнопки**: `endRow = startRow + 0`, `endCol = startCol + 1`
4. **Не экспортировать**: Header, Debug info, Scanner button, лишние элементы UI
5. **Использовать новые action types**: `RECEIVING`, `ORDER_PICKING`, и т.д.
6. **Опционально route**: Можно указывать прямой маршрут для кнопки

---

## ✨ Результат

После применения этих инструкций в constructor, экспортированные интерфейсы будут:

- ✅ Корректно отображаться с первого импорта
- ✅ Иметь правильные размеры кнопок (квадратные по сетке)
- ✅ Правильно размещаться на экране
- ✅ Отображать счетчики документов в правильных местах
- ✅ Использовать правильные маршруты для навигации
- ✅ Не содержать лишних элементов интерфейса конструктора

---

**Файл создан:** 15 ноября 2025  
**Автор:** AI Assistant (Claude Sonnet 4.5)  
**Проект:** proto-3 (PWA-клиент Cleverence)

