# 📘 Руководство по восстановлению проекта «Склад‑15» с нуля

Документ описывает, как при помощи Cursor/Claude заново собрать текущий PWA‑клиент, подключающийся к серверу Cleverence Mobile SMARTS. Следуя шагам, можно воспроизвести архитектуру, структуру директорий и ключевые сценарии (документы, кастомные интерфейсы, счётчики).

---

## 1. Технологический стек

- **Vite + React 18 + TypeScript**
- **Tailwind CSS** (кастомные utility + собственные правила в `index.css`)
- **Dexie.js** — IndexedDB кэш для OData
- **Axios** — HTTP-клиент с динамическим `baseURL`
- **React Router 6** — навигация
- **Framer Motion**, **html5-qrcode**, **lucide-react** — UI и сканер
- **Custom schema loader** для интерфейсов из Creator (`SchemaLoader`, `DynamicGridInterface`)

---

## 2. Подготовка окружения

1. **Установить Node.js ≥ 18** (проверить `node -v`, `npm -v`).
2. **Клонировать/создать репозиторий** (в примере директория `proto-3`).
3. В Cursor открыть папку: `/cmd open-folder proto-3`.

---

## 3. Бутстрап проекта

Запустить следующие команды (можно просить Claude сформировать «шаги»):

```bash
/cmd npm create vite@latest warehouse-proto-3 -- --template react-ts
/cmd cd warehouse-proto-3
/cmd npm install
```

### Конфигурация Tailwind

```bash
/cmd npm install -D tailwindcss postcss autoprefixer
/cmd npx tailwindcss init -p
```

В `tailwind.config.js` прописать `content` со всеми `src/**/*.{js,ts,jsx,tsx}` и расширения для цветов, кнопок и т.д. (см. текущий файл в репозитории).

---

## 4. Структура директорий

```
src/
 ├─ components/            UI-блоки (Header, DocumentCard, DynamicGridInterface…)
 ├─ contexts/              контексты (Theme, Auth, DocumentHeader)
 ├─ hooks/                 useScanner, useSync, useOfflineStorage
 ├─ pages/                 экран Home, Documents, Setup, Module pages
 ├─ services/              api.ts, odataCache.ts, documentService.ts, configService.ts
 ├─ types/                 доменные типы (document.ts, odata.ts, ui-schema.ts)
 ├─ utils/                 feedback, sound, vibration, loadInitialData
 └─ data/                  JSON-данные для офлайн/демо режимов
```

При восстановлении попросите Claude создать каталог и базовые файлы одной командой:

```
/cmd mkdir -p src/{components,pages,services,contexts,hooks,types,utils,data}
```

---

## 5. Базовая настройка приложения

1. **App.tsx**: оборачиваем `<BrowserRouter>` провайдерами `ThemeProvider`, `AuthProvider`, `MenuProvider`, `DocumentHeaderProvider`. Публичные маршруты: `/setup`, `/login`. Защищённые — через `<ProtectedRoute>`.
2. **ThemeContext**: хранит тему в `localStorage`, переключает `data-theme` на `<html>`.
3. **Setup.tsx**: форма ввода `serverUrl`, тест `fetch(url/DocTypes)` и сохранение через `configService`.
4. **AuthContext/AuthService**: заглушка токенов (DEV-режим принимает любые данные).

Сгенерировать базовый компонент можно командой:
```
/cmd cat <<'EOF' > src/App.tsx
// ... содержимое из репозитория ...
EOF
```

---

## 6. Интеграция с Mobile SMARTS API

### 6.1 ApiService (`src/services/api.ts`)

- Создать singleton `ApiService`.
- `updateBaseURL()`:
  - если пользователь указал URL со `http://localhost:9000`, а фронт запущен на `5173` — автоматически переключаемся на `/MobileSMARTS/api/v1` (Vite proxy, избегаем CORS).
  - fallback — `http://localhost:9000/MobileSMARTS/api/v1`.
- Методы: `getDocTypes`, `getAllDocs`, `getDocsByType`, `getDocById`, `getDocsCount(docTypeUni)`, плюс CRUD под модули (receiving, picking…).

Пример команды для вставки кода:
```
/cmd apply_patch <<'PATCH'
*** Begin Patch
// вставить реализацию ApiService
*** End Patch
PATCH
```

### 6.2 OData cache (`src/services/odataCache.ts`)

- Dexie-таблицы: `odataDocTypes`, `odataDocuments`, `cacheMetadata`.
- Метод `getDocsByType` пробует:
  1. `GET /Docs/{DocType}`.
  2. `GET /Docs?$filter=documentTypeName eq '...'`.
  3. `GET /Docs`.
- Результат кэшируется, используется Home и DocumentsByType.

### 6.3 documentService

- `getAllDocuments()` → сначала пробует OData, потом падает на локальные Dexie-таблицы (legacy режим).
- Маппит `ODataDocument` в `UniversalDocument`, добавляя `origin: 'odata'` и `docTypeUni`.
- `getDocumentUrl` возвращает `/docs/{docTypeUni}/{id}` для odata-документов.

---

## 7. UI: страницы и компоненты

### 7.1 Home

- Загружает DocTypes через `odataCache`.
- Для каждой плитки пытается получить список документов (`getDocsByType`), затем показывает число (fallback на `getDocsCount`).
- Если сервер недоступен — включается mock-режим (данные из `src/data/mockDocTypes.ts`, подсказка «Демо-режим»).

### 7.2 Списки документов

- `/documents`: использует `documentService.getAllDocuments`, фильтры в `components/documents`.
- `/docs/:docTypeUni`: отдельный экран `DocumentsByType`, использующий `odataCache.getDocsByType`.
- `/docs/:docTypeUni/:docId`: `DocumentDetails.tsx`, берёт документ через `api.getDocumentById` + `$expand=declaredItems,currentItems`.

### 7.3 Кастомные интерфейсы (Creator)

- `DynamicGridInterface` читает схему из `localStorage (ui-schema-*)`.
- Счётчики подтягиваются через `documentCounter` (использует те же OData запросы).
- Установка из меню → `InterfaceInstaller`.

---

## 8. Работа со скинами (Creator)

1. В проекте Creator собираем layout.
2. Экспортируем JSON (`schema.metadata`, `schema.grid`, `schema.buttons`).
3. В клиенте используем «Установить интерфейс» → вставляем JSON → сохраняется в `localStorage`.
4. Под капотом кнопки вызывают маршруты из `ACTION_ROUTES`.

> При необходимости изменить формат скина: убедитесь, что каждая кнопка содержит `action` (например, `RECEIVING`, `ORDER_PICKING`). Это всё, что нужно для корректной работы счётчиков и переходов.

---

## 9. Работа с Cursor/Claude: подсказки

Примеры полезных команд/промтов:

- **Создать новый файл**:
  ```
  /cmd apply_patch <<'PATCH'
  *** Begin Patch
  *** Add File: src/services/newService.ts
  // ...
  *** End Patch
  PATCH
  ```
- **Запросить код**:  
  «Создай компонент `src/pages/Setup.tsx`, который содержит форму URL и кнопку проверки, аналогичную описанию в DOCS/REBUILD_FROM_SCRATCH.md».
- **Проверить сервер**:  
  `/cmd curl http://localhost:9000/MobileSMARTS/api/v1/DocTypes`
- **Запустить dev**:  
  `/cmd npm run dev`

---

## 10. Проверка и запуск

1. `npm install`
2. Запустить Mobile SMARTS сервер (по умолчанию `http://localhost:9000`).
3. `/cmd npm run dev`
4. Открыть `http://localhost:5173/setup`, указать `http://localhost:9000/MobileSMARTS/api/v1`.
5. После входа убедиться, что:
   - Плитки на главной показывают реальные числа.
   - `/documents` и `/docs/:docType` подгружают данные.
   - Кастомные интерфейсы (если загружены) тоже отображают счётчики.

---

## 11. Сборка и деплой

- `npm run build` → результат в `dist/`.
- Для PWA используется `vite-plugin-pwa` + `workbox-window`. После изменения Service Worker перезапустите `npm run dev -- --host`.

---

## 12. Что делать при переносе

- Если используете другой домен/порт — либо включите CORS на сервере, либо при разработке оставьте относительный `/MobileSMARTS/api/v1`.
- При изменении структуры DocTypes/Docs обновите `DOCS/$metadata.xml`, чтобы типы `documentService` совпадали с серверными `uni`.
- Любые новые режимы скинов должны продолжать отдавать `ButtonAction` — тогда существующие сервисы смогут подтянуть статистику.

---

Документ предназначен для быстрого восстановления проекта через Cursor/Claude. Используйте его как чек-лист, когда требуется воссоздать рабочий клиент «Склад‑15» или развернуть его на новой машине. Удачной разработки!


