# 🎯 Исправление размера бейджей

**Дата:** 23 ноября 2025  
**Проблема:** Бейджи фильтров слишком большие

---

## 🔍 Анализ проблемы

### Было (неправильно):
```tsx
text-xs px-3 py-1
```
- Размер шрифта: `12px` (text-xs)
- Padding: `12px × 4px` (px-3 py-1)
- **Это размер "lg" Badge!**

### Стало (правильно):
```tsx
text-[10px] px-1.5 py-0.5
```
- Размер шрифта: `10px`
- Padding: `6px × 2px` (px-1.5 py-0.5)
- **Это размер "sm" Badge из дизайн-системы!**

---

## 📐 Размеры Badge по дизайн-системе

Из `src/design/components/Badge.tsx`:

```typescript
const sizes: Record<BadgeSize, string> = {
  sm: 'text-[10px] px-1.5 py-0.5 gap-1',      // ✅ Используем это
  md: 'text-xs px-2.5 py-0.5 gap-1.5',        // Средний
  lg: 'text-sm px-3 py-1 gap-2',              // ❌ Было это (слишком большой)
};
```

---

## 🎨 Визуальное сравнение

### Размер LG (было):
```
┌─────────┐
│   Все   │  ← 12px шрифт, 12px × 4px padding
└─────────┘
```

### Размер SM (стало):
```
┌────┐
│Все │  ← 10px шрифт, 6px × 2px padding
└────┘
```

---

## 📝 Изменённые файлы

### `src/pages/DocumentsByType.tsx`

**Строка изменения:**
```diff
- text-xs px-3 py-1
+ text-[10px] px-1.5 py-0.5
```

**Полный код кнопки:**
```tsx
<button
  className={`
    inline-flex items-center justify-center 
    rounded-full font-bold border text-[10px] px-1.5 py-0.5
    transition-all duration-200
    ${isActive 
      ? 'bg-brand-primary text-brand-primaryDark border-brand-primary' 
      : 'bg-surface-secondary border-surface-tertiary text-content-secondary'
    }
  `}
>
  {s.label}
</button>
```

---

## ✅ Результат

### Характеристики обновлённых бейджей:

1. **Компактность:**
   - Высота: ~18px (было ~28px)
   - Ширина: пропорционально уменьшена
   - Экономия места: ~35%

2. **Читаемость:**
   - Шрифт 10px всё ещё читаемый
   - Контраст сохранён (тёмный текст на оранжевом)
   - Округлые края (rounded-full)

3. **Единообразие:**
   - Соответствует Badge SM из дизайн-системы
   - Такой же размер, как у других мелких бейджей
   - Консистентный UI

---

## 🎯 Применение

Эти бейджи используются для фильтрации документов по статусам:
- **Все** - показать все документы
- **Новые** - только новые
- **В работе** - в процессе выполнения  
- **Завершён** - завершённые

**Активный фильтр** выделяется оранжевым цветом с тёмным текстом.

---

✅ **Размер бейджей теперь соответствует дизайн-системе!**

