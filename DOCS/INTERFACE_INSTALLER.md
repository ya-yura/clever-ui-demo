# 🎨 Установка кастомного интерфейса

**Дата:** 12 ноября 2025  
**Версия:** 1.0.0  
**Файл:** `src/components/InterfaceInstaller.tsx`

---

## 📋 Обзор

Система "Склад-15" поддерживает загрузку кастомных интерфейсов **тремя способами**:

1. 📱 **Сканирование QR-кода** — быстрая установка из приложения Constructor
2. 📁 **Загрузка JSON-файла** — установка из файла на устройстве
3. 📝 **Вставка кода** — copy-paste JSON или сжатой строки

После установки интерфейс **сохраняется в localStorage** и используется во всех последующих сессиях до следующего изменения.

---

## 🚀 Как использовать

### Доступ к функции

**Через гамбургер-меню:**
1. Открыть меню (☰)
2. Выбрать "Установить интерфейс"
3. Выбрать способ загрузки

### Способ 1: Сканирование QR-кода

**Когда использовать:** Есть QR-код от приложения Constructor

**Шаги:**
1. Нажать "Открыть сканер"
2. Навести камеру на QR-код
3. Дождаться автоматической загрузки
4. Перезагрузить страницу

**Преимущества:**
- ✅ Самый быстрый способ
- ✅ Не требует ручного ввода
- ✅ Работает с сжатыми конфигурациями

**Формат QR-кода:**
- Сжатая строка конфигурации (base64 + gzip)
- Генерируется в приложении Constructor

### Способ 2: Загрузка JSON-файла

**Когда использовать:** Есть файл с конфигурацией

**Шаги:**
1. Нажать "Выбрать файл"
2. Выбрать `.json` файл из файловой системы
3. Дождаться загрузки
4. Перезагрузить страницу

**Преимущества:**
- ✅ Удобно для хранения нескольких конфигураций
- ✅ Можно редактировать в текстовом редакторе
- ✅ Легко шарить через файловые хранилища

**Формат файла:**
```json
{
  "id": "my-interface",
  "name": "Warehouse A Interface",
  "version": "1.0.0",
  "metadata": {
    "name": "Склад A",
    "description": "Интерфейс для склада A"
  },
  "grid": {
    "rows": 4,
    "columns": 4
  },
  "buttons": [
    {
      "id": "btn-1",
      "label": "Приёмка",
      "action": "navigate",
      "params": { "path": "/docs/PrihodNaSklad" },
      "position": {
        "startRow": 0,
        "endRow": 2,
        "startCol": 0,
        "endCol": 2
      },
      "style": "light",
      "color": "#DAA420"
    }
  ]
}
```

### Способ 3: Вставка кода (Copy-Paste)

**Когда использовать:** Получили код через мессенджер, email, etc.

**Шаги:**
1. Скопировать JSON-код или сжатую строку
2. Вставить в текстовое поле
3. Нажать "Установить интерфейс"
4. Перезагрузить страницу

**Преимущества:**
- ✅ Быстрая отправка через мессенджеры
- ✅ Не требует файлов
- ✅ Работает с обычным JSON и сжатыми строками

**Поддерживаемые форматы:**
```javascript
// 1. Обычный JSON
{"id":"my-interface","version":"1.0.0",...}

// 2. Сжатая строка (от Constructor)
H4sIAAAAAAAAE6tWKi5JLElVslJQKkhMzk6tBQ...
```

---

## 💾 Сохранение и применение

### Как сохраняется

После успешной загрузки интерфейс сохраняется в **localStorage** браузера:

```javascript
// Ключ: 'ui-schema-active'
localStorage.setItem('ui-schema-active', JSON.stringify(schema));
```

### Когда применяется

1. **Немедленно** — при первой загрузке главной страницы
2. **Автоматически** — при каждом запуске приложения
3. **Постоянно** — сохраняется между сессиями

### Как работает

```typescript
// При загрузке главной страницы (Home.tsx)
useEffect(() => {
  // Проверка наличия кастомного интерфейса
  const customSchema = SchemaLoader.loadFromLocalStorage('active');
  
  if (customSchema) {
    // Используем кастомный интерфейс
    setHasCustomInterface(true);
  } else {
    // Используем стандартный интерфейс
    loadDocTypes();
  }
}, []);
```

---

## 🔄 Обновление интерфейса

### Замена текущего интерфейса

Чтобы заменить текущий интерфейс на новый:
1. Открыть "Установить интерфейс"
2. Загрузить новую конфигурацию любым способом
3. Старая конфигурация автоматически перезаписывается
4. Перезагрузить страницу

### Возврат к стандартному

Чтобы вернуться к стандартному интерфейсу:
1. Открыть "Установить интерфейс"
2. Нажать "Сбросить интерфейс"
3. Подтвердить действие
4. Перезагрузить страницу

**Что происходит:**
```javascript
// Удаление кастомных схем из localStorage
SchemaLoader.deleteFromLocalStorage('active');
SchemaLoader.deleteFromLocalStorage('default');

// Приложение вернется к встроенному интерфейсу
```

---

## 📊 Аналитика

Все действия с интерфейсом отслеживаются системой аналитики:

### События

#### 1. Загрузка интерфейса

```typescript
analytics.trackCustomInterfaceLoaded({
  id: 'warehouse-a-v2',
  version: '2.1.0',
  buttonsCount: 8,
  source: 'qr' | 'file' | 'text'
});
```

**Что записывается:**
- ID конфигурации
- Версия конфигурации
- Количество кнопок
- Источник загрузки (QR/файл/текст)

#### 2. Сканирование QR-кода

```typescript
analytics.trackCustomInterfaceQRScan(
  success: true | false,
  error?: 'Invalid schema' | 'Decompression error' | ...
);
```

**Что записывается:**
- Успешность сканирования
- Текст ошибки (если неудачно)

#### 3. Ошибки загрузки

```typescript
analytics.trackError(error, {
  component: 'InterfaceInstaller',
  method: 'qr' | 'file' | 'text'
});
```

### Анализ данных

См. подробнее в [`ANALYTICS_CUSTOM_INTERFACES.md`](ANALYTICS_CUSTOM_INTERFACES.md)

---

## 🛠️ Технические детали

### Компонент

**Файл:** `src/components/InterfaceInstaller.tsx`

**Props:**
```typescript
interface InterfaceInstallerProps {
  onClose: () => void;
  onSuccess?: (schema: UISchema) => void;
}
```

**State:**
```typescript
const [activeMethod, setActiveMethod] = useState<'qr' | 'file' | 'text'>('qr');
const [showScanner, setShowScanner] = useState(false);
const [jsonText, setJsonText] = useState('');
const [error, setError] = useState<string | null>(null);
const [loading, setLoading] = useState(false);
```

### Интеграция в Layout

```typescript
// src/components/Layout.tsx
const [showInterfaceInstaller, setShowInterfaceInstaller] = useState(false);

const handleInstallInterface = () => {
  setShowInterfaceInstaller(true);
};

// В JSX
{showInterfaceInstaller && (
  <InterfaceInstaller 
    onClose={() => setShowInterfaceInstaller(false)}
    onSuccess={handleInterfaceSuccess}
  />
)}
```

### SchemaLoader API

```typescript
// Загрузка из сжатой строки
SchemaLoader.loadFromCompressed(compressedString): UISchema | null

// Загрузка из файла
SchemaLoader.loadFromFile(file: File): Promise<UISchema | null>

// Сохранение в localStorage
SchemaLoader.saveToLocalStorage(schema: UISchema, name: string): boolean

// Загрузка из localStorage
SchemaLoader.loadFromLocalStorage(name: string): UISchema | null

// Удаление из localStorage
SchemaLoader.deleteFromLocalStorage(name: string): boolean
```

---

## ⚠️ Обработка ошибок

### Типичные ошибки

#### 1. Неверный формат QR-кода

**Ошибка:** "Invalid schema format"

**Причина:** QR-код не содержит валидную конфигурацию

**Решение:**
- Проверить, что QR-код сгенерирован в Constructor
- Убедиться, что QR-код не поврежден

#### 2. Ошибка декомпрессии

**Ошибка:** "Decompression error"

**Причина:** Сжатая строка повреждена или имеет неверный формат

**Решение:**
- Попробовать загрузить заново
- Использовать другой способ (файл/текст)

#### 3. Невалидная схема

**Ошибка:** "Schema validation failed"

**Причина:** JSON не соответствует требуемой структуре

**Решение:**
- Проверить структуру JSON
- Убедиться, что есть обязательные поля: `id`, `name`, `version`, `buttons`, `grid`

#### 4. Ошибка парсинга JSON

**Ошибка:** "Unexpected token in JSON"

**Причина:** Невалидный JSON-синтаксис

**Решение:**
- Проверить JSON в валидаторе (jsonlint.com)
- Исправить синтаксические ошибки

### Логирование

Все ошибки логируются в консоль:

```javascript
console.error('QR scan error:', error);
console.error('File upload error:', error);
console.error('Text paste error:', error);
```

И отправляются в аналитику:

```typescript
analytics.trackError(error, { 
  component: 'InterfaceInstaller', 
  method: 'qr' | 'file' | 'text' 
});
```

---

## 📱 UX/UI особенности

### Модальное окно

**Дизайн:**
- Темная тема (#2A2A2C)
- Полноэкранный overlay с затемнением
- Адаптивная ширина (max 600px)
- Прокрутка при необходимости

**Три вкладки:**
- QR-код (📱)
- Файл (📁)
- Текст (📝)

**Кнопки:**
- Основная: оранжевая (#F3A36A)
- Отмена/закрытие: серая
- Сброс: красная (#FF3B30)

### Обратная связь

**Процесс загрузки:**
- Кнопка "Загрузка..." (disabled)
- Невозможность повторного нажатия

**Успех:**
- Alert с сообщением
- Автоматическое закрытие модального окна
- Переход на главную страницу
- Перезагрузка страницы через 500ms

**Ошибка:**
- Красный блок с текстом ошибки
- Возможность повторить попытку
- Кнопки остаются активными

---

## 🔐 Безопасность

### Валидация данных

**Обязательная проверка:**
1. Формат JSON (парсинг)
2. Структура схемы (validateSchema)
3. Наличие обязательных полей
4. Типы данных

**Что проверяется:**
```typescript
// Минимально необходимые поля
{
  id: string,
  name: string,
  version: string,
  grid: { rows: number, columns: number },
  buttons: Array<ButtonConfig>
}
```

### Ограничения

**Размер данных:**
- localStorage: ~5-10 MB
- Рекомендуется: < 1 MB на конфигурацию

**Формат файлов:**
- Только `.json`
- Только text/plain или application/json MIME-type

---

## 🎯 Best Practices

### Для пользователей

1. **Храните резервные копии** конфигураций в файлах
2. **Тестируйте** новые конфигурации перед развертыванием
3. **Документируйте** изменения в версиях
4. **Используйте версионирование** (1.0.0, 1.1.0, etc.)

### Для разработчиков

1. **Генерируйте QR-коды** в Constructor с валидацией
2. **Включайте метаданные** (id, version, name)
3. **Тестируйте на разных устройствах**
4. **Мониторьте аналитику** загрузок

---

## 📖 См. также

- **[ANALYTICS_CUSTOM_INTERFACES.md](ANALYTICS_CUSTOM_INTERFACES.md)** — Аналитика кастомных интерфейсов
- **[SYSTEM_FUNCTIONS_REFERENCE.md](SYSTEM_FUNCTIONS_REFERENCE.md)** — Справочник функций
- **[DOCS/INDEX.md](INDEX.md)** — Главная документация

---

**Версия:** 1.0.0  
**Дата:** 12 ноября 2025  
**Статус:** ✅ Production-Ready


