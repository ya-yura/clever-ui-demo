# 🎨 Редизайн фильтров-чипов

**Дата:** 16 ноября 2025  
**Статус:** ✅ Выполнено

---

## 📋 Описание изменений

Обновлены стили кнопок-фильтров (чипов) на страницах документов в стиле скриншота из приложения "Голосовой Тренер".

### Было:
- Активная кнопка: оранжевый фон `#FEA079`
- Отступы: `px-3 py-1.5`
- Переходы: `transition-colors`

### Стало:
- Активная кнопка: бирюзовый фон `#86e0cb` (основной цвет проекта)
- Улучшенные отступы: `px-4 py-2` (как на скриншоте)
- Плавные переходы: `transition-all`
- Добавлена тень для активной кнопки: `shadow-sm`
- Центрирование контента: `justify-center`
- Улучшенный hover-эффект для границы

---

## 🎯 Затронутые компоненты

### 1. **Страница Documents** (`src/pages/Documents.tsx`)
Использует `DocumentFilters` компонент с чипами:
- Фильтры по типу документа
- Фильтры по статусу
- Сортировка

### 2. **Страница DocumentsByType** (`src/pages/DocumentsByType.tsx`)
Чипы для:
- Быстрого переключения между типами документов
- Фильтрации по статусам: "Все", "Новые", "В работе", "Завершён"

### 3. **Компонент DocumentFilters** (`src/components/documents/DocumentFilters.tsx`)
Основной компонент фильтрации с чипами

---

## 💅 Новые стили

### Темная тема (по умолчанию)

```css
.chip {
  /* Основной стиль */
  display: inline-flex;
  align-items: center;
  justify-center: center;
  border-radius: 9999px; /* fully rounded */
  padding: 0.5rem 1rem; /* px-4 py-2 */
  font-size: 0.875rem; /* text-sm */
  font-weight: 500; /* font-medium */
  border: 1px solid #474747;
  background: transparent;
  color: #e3e3dd;
  transition: all 0.2s;
  white-space: nowrap;
  user-select: none;
}

.chip:hover:not(.chip-active) {
  background-color: #2a2a2c;
  border-color: #5a5a5a;
}

.chip-active {
  background-color: #86e0cb; /* бирюзовый акцент */
  color: #2d2d2d; /* темный текст */
  border-color: #86e0cb;
  font-weight: 600; /* font-semibold */
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
}
```

### Светлая тема

```css
html[data-theme='light'] .chip {
  border-color: #cbd5f5;
  color: #1f2933;
}

html[data-theme='light'] .chip:hover:not(.chip-active) {
  background-color: #f1f5f9;
  border-color: #94a3b8;
}

html[data-theme='light'] .chip-active {
  background-color: #86e0cb;
  color: #1f2933;
  border-color: #86e0cb;
}
```

---

## 📸 Референс

Дизайн вдохновлен скриншотом из приложения **"Голосовой Тренер"** с кнопками:
- Голос
- Барабан
- Метрон
- Колодка

Круглые кнопки-таблетки (pills) с ярким синим фоном для активного состояния.

В нашем проекте используется фирменный бирюзовый цвет `#86e0cb` вместо синего.

---

## ✅ Результат

- ✅ Чипы выглядят современно и стильно
- ✅ Активное состояние четко выделяется
- ✅ Улучшена читаемость
- ✅ Единообразный стиль на всех страницах
- ✅ Поддержка темной и светлой темы
- ✅ Соответствие гайдлайнам Material Design (круглые кнопки, 44px touch target)

---

## 🔄 Где применяется

| Страница/Компонент | Использование чипов |
|-------------------|---------------------|
| `/documents` | Фильтры типов, статусов, сортировка |
| `/docs/:docType` | Переключение типов документов, статусы |
| `DocumentFilters` | Все фильтры и сортировки |

---

## 📝 Примечания

1. Стили определены в `src/index.css` (строки 183-191)
2. Светлая тема в `src/index.css` (строки 286-300)
3. Классы применяются через Tailwind CSS `@apply` директиву
4. Touch target минимум 44px соблюдается

---

**Файл:** `/DOCS/FILTER_REDESIGN.md`
