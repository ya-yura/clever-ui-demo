# 📋 Формат кастомного интерфейса

**Версия:** 1.0  
**Дата:** 23 ноября 2025

---

## 📖 Обзор

Система поддерживает импорт кастомных интерфейсов в формате JSON. Интерфейс определяет сетку кнопок и их расположение на главном экране.

---

## 📄 Структура JSON

```json
{
  "version": "1.0",
  "metadata": {
    "name": "Custom Interface",
    "description": "Созданный интерфейс для ТСД",
    "created": "2025-11-12T18:06:08.655Z",
    "updated": "2025-11-12T18:06:08.655Z",
    "version": "1.0"
  },
  "grid": {
    "columns": 4,
    "rows": 8
  },
  "buttons": [
    {
      "id": "btn-1",
      "position": {
        "startCol": 0,
        "endCol": 1,
        "startRow": 0,
        "endRow": 1
      },
      "label": "Приход на склад",
      "action": "RECEIVING",
      "style": "light",
      "documentCount": 54,
      "route": "/docs/PrihodNaSklad"
    }
  ]
}
```

---

## 🔧 Поля

### Корневой объект

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `version` | string | Нет | Версия схемы (например, "1.0") |
| `metadata` | object | Да | Метаданные интерфейса |
| `grid` | object | Да | Параметры сетки |
| `buttons` | array | Да | Массив кнопок |

### `metadata`

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `name` | string | Да | Название интерфейса |
| `description` | string | Нет | Описание интерфейса |
| `version` | string | Нет | Версия интерфейса |
| `created` | string | Нет | Дата создания (ISO 8601) |
| `updated` | string | Нет | Дата обновления (ISO 8601) |

### `grid`

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `columns` | number | Да | Количество колонок (обычно 4) |
| `rows` | number | Да | Количество рядов (рекомендуется 6-8) |

### `buttons[]`

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `id` | string | Да | Уникальный идентификатор кнопки |
| `label` | string | Да | Текст на кнопке |
| `action` | string | Да | Действие кнопки (см. [Действия](#действия)) |
| `position` | object | Да | Позиция в сетке |
| `style` | string | Нет | Стиль кнопки: `"light"` или `"dark"` (по умолчанию `"dark"`) |
| `route` | string | Нет | Прямой маршрут (переопределяет `action`) |
| `color` | string | Нет | Явный цвет фона (HEX, RGB) |
| `documentCount` | number | Нет | Статический счетчик документов |

### `position`

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `startCol` | number | Да | Начальная колонка (от 0) |
| `endCol` | number | Да | Конечная колонка (включительно) |
| `startRow` | number | Да | Начальный ряд (от 0) |
| `endRow` | number | Да | Конечный ряд (включительно) |

**Примеры позиций:**
- `{startCol: 0, endCol: 1, startRow: 0, endRow: 1}` — квадрат 2x2 в левом верхнем углу
- `{startCol: 0, endCol: 3, startRow: 2, endRow: 2}` — полная ширина в 3-м ряду
- `{startCol: 2, endCol: 3, startRow: 0, endRow: 0}` — одна ячейка в правом верхнем углу

---

## ⚡ Действия (Actions)

Система поддерживает следующие действия:

| Action | Маршрут | Модуль | Цвет |
|--------|---------|--------|------|
| `RECEIVING` | `/docs/PrihodNaSklad` | Приёмка | `#d89668` (песочный оранжевый) |
| `ORDER_PICKING` | `/docs/PodborZakaza` | Подбор | `#c9a636` (горчичный желтый) |
| `SHIPPING` | `/docs/Otgruzka` | Отгрузка | `#a0d995` (светло-зелёный) |
| `INVENTORY` | `/docs/Inventarizaciya` | Инвентаризация | `#f0a58a` (коралловый розовый) |
| `PLACEMENT` | `/docs/RazmeshhenieVYachejki` | Размещение | `#86e0cb` (мятный) |
| `RETURN` | `/docs/Vozvrat` | Возврат | `#e35454` (красный) |
| `TRANSFER` | `/docs/Peremeshenie` | Перемещение | `#4dd0e1` (голубой) |
| `MARKING` | `/docs/Markirovka` | Маркировка | `#7ed321` (зелёный) |

---

## 🎨 Стили

### `style: "light"`

**Назначение:** Основные (hero) плитки с яркими цветами модулей.

**Поведение:**
- Автоматически применяется цвет модуля на основе `action`
- Текст в контрастном цвете
- Используется для 2-3 главных операций

**Пример:**
```json
{
  "label": "Приход на склад",
  "action": "RECEIVING",
  "style": "light"
}
```
**Результат:** Песочно-оранжевая плитка с тёмным текстом.

---

### `style: "dark"`

**Назначение:** Вторичные плитки с нейтральным серым фоном.

**Поведение:**
- Серый фон (`bg-surface-secondary`)
- Заголовок окрашивается в циклический акцентный цвет
- Используется для дополнительных операций

**Пример:**
```json
{
  "label": "Размещение",
  "action": "PLACEMENT",
  "style": "dark"
}
```
**Результат:** Серая плитка с заголовком в акцентном цвете (циан, зелёный, жёлтый и т.д.).

---

## 📦 Импорт интерфейса

### Способ 1: Через файл

1. Откройте меню (☰)
2. Выберите **"Установить интерфейс"**
3. Вкладка **"Файл"**
4. Выберите JSON-файл с интерфейсом
5. Нажмите **"Загрузить"**

### Способ 2: Через QR-код

1. Откройте меню (☰)
2. Выберите **"Установить интерфейс"**
3. Вкладка **"QR-код"**
4. Отсканируйте QR-код с интерфейсом

### Способ 3: Вставить текстом

1. Откройте меню (☰)
2. Выберите **"Установить интерфейс"**
3. Вкладка **"Текст"**
4. Вставьте JSON в текстовое поле
5. Нажмите **"Загрузить"**

---

## ✅ Валидация

При импорте система проверяет:

1. ✅ Валидность JSON
2. ✅ Наличие обязательных полей (`metadata.name`, `grid`, `buttons`)
3. ✅ Корректность типов данных
4. ✅ Допустимые значения `action`
5. ✅ Корректность позиций (не выходят за границы сетки)

**При ошибке валидации:**
- Выводится сообщение с описанием проблемы
- Интерфейс не применяется
- Текущий интерфейс остаётся без изменений

---

## 🔄 Сброс на стандартный интерфейс

Чтобы вернуться к встроенному интерфейсу:

1. Откройте **DevTools** (F12)
2. Консоль:
   ```javascript
   localStorage.removeItem('ui-schema-active')
   location.reload()
   ```

Или:

1. Откройте меню (☰)
2. **"Настройки"** → **"Сбросить интерфейс"** (если реализовано)

---

## 💡 Рекомендации

### Оптимальная сетка
- **Ширина:** 4 колонки (стандарт для мобильных)
- **Высота:** 6-8 рядов (оптимально для экранов)

### Размещение кнопок
- **Hero tiles (2x2):** 2-3 основных операции (`style: "light"`)
- **Full-width (4x1):** Важные, но не частые операции
- **Secondary (2x1 или 1x1):** Дополнительные функции (`style: "dark"`)

### Производительность
- Не используйте более 12-15 кнопок
- Избегайте кастомных цветов (`color`) — используйте `action` для автоматического маппинга

### Доступность
- Заголовки не длиннее 20 символов
- Используйте понятные короткие названия
- Группируйте связанные операции визуально

---

## 🐛 Отладка

### Проверить загруженный интерфейс

```javascript
const schema = localStorage.getItem('ui-schema-active');
console.log(JSON.parse(schema));
```

### Проверить маршруты

```javascript
import { ACTION_ROUTES } from '@/types/ui-schema';
console.log(ACTION_ROUTES);
```

### Проверить цвета модулей

```javascript
const root = document.documentElement;
console.log(getComputedStyle(root).getPropertyValue('--color-module-receiving-bg'));
```

---

## 📚 Примеры

### Пример 1: Минимальный интерфейс

```json
{
  "metadata": {
    "name": "Простой интерфейс"
  },
  "grid": {
    "columns": 4,
    "rows": 4
  },
  "buttons": [
    {
      "id": "btn-1",
      "label": "Приход",
      "action": "RECEIVING",
      "position": { "startCol": 0, "endCol": 3, "startRow": 0, "endRow": 0 },
      "style": "light"
    },
    {
      "id": "btn-2",
      "label": "Отгрузка",
      "action": "SHIPPING",
      "position": { "startCol": 0, "endCol": 3, "startRow": 1, "endRow": 1 },
      "style": "light"
    }
  ]
}
```

### Пример 2: Сложный интерфейс с группировкой

```json
{
  "metadata": {
    "name": "Складской интерфейс Pro"
  },
  "grid": {
    "columns": 4,
    "rows": 8
  },
  "buttons": [
    {
      "id": "hero-receiving",
      "label": "Приход",
      "action": "RECEIVING",
      "position": { "startCol": 0, "endCol": 1, "startRow": 0, "endRow": 1 },
      "style": "light"
    },
    {
      "id": "hero-picking",
      "label": "Подбор",
      "action": "ORDER_PICKING",
      "position": { "startCol": 2, "endCol": 3, "startRow": 0, "endRow": 1 },
      "style": "light"
    },
    {
      "id": "inventory",
      "label": "Инвентаризация",
      "action": "INVENTORY",
      "position": { "startCol": 0, "endCol": 3, "startRow": 2, "endRow": 2 },
      "style": "dark"
    },
    {
      "id": "placement",
      "label": "Размещение",
      "action": "PLACEMENT",
      "position": { "startCol": 0, "endCol": 1, "startRow": 3, "endRow": 3 },
      "style": "dark"
    },
    {
      "id": "return",
      "label": "Возврат",
      "action": "RETURN",
      "position": { "startCol": 2, "endCol": 3, "startRow": 3, "endRow": 3 },
      "style": "dark"
    }
  ]
}
```

---

**Документация актуальна:** 23 ноября 2025  
**Поддержка:** Cleverence Development Team

