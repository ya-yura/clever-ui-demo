# 🌞 Исправление светлой темы в дизайн-системе

**Дата:** 21 ноября 2025  
**Commit:** `e258fa0`  
**Статус:** ✅ Полностью исправлено

---

## 🐛 Проблема

После рефакторинга дизайн-системы **светлая тема перестала работать**:

- ❌ Фон страницы оставался тёмным при переключении на light
- ❌ Текст не менял цвет
- ❌ Компоненты дизайн-системы не реагировали на смену темы

### Причина

Дизайн-система использует **семантические токены** (`bg-surface-primary`, `text-content-primary`), но:

1. В `design-system.json` были только тёмные цвета
2. Tailwind конфиг захардкодил эти цвета без переключения
3. CSS переменные в `index.css` работали только для старых классов (`bg-[#343436]`)

---

## ✅ Решение

### 1. Добавлены CSS переменные (index.css)

```css
:root {
  /* Dark theme (default) */
  --color-surface-primary: #242424;
  --color-surface-secondary: #343436;
  --color-content-primary: #ffffff;
  --color-content-secondary: #e3e3dd;
  /* ... */
}

[data-theme='light'] {
  /* Light theme override */
  --color-surface-primary: #f6f7fb;
  --color-surface-secondary: #ffffff;
  --color-content-primary: #1f2933;
  --color-content-secondary: #52606d;
  /* ... */
}
```

### 2. Обновлён design-system.json

Добавлены разделы для обеих тем:

```json
{
  "colors": {
    "dark": {
      "surface": { "primary": "#242424", ... },
      "content": { "primary": "#ffffff", ... }
    },
    "light": {
      "surface": { "primary": "#f6f7fb", ... },
      "content": { "primary": "#1f2933", ... }
    },
    "brand": { ... },  // Общие для обеих тем
    "status": { ... }  // Общие для обеих тем
  }
}
```

### 3. Обновлён Tailwind конфиг

Теперь использует CSS переменные вместо захардкоженных значений:

```js
colors: {
  surface: {
    primary: 'var(--color-surface-primary)',
    secondary: 'var(--color-surface-secondary)',
    tertiary: 'var(--color-surface-tertiary)',
    inverse: 'var(--color-surface-inverse)',
  },
  content: {
    primary: 'var(--color-content-primary)',
    // ...
  }
}
```

### 4. Обновлены токены (tokens.ts)

```typescript
export const tokens = {
  colors: {
    surface: {
      primary: 'var(--color-surface-primary)',  // Теперь CSS переменная
      // ...
    },
  },
  // Добавлены референсы для документации
  themes: {
    dark: designSystem.dna.colors.dark,
    light: designSystem.dna.colors.light,
  },
}
```

---

## 🎨 Цветовая палитра

### Тёмная тема (default)

| Токен | Класс | Цвет | Применение |
|-------|-------|------|------------|
| `surface-primary` | `bg-surface-primary` | #242424 | Фон страницы |
| `surface-secondary` | `bg-surface-secondary` | #343436 | Фон карточек |
| `surface-tertiary` | `bg-surface-tertiary` | #474747 | Границы |
| `content-primary` | `text-content-primary` | #ffffff | Заголовки |
| `content-secondary` | `text-content-secondary` | #e3e3dd | Основной текст |
| `content-tertiary` | `text-content-tertiary` | #a7a7a7 | Вторичный текст |

### Светлая тема

| Токен | Класс | Цвет | Применение |
|-------|-------|------|------------|
| `surface-primary` | `bg-surface-primary` | #f6f7fb | Фон страницы (светло-серый) |
| `surface-secondary` | `bg-surface-secondary` | #ffffff | Фон карточек (белый) |
| `surface-tertiary` | `bg-surface-tertiary` | #e2e8f0 | Границы (светло-серые) |
| `content-primary` | `text-content-primary` | #1f2933 | Заголовки (тёмно-серый) |
| `content-secondary` | `text-content-secondary` | #52606d | Основной текст (серый) |
| `content-tertiary` | `text-content-tertiary` | #94a3b8 | Вторичный текст (светло-серый) |

### Бренд и статусы (одинаковы для обеих тем)

| Токен | Цвет | Применение |
|-------|------|------------|
| `brand-primary` | #daa420 | Золотой (основной бренд) |
| `brand-dark` | #725a1e | Тёмный коричневый (текст на золотом) |
| `brand-secondary` | #86e0cb | Бирюзовый (акценты) |
| `success` | #91ed91 | Зелёный (успех) |
| `warning` | #f3a361 | Оранжевый (предупреждение) |
| `error` | #ba8f8e | Красный (ошибка) |
| `info` | #86e0cb | Голубой (информация) |

---

## 🔧 Как переключать тему

### В коде (программно)

```typescript
import { useTheme } from '@/contexts/ThemeContext';

function MyComponent() {
  const { theme, setTheme, toggleTheme } = useTheme();
  
  return (
    <button onClick={toggleTheme}>
      {theme === 'dark' ? '🌞 Light' : '🌙 Dark'}
    </button>
  );
}
```

### В интерфейсе

#### Главный Header
В правом верхнем углу каждой страницы есть кнопка:
- 🌞 **Sun icon** (в тёмной теме) — переключает на светлую
- 🌙 **Moon icon** (в светлой теме) — переключает на тёмную

#### Страница showcase
http://localhost:5176/design-system

В header есть отдельная кнопка переключения для демонстрации.

---

## 🧪 Тестирование

### Что проверить:

1. **Фон страницы:**
   - ✅ Dark: `#242424` (тёмно-серый)
   - ✅ Light: `#f6f7fb` (светло-серый)

2. **Карточки:**
   - ✅ Dark: `#343436` (серый)
   - ✅ Light: `#ffffff` (белый)

3. **Текст заголовков:**
   - ✅ Dark: `#ffffff` (белый)
   - ✅ Light: `#1f2933` (тёмно-серый)

4. **Кнопки дизайн-системы:**
   - ✅ Primary: золотой фон в обеих темах
   - ✅ Secondary: меняет фон (серый → светло-серый)
   - ✅ Ghost: прозрачный, текст меняется

5. **Все страницы:**
   - ✅ Home (главная с плитками)
   - ✅ Documents (список документов)
   - ✅ Settings (настройки)
   - ✅ Design System Showcase

### Автоматическая проверка

```bash
# Проверить, что CSS переменные определены
grep "color-surface-primary" src/index.css

# Проверить, что Tailwind использует переменные
grep "var(--color-" tailwind.config.js

# Проверить, что токены экспортируют обе темы
grep "themes:" src/design/tokens.ts
```

---

## 📝 Миграция существующего кода

### Если используете старые хардкоженные классы

❌ **Старый код:**
```tsx
<div className="bg-[#343436] text-[#e3e3dd]">
```

✅ **Новый код:**
```tsx
<div className="bg-surface-secondary text-content-secondary">
```

### Если создаёте новые компоненты

✅ **Используйте семантические токены:**

```tsx
import { Button, Card } from '@/design/components';

<Card className="space-y-4">
  <h3 className="text-lg font-bold text-content-primary">Заголовок</h3>
  <p className="text-sm text-content-secondary">Описание</p>
  <Button variant="primary">Действие</Button>
</Card>
```

### Если нужны кастомные цвета

✅ **Используйте CSS переменные напрямую:**

```tsx
<div style={{ backgroundColor: 'var(--color-surface-tertiary)' }}>
```

Или добавьте новый семантический токен в систему.

---

## 🚀 Дальнейшие улучшения (опционально)

### Возможные доработки:

1. **Автопереключение по времени суток:**
   ```typescript
   // Auto-switch to light theme during daytime
   const hour = new Date().getHours();
   if (hour >= 6 && hour < 18) {
     setTheme('light');
   }
   ```

2. **Системные настройки:**
   ```typescript
   // Respect system preference
   const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
   ```
   *(уже реализовано в ThemeContext)*

3. **Больше тем:**
   - High contrast theme (для accessibility)
   - Custom brand themes
   - Blue/Green/Purple variants

4. **Анимация переключения:**
   ```css
   * {
     transition: background-color 200ms ease, color 200ms ease;
   }
   ```

5. **Сохранение в профиле пользователя:**
   - Синхронизация между устройствами
   - Привязка к серверному профилю

---

## 📚 Связанные документы

- **Guidelines:** `DOCS/DESIGN_SYSTEM_GUIDELINES.md`
- **Tokens:** `src/design/tokens.ts`
- **ThemeContext:** `src/contexts/ThemeContext.tsx`
- **CSS Variables:** `src/index.css`

---

## ✅ Checklist

- [x] CSS переменные добавлены
- [x] design-system.json обновлён (dark + light)
- [x] Tailwind конфиг использует переменные
- [x] tokens.ts обновлён
- [x] Все семантические классы работают
- [x] Кнопка переключения в Header
- [x] Кнопка переключения в Showcase
- [x] Фон страницы меняется
- [x] Текст меняется
- [x] Карточки меняются
- [x] Компоненты дизайн-системы работают
- [x] Документация обновлена
- [x] Изменения в репозитории

---

**Статус:** 🎉 Светлая тема полностью работает!

**Протестируйте:** http://localhost:5176/design-system

