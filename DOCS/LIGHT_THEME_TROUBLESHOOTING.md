# 🔧 Проверка светлой темы для плиток

**Дата:** 16 ноября 2025  
**Статус:** Инструкция по проверке

---

## 📋 Что было исправлено

Добавлены более специфичные CSS селекторы для плиток с классом `.tile-outline` в светлой теме:

### Изменения в `src/index.css`:

1. **Фон плиток** — светло-серый `#f1f5f9`
2. **Обводка** — средне-серая `#94a3b8`, толщина 2px
3. **Заголовки** — темные оттенки цветных акцентов
4. **Текст описания** — темно-серый `#52606d`
5. **Счетчики** — темный `#1f2933`

---

## 🔍 Как проверить, что изменения работают

### 1. Убедитесь, что атрибут `data-theme` меняется

Откройте **DevTools** (F12) → **Elements** → найдите тег `<html>`:

**В темной теме:**
```html
<html data-theme="dark">
```

**В светлой теме:**
```html
<html data-theme="light">
```

### 2. Очистите кэш браузера

**Способ 1: Жесткое обновление**
- `Ctrl + Shift + R` (Windows/Linux)
- `Cmd + Shift + R` (Mac)

**Способ 2: Очистка через DevTools**
1. Откройте DevTools (F12)
2. Кликните правой кнопкой на кнопку обновления
3. Выберите "Empty Cache and Hard Reload"

### 3. Проверьте применение стилей

Откройте **DevTools** → **Elements** → выберите плитку "Возврат" или "Инвентаризация"

**Должны быть применены стили:**
```css
html[data-theme='light'] .tile-outline {
  background-color: #f1f5f9 !important;
  border-color: #94a3b8 !important;
  border-width: 2px !important;
}
```

### 4. Проверьте цвета текста

**В светлой теме** текст в плитках `.tile-outline` должен быть:

- **Заголовок "Возврат"** — темно-бирюзовый `#2d7a6b`
- **Заголовок "Размещение"** — темно-зеленый `#2d6b2d`
- **Заголовок "Инвентаризация"** — темно-розовый `#6b3d3c`
- **Описание** — серый `#52606d`
- **Счетчик** — темный `#1f2933`

---

## 🎨 Ожидаемый результат

### Темная тема:
- Плитки: прозрачные, темно-серая обводка (1px)
- Текст: светлый (белый, светло-бирюзовый, светло-зеленый, светло-розовый)

### Светлая тема:
- Плитки: светло-серый фон `#f1f5f9`, средне-серая обводка (2px)
- Текст: темный (темно-бирюзовый, темно-зеленый, темно-розовый, серый)

---

## 🐛 Если изменения не применяются

### Проблема 1: Кэш не очищен

**Решение:**
- Сделайте жесткое обновление (Ctrl+Shift+R)
- Или откройте в режиме инкогнито

### Проблема 2: Старая версия файла CSS

**Решение:**
```bash
cd O:\Dev\Cleverence\proto-3
git pull
npm run dev
```

### Проблема 3: Атрибут data-theme не меняется

**Проверьте:**
- Открыт ли `ThemeProvider` в корне приложения
- Работает ли кнопка переключения темы в шапке

**Решение:**
Проверьте файл `src/main.tsx` — должен быть обернут в `<ThemeProvider>`:

```tsx
<ThemeProvider>
  <App />
</ThemeProvider>
```

### Проблема 4: CSS правила не применяются

**Проверьте специфичность:**

В DevTools → Elements → выберите элемент → вкладка "Computed"

Если стили перечеркнуты, значит они переопределены более специфичным селектором.

**Решение:**
Все наши правила используют `!important`, поэтому они должны применяться.

---

## 📱 Тестирование

### Шаги для полного теста:

1. Откройте главную страницу `/`
2. Переключитесь в светлую тему (иконка солнца/луны в шапке)
3. Проверьте плитки:
   - "Возврат" (левая нижняя)
   - "Размещение в ячейки" (правая нижняя)
   - "Инвентаризация" (полная ширина внизу)

### Ожидаемый вид (светлая тема):

- Фон плиток: очень светло-серый (едва заметный)
- Обводка: заметная средне-серая (2px)
- Заголовки: темные, контрастные
- Описание: серый, читаемый
- Счетчики: темные

---

## 🔄 Откат изменений (если что-то пошло не так)

```bash
cd O:\Dev\Cleverence\proto-3
git revert HEAD
git push
```

---

## 📞 Контакты

Если проблема не решена, сообщите:
- Версию браузера
- Скриншот DevTools с примененными стилями
- Значение атрибута `data-theme` на элементе `<html>`

---

**Файл:** `/DOCS/LIGHT_THEME_TROUBLESHOOTING.md`

