# 💻 Использование через командную строку

> Руководство по загрузке данных через Node.js скрипты

## Содержание

1. [Обзор](#обзор)
2. [Базовое использование](#базовое-использование)
3. [Параметры командной строки](#параметры-командной-строки)
4. [Примеры использования](#примеры-использования)
5. [Автоматизация](#автоматизация)
6. [Устранение проблем](#устранение-проблем)

---

## Обзор

Node.js скрипт `fetchServerData.js` позволяет загружать данные с сервера через командную строку.

### Преимущества

- ✅ Автоматизация загрузки
- ✅ Интеграция в CI/CD
- ✅ Пакетная обработка
- ✅ Гибкая настройка
- ✅ Подробное логирование

### Расположение

```
scripts/
├── fetchServerData.js      # Рабочая версия (Node.js)
└── fetchServerData.ts      # Исходный код (TypeScript)
```

---

## Базовое использование

### NPM команды

#### Загрузка с сервера по умолчанию

```bash
npm run fetch-data
```

Эквивалентно:
```bash
node scripts/fetchServerData.js http://localhost:9000/MobileSMARTS/api/v1 ./src/data/demo
```

#### Загрузка с кастомного сервера

```bash
npm run fetch-data:custom http://your-server:9000/MobileSMARTS/api/v1
```

---

## Параметры командной строки

### Синтаксис

```bash
node scripts/fetchServerData.js [serverUrl] [outputDir] [username] [password]
```

### Параметры

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `serverUrl` | string | `http://localhost:9000/MobileSMARTS/api/v1` | URL API сервера |
| `outputDir` | string | `../src/data/demo` | Директория для JSON файлов |
| `username` | string | - | Имя пользователя (опционально) |
| `password` | string | - | Пароль (опционально) |

### Примечания

- Все параметры опциональны
- Параметры указываются в строгом порядке
- Пропущенные параметры заменяются значениями по умолчанию

---

## Примеры использования

### Пример 1: Базовая загрузка

```bash
# Загрузка с локального сервера
npm run fetch-data
```

**Что произойдет:**
- Подключение к `http://localhost:9000/MobileSMARTS/api/v1`
- Загрузка всех данных (типы, документы, товары и т.д.)
- Сохранение в `src/data/demo/`

**Вывод:**
```
═══════════════════════════════════════════════
  MobileSMARTS Data Fetcher
═══════════════════════════════════════════════
Server: http://localhost:9000/MobileSMARTS/api/v1
Output: src/data/demo
═══════════════════════════════════════════════

🚀 Starting data fetch...

📋 Fetching document types...
🔍 Fetching: /DocTypes
✅ Success: /DocTypes (200)
✅ Saved: src/data/demo/doctypes.json

📄 Fetching documents...
📄 Fetching documents for: ПриходНаСклад (PrihodNaSklad)
   ✅ Found 25 documents
...

✅ Data fetch complete!

📊 Statistics:
   Document Types: 15
   Documents: 123
   Products: 350
   Cells: 120
   Partners: 45
   Employees: 12
   Warehouses: 3

📁 Output directory: src/data/demo
```

### Пример 2: Кастомный сервер

```bash
node scripts/fetchServerData.js http://192.168.1.100:9000/MobileSMARTS/api/v1
```

**Использование:**
- Загрузка с удаленного сервера
- Тестирование разных серверов
- Production данные

### Пример 3: Кастомная папка

```bash
node scripts/fetchServerData.js http://localhost:9000/MobileSMARTS/api/v1 ./backup/demo-data
```

**Использование:**
- Сохранение в другую папку
- Создание резервных копий
- Версионирование данных

### Пример 4: С авторизацией

```bash
node scripts/fetchServerData.js http://localhost:9000/MobileSMARTS/api/v1 ./src/data/demo admin MyPassword123
```

**Использование:**
- Сервер требует авторизацию
- Загрузка данных с правами конкретного пользователя

### Пример 5: Production сервер с авторизацией

```bash
node scripts/fetchServerData.js https://warehouse.company.com/MobileSMARTS/api/v1 ./production-data warehouse_user SecurePass456
```

**Использование:**
- Production сервер
- Экспорт данных для анализа
- Миграция данных

---

## Автоматизация

### Bash скрипт

Создайте `fetch-daily.sh`:

```bash
#!/bin/bash

# Конфигурация
SERVER="http://localhost:9000/MobileSMARTS/api/v1"
OUTPUT_DIR="./src/data/demo"
BACKUP_DIR="./backups/$(date +%Y-%m-%d)"

# Создать backup
mkdir -p "$BACKUP_DIR"
cp -r "$OUTPUT_DIR"/* "$BACKUP_DIR/"

# Загрузить новые данные
node scripts/fetchServerData.js "$SERVER" "$OUTPUT_DIR"

echo "✅ Daily data fetch complete!"
echo "📁 Backup: $BACKUP_DIR"
```

### Windows PowerShell

Создайте `fetch-daily.ps1`:

```powershell
# Конфигурация
$Server = "http://localhost:9000/MobileSMARTS/api/v1"
$OutputDir = "./src/data/demo"
$BackupDir = "./backups/$(Get-Date -Format 'yyyy-MM-dd')"

# Создать backup
New-Item -ItemType Directory -Force -Path $BackupDir
Copy-Item "$OutputDir/*" -Destination $BackupDir -Recurse

# Загрузить новые данные
node scripts/fetchServerData.js $Server $OutputDir

Write-Host "✅ Daily data fetch complete!"
Write-Host "📁 Backup: $BackupDir"
```

### Cron задача (Linux/Mac)

Добавьте в crontab:

```bash
# Загрузка данных каждый день в 2:00 ночи
0 2 * * * cd /path/to/proto-3 && npm run fetch-data >> /var/log/fetch-data.log 2>&1
```

### Task Scheduler (Windows)

1. Откройте "Планировщик заданий"
2. Создайте новую задачу
3. Триггер: Ежедневно в 2:00
4. Действие: `node` с аргументами:
   ```
   scripts/fetchServerData.js http://localhost:9000/MobileSMARTS/api/v1 ./src/data/demo
   ```

---

## Логирование

### Формат вывода

Скрипт использует эмодзи и цветное логирование:

```
🚀 - Начало работы
🔍 - Запрос данных
✅ - Успех
❌ - Ошибка
⚠️ - Предупреждение
📁 - Файловая операция
📊 - Статистика
```

### Перенаправление в файл

```bash
# Сохранить вывод в файл
npm run fetch-data > fetch-log.txt 2>&1

# Добавить к существующему логу
npm run fetch-data >> fetch-log.txt 2>&1
```

### Тихий режим

```bash
# Только ошибки
npm run fetch-data 2>&1 | grep "❌"

# Только успехи
npm run fetch-data 2>&1 | grep "✅"
```

---

## Интеграция в workflow

### Package.json скрипты

```json
{
  "scripts": {
    "fetch-data": "node scripts/fetchServerData.js",
    "fetch-data:prod": "node scripts/fetchServerData.js https://prod-server/api/v1 ./prod-data",
    "fetch-data:dev": "node scripts/fetchServerData.js http://localhost:9000/MobileSMARTS/api/v1",
    "fetch-data:backup": "node scripts/fetchServerData.js http://localhost:9000/MobileSMARTS/api/v1 ./backups/$(date +%Y-%m-%d)"
  }
}
```

### Использование

```bash
# Development
npm run fetch-data:dev

# Production
npm run fetch-data:prod

# Backup
npm run fetch-data:backup
```

---

## Возвращаемые коды

| Код | Значение |
|-----|----------|
| 0 | Успех - все данные загружены |
| 1 | Ошибка - проверьте лог |

### Использование в скриптах

```bash
#!/bin/bash

npm run fetch-data
if [ $? -eq 0 ]; then
    echo "✅ Data fetched successfully"
    # Дополнительные действия
else
    echo "❌ Data fetch failed"
    exit 1
fi
```

---

## Расширенные возможности

### Создание версий данных

```bash
# Создать папку с датой
OUTPUT="./data-versions/$(date +%Y-%m-%d-%H%M%S)"
node scripts/fetchServerData.js http://localhost:9000/MobileSMARTS/api/v1 "$OUTPUT"
```

### Сравнение версий

```bash
# Загрузить в две разные папки
node scripts/fetchServerData.js http://server1:9000/api/v1 ./data-v1
node scripts/fetchServerData.js http://server2:9000/api/v1 ./data-v2

# Сравнить
diff data-v1/doctypes.json data-v2/doctypes.json
```

---

## Следующие шаги

После успешной загрузки через CLI:

1. Проверьте созданные файлы в выходной директории
2. Убедитесь, что файлы не пустые
3. Скопируйте в `src/data/demo/` если нужно
4. Перезапустите приложение

См. также:
- [05-DEMO-MODE.md](./05-DEMO-MODE.md) - Работа с демо-режимом
- [07-TROUBLESHOOTING.md](./07-TROUBLESHOOTING.md) - Решение проблем

---

**Версия:** 1.0.0  
**Обновлено:** 1 декабря 2024







