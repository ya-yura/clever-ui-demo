# 🎨 Настройка Figma MCP для Cursor

**Дата создания:** 20 ноября 2025  
**Цель:** Дать AI ассистенту доступ к файлам Figma для анализа дизайна

---

## 📋 Что такое Figma MCP?

**MCP (Model Context Protocol)** — это протокол, позволяющий AI ассистентам подключаться к внешним сервисам.

**Figma MCP сервер** — это мост между Cursor AI и Figma API, который позволяет:
- Читать файлы Figma
- Извлекать дизайн-токены (цвета, шрифты, spacing)
- Анализировать компоненты
- Экспортировать элементы

---

## 🚀 Пошаговая настройка

### Шаг 1: Получить Personal Access Token из Figma

#### 1.1 Войти в Figma
Откройте [figma.com](https://figma.com) и войдите в свой аккаунт.

#### 1.2 Перейти в Settings
1. Кликните на свой аватар (правый верхний угол)
2. Выберите **Settings**

#### 1.3 Создать токен
1. Прокрутите до раздела **Personal Access Tokens**
2. Введите описание токена (например: "Cursor AI - Cleverence")
3. Нажмите **Generate new token**
4. **ВАЖНО:** Скопируйте токен сразу! Он показывается только один раз
5. Сохраните токен в безопасном месте (например, в менеджере паролей)

**Формат токена:** `figd_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX`

---

### Шаг 2: Установить Figma MCP сервер

Figma MCP сервер доступен через npm пакет.

#### 2.1 Установка через npm (глобально)

Откройте терминал PowerShell и выполните:

```powershell
npm install -g @modelcontextprotocol/server-figma
```

**Или** установка через npx (без глобальной установки):

```powershell
# Проверка доступности
npx @modelcontextprotocol/server-figma --version
```

#### 2.2 Альтернатива: Клонировать репозиторий

Если пакет не найден в npm, можно использовать официальный репозиторий:

```powershell
# Перейти в папку для MCP серверов
cd $HOME
mkdir mcp-servers
cd mcp-servers

# Клонировать репозиторий
git clone https://github.com/modelcontextprotocol/servers.git
cd servers/src/figma

# Установить зависимости
npm install

# Собрать проект
npm run build
```

---

### Шаг 3: Настроить Cursor

#### 3.1 Найти конфигурационный файл Cursor

Cursor использует файл конфигурации для MCP серверов. Расположение:

**Windows:**
```
C:\Users\<Ваше_имя>\AppData\Roaming\Cursor\User\globalStorage\config.json
```

**Или:**
```
%APPDATA%\Cursor\User\globalStorage\config.json
```

#### 3.2 Создать/отредактировать конфигурацию MCP

Если файл не существует, создайте его. Структура:

```json
{
  "mcpServers": {
    "figma": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-figma"
      ],
      "env": {
        "FIGMA_PERSONAL_ACCESS_TOKEN": "figd_ВАХХХХХХХХХХХХХХХХХХХХХХХХХХХХХХХ"
      }
    }
  }
}
```

#### 3.3 Если установлено локально

Если вы клонировали репозиторий в `C:\Users\Game-PC\mcp-servers\servers\src\figma`:

```json
{
  "mcpServers": {
    "figma": {
      "command": "node",
      "args": [
        "C:\\Users\\Game-PC\\mcp-servers\\servers\\src\\figma\\build\\index.js"
      ],
      "env": {
        "FIGMA_PERSONAL_ACCESS_TOKEN": "figd_ВАХХХХХХХХХХХХХХХХХХХХХХХХХХХХХХХ"
      }
    }
  }
}
```

**Важно:** 
- Используйте двойные обратные слеши `\\` в Windows путях
- Замените `figd_ВАШ_ТОКЕН` на реальный токен из Figma

---

### Шаг 4: Перезапустить Cursor

1. Полностью закройте Cursor (File → Exit)
2. Откройте снова
3. MCP сервер должен автоматически подключиться

---

## ✅ Проверка работы

### Тест 1: Проверка в Cursor

Спросите меня (AI ассистента):

> "Прочитай файл Figma с ключом gwuHD3Vqm68VwQFghwNRD9"

Я должен суметь вызвать:
```typescript
call_mcp_tool("figma", "get_file", { file_key: "gwuHD3Vqm68VwQFghwNRD9" })
```

### Тест 2: Проверка через терминал

```powershell
# Установить переменную окружения
$env:FIGMA_PERSONAL_ACCESS_TOKEN="figd_ВАШ_ТОКЕН"

# Запустить сервер напрямую
npx @modelcontextprotocol/server-figma
```

---

## 🔧 Troubleshooting

### ❌ "MCP server 'figma' is not available"

**Причины:**
1. Конфигурация не сохранена правильно
2. Cursor не перезапущен
3. Неверный путь к серверу
4. Токен не установлен

**Решения:**
- Проверьте JSON синтаксис конфигурации (запятые, кавычки)
- Убедитесь, что токен правильно вставлен
- Перезапустите Cursor полностью
- Проверьте логи Cursor: `Help → Toggle Developer Tools → Console`

### ❌ "Invalid token"

**Причины:**
- Токен скопирован не полностью
- Лишние пробелы в начале/конце
- Токен отозван в Figma

**Решения:**
- Создайте новый токен в Figma Settings
- Проверьте, что токен начинается с `figd_`

### ❌ Пакет не найден

**Если `@modelcontextprotocol/server-figma` не существует:**

1. **Поиск альтернатив:**
   - Проверьте официальный репозиторий: https://github.com/modelcontextprotocol
   - Найдите актуальное название пакета

2. **Самописный MCP сервер:**
   Если официального нет, можно создать простой Node.js сервер:

```javascript
// figma-mcp-server.js
const axios = require('axios');

const FIGMA_TOKEN = process.env.FIGMA_PERSONAL_ACCESS_TOKEN;

async function getFigmaFile(fileKey) {
  const response = await axios.get(
    `https://api.figma.com/v1/files/${fileKey}`,
    {
      headers: {
        'X-Figma-Token': FIGMA_TOKEN
      }
    }
  );
  return response.data;
}

// MCP protocol handler
process.stdin.on('data', async (data) => {
  const request = JSON.parse(data.toString());
  
  if (request.method === 'get_file') {
    const result = await getFigmaFile(request.params.file_key);
    console.log(JSON.stringify({ result }));
  }
});
```

---

## 📚 Доступные MCP методы (Figma)

После настройки будут доступны:

### `get_file`
Получить структуру файла Figma
```json
{
  "file_key": "gwuHD3Vqm68VwQFghwNRD9"
}
```

### `get_file_nodes`
Получить конкретные ноды
```json
{
  "file_key": "gwuHD3Vqm68VwQFghwNRD9",
  "node_ids": ["11547-229"]
}
```

### `get_image`
Экспорт изображений
```json
{
  "file_key": "gwuHD3Vqm68VwQFghwNRD9",
  "node_ids": ["11547-229"],
  "format": "png",
  "scale": 2
}
```

### `get_comments`
Получить комментарии из файла
```json
{
  "file_key": "gwuHD3Vqm68VwQFghwNRD9"
}
```

---

## 🎯 Что я смогу делать после настройки?

✅ **Анализ дизайна:**
- Извлечение цветовой палитры
- Анализ типографики
- Spacing и grid системы
- Компоненты и их варианты

✅ **Генерация кода:**
- Tailwind конфиг из Figma цветов
- React компоненты по макету
- Design tokens (JSON/TS)
- CSS переменные

✅ **Автоматизация:**
- Синхронизация дизайна с кодом
- Обновление токенов при изменениях
- Проверка соответствия макету

---

## 📖 Полезные ссылки

- [Figma API Documentation](https://www.figma.com/developers/api)
- [Model Context Protocol](https://modelcontextprotocol.io)
- [Cursor Documentation](https://docs.cursor.com)

---

## 📝 Пример финальной конфигурации

**Файл:** `%APPDATA%\Cursor\User\globalStorage\config.json`

```json
{
  "mcpServers": {
    "figma": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-figma"],
      "env": {
        "FIGMA_PERSONAL_ACCESS_TOKEN": "figd_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
      }
    }
  }
}
```

---

## ✨ После настройки

Напишите мне:

> "Изучи макет из Figma: gwuHD3Vqm68VwQFghwNRD9"

И я смогу:
1. Прочитать файл
2. Извлечь дизайн-систему
3. Создать `design-system.json`
4. Сгенерировать компоненты
5. Настроить Tailwind

---

**Статус:** 📝 Инструкция готова  
**Следующий шаг:** Настройте MCP и вернитесь ко мне для анализа дизайна!


