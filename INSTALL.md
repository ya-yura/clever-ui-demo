# 📦 Инструкция по установке Склад-15

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Генерация иконок для PWA

Откройте файл `scripts/generate-icons.html` в браузере. Иконки будут автоматически сгенерированы и скачаны. Поместите их в папку `public/icons/`.

Или используйте любой онлайн-генератор иконок PWA, например:
- https://www.pwabuilder.com/imageGenerator
- https://realfavicongenerator.net/

Необходимые размеры: 72x72, 96x96, 128x128, 144x144, 152x152, 192x192, 384x384, 512x512

## Шаг 3: Конфигурация API

Отредактируйте файл `src/config/server.json`:

```json
{
  "apiBaseUrl": "https://your-api-server.com/api",
  "syncIntervalSec": 60,
  "authMode": "token",
  "demoMode": false
}
```

## Шаг 4: Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

## Шаг 5: Сборка для production

```bash
npm run build
```

Результат сборки будет в папке `dist/`

## Шаг 6: Тестирование PWA

Для полноценного тестирования PWA используйте production сборку:

```bash
npm run preview
```

Или разверните содержимое `dist/` на HTTPS-сервере.

## 🧪 Тестирование модулей

### Модуль Приёмка:
1. Откройте модуль "📦 Приёмка"
2. Наберите на клавиатуре: `DOC-001` и нажмите Enter
3. Документ загрузится с тестовыми товарами
4. Наберите штрихкод товара: `1234567890` и Enter
5. Количество принятого товара увеличится

### Модуль Сбор штрихкодов:
1. Откройте модуль "📋 Сбор штрихкодов"
2. Сканируйте любые штрихкоды подряд
3. Экспортируйте в CSV или TXT

### Оффлайн-режим:
1. Откройте DevTools → Network → Offline
2. Продолжайте работу — все данные сохраняются локально
3. Включите сеть — данные синхронизируются автоматически

## 📱 Установка PWA на устройство

### Android Chrome:
1. Откройте приложение в Chrome
2. Нажмите меню (⋮) → "Установить приложение"
3. Подтвердите установку

### iOS Safari:
1. Откройте приложение в Safari
2. Нажмите "Поделиться" (кнопка со стрелкой)
3. Выберите "На экран Домой"

### Desktop Chrome:
1. В адресной строке появится иконка установки (+)
2. Нажмите для установки PWA

## 🔧 Устранение проблем

### Проблема: Service Worker не регистрируется
- Убедитесь, что приложение открыто через HTTPS или localhost
- Проверьте консоль браузера на ошибки

### Проблема: Иконки не отображаются
- Проверьте наличие файлов в `public/icons/`
- Убедитесь, что пути в `manifest.json` корректны

### Проблема: Синхронизация не работает
- Проверьте настройки в `src/config/server.json`
- Убедитесь, что API сервер доступен
- Проверьте наличие токена авторизации

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории проекта.

© 2024 Cleverence

