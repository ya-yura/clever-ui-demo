
# 🚀 **MASTER PROMPT ДЛЯ CLAUDE 4.5 / CURSOR**

### **Собрать полностью рабочий PWA-комплекс «Склад-15» с новым UX, метриками и системой команд**

---

## 📌 **ВСТАВЬ В CURSOR БЕЗ ИЗМЕНЕНИЙ**

---

> **Claude, ты должен полностью собрать производственную версию мобильного PWA-приложения «Склад-15» в архитектуре модульного фронтенда.
>
> Все файлы, каталоги и компоненты — создавай самостоятельно, без моего участия.
>
> Организуй весь проект, как будто ты старший фронтенд-архитектор.
>
> Все изменения должны быть совместимы, чисты, поддерживаемы и расширяемы.
>
> Код — выводи строго пакетами, с корректным расположением файлов.**

---

# 🏗️ **1. Архитектура приложения**

Создай PWA-проект **React + TypeScript + Vite** с:

* TailwindCSS
* Framer Motion
* Zustand (или Jotai) для состояния
* React Router
* Workbox для оффлайн-режима
* IndexedDB через Dexie для локального кэша
* Абстракцией API `/src/api/`
* Юнитами операций:

  * Приёмка
  * Подбор
  * Отгрузка
  * Возврат/Списание
  * Инвентаризация
  * Сбор штрихкодов
  * Печать этикеток
  * Настройки
* Универсальным компонентом поиска
* Служебным слоем метрик
* Системой выбора напарника
* Механизмом онбординга (несколько вариантов)

Структура:

```
src/
  components/
  modules/
    receiving/
    picking/
    shipping/
    inventory/
    returns/
    barcodes/
    labels/
    settings/
  ui/
    cards/
    lists/
    modals/
    search/
    forms/
  hooks/
  api/
  metrics/
  state/
  offline/
  partners/
  onboarding/
  styles/
```

---

# 🎨 **2. Реализуй новый UX, основанный на поведенческом дизайне**

Внедри все следующие улучшения:

### **2.1. Список документов**

* карточки вместо строк;
* группировка: Сегодня/Неделя/Старые;
* быстрые фильтры;
* умный поиск (`SmartSearch`).

### **2.2. Список товаров**

* карточки c прогрессбарами;
* свайпы для увеличения/уменьшения количества;
* подсказки по Фоггу.

### **2.3. Экран сканирования**

* большой CTA: «Сканируйте товар»;
* вибро;
* цветовые статусы;
* auto-progress;
* auto-navigation.

### **2.4. Контекстная навигация**

* хлебные крошки;
* автоматические переходы;
* сохранение состояния.

### **2.5. Улучшение действий**

* автоматическое добавление количества;
* подсветка ошибок;
* auto-save;
* full offline support.

Создай UI-компоненты:

* `<DocumentCard />`
* `<GroupedDocumentList />`
* `<ItemCard />`
* `<ScannerScreen />`
* `<Breadcrumbs />`
* `<AutoHint />`
* `<PartnerPicker />`
* `<SwipeableRow />`
* `<ProgressStats />`

---

# 👥 **3. Поддержка команд из двух операторов**

Создать модуль:

```
partners/
  PartnerContext.ts
  PartnerPickerModal.tsx
  partnerStore.ts
```

Функции:

* выбор напарника;
* отображение выбранного напарника;
* сохранение в IndexedDB;
* участие пары в метриках.

---

# 📊 **4. Встроенный скрытый модуль метрик**

Создать `/src/metrics/`:

```
metrics/
  collector.ts
  metricsTypes.ts
  timers.ts
  reporters.ts
  session.ts
```

### Собирай следующие метрики:

#### **Рабочие показатели**

* время приёмки документа
* время подбора заказа
* время инвентаризации ячейки
* время отгрузки
* количество SKU
* количество корректировок
* кол-во неверных сканов

#### **Ошибки**

* % отклонений
* % ошибок подбора
* кол-во конфликтов количества
* кол-во отмен
* время зависаний

#### **UX Метрики**

* время поиска документа
* кол-во кликов к цели
* кол-во возвратов назад
* кол-во всплывающих ошибок

#### **Командные метрики**

* вклад оператора А
* вклад оператора B
* распределение времени
* средняя скорость пары

### Метрики сохраняются:

* в локальный буфер IndexedDB;
* периодически отправляются на `/api/metrics/report`.

---

# 🧩 **5. Реализуй онбординг (3 набора, случайный выбор)**

```
onboarding/
  set1/
  set2/
  set3/
  selector.ts
```

Каждый набор — 3–4 экрана:

* «Как быстро находить документы»
* «Как правильно сканировать»
* «Как минимизировать ошибки»
* «Как ускорить работу пары»

---

# 🔌 **6. Отдели API конфигурацией**

Создать:
`/src/config/server.ts`

Параметры:

* API_URL
* TIMEOUT
* RETRY_POLICY
* OFFLINE_QUEUE
* ENABLE_METRICS

Чтоб можно было менять не перекомпилируя приложение.

---

# 🚦 **7. Реализуй маршрутизацию**

В `/src/routes.tsx`:

```
/
  /onboarding
  /documents
  /documents/:id
  /scanner
  /partners
  /settings
```

Плюс редиректы:

* если нет партнера → /partners
* если нет документов → /documents

---

# ⚙️ **8. Внедри все операции по документации «Склад-15»**

### Реализовать полный функционал модулей:

* Приёмка
* Размещение
* Подбор
* Отгрузка
* Инвентаризация
* Возврат/списание
* Сбор штрихкодов
* Печать этикеток
* Настройки (расширенные)

Каждый модуль должен:

* иметь экран списка;
* иметь экран документа;
* иметь экран сканирования;
* иметь авто-подсказки;
* собирать метрики;
* работать оффлайн;
* быть связным в навигации.

---

# 🧠 **9. Реализуй поведенческий дизайн (Вендел + Фогг)**

### Повысить способность:

* уменьшить трение;
* заменить таблицы карточками;
* скрывать вторичные действия.

### Повысить мотивацию:

* зелёная линия прогресса;
* micro-rewards («Отлично! Ещё 2 позиции»)
* стабильная обратная связь.

### Триггеры:

* подсказки в нужный момент;
* автоматический переход;
* подсказки при ошибках.

Вся логика — в `/src/behavior/`.

---

# 🧪 **10. Добавь встроенные UX-гипотезы для A/B-тестов**

Создать:

```
experiments/
  flags.ts
  variants/
```

Гипотезы:

* карточки vs таблицы;
* быстрые фильтры vs обычный поиск;
* свайпы vs кнопки;
* авто-переход vs ручной;
* подсказки vs отсутствие подсказок.

---

# 🚀 **11. НИЧЕГО НЕ СЛОМАТЬ**

При создании всех файлов:

* проверяй маршруты;
* избегай дубликатов;
* соблюдай архитектуру;
* разделяй бизнес-логику, UI и сеть;
* избегай крупных монолитных файлов.

---

# 🏁 **12. Производственная сборка**

После завершения:

* собрать PWA manifest
* настроить сервис-воркер
* оптимизировать изображения
* включить code splitting
* подготовить к деплою

---

# ✨ ФИНАЛЬНО

> «Claude, начни пошагово собирать проект.
> Выводи файлы блоками, с правильными путями, до полной рабочей версии.
>
> Каждый шаг — создавай, изменяй или улучшай файлы так, как это сделал бы старший архитектор.
>
> Продолжай, пока весь проект не будет готов.»

