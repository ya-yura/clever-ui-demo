
# 🚀 MASTER PROMPT / CURSOR

**Собрать производственную мобильную PWA «Склад-15» (React + TS + Vite) с новым UX, метриками и системой команд.**

---

## Общее
- Собирай проект как старший фронтенд-архитектор. Все файлы/каталоги создавай сам, код — пакетами, корректные пути, поддерживаемость и расширяемость обязательны.

## Архитектура
- Стек: React, TypeScript, Vite, TailwindCSS, Framer Motion, Zustand/Jotai, React Router, Workbox (PWA), IndexedDB (Dexie), API-абстракция `/src/api/`.
- Модули операций: приёмка, размещение, подбор, отгрузка, инвентаризация, возврат/списание, сбор ШК, печать этикеток, настройки.
- Слои: поиск (универсальный), метрики, выбор напарника, онбординг.
- Структура `src/`: components, modules/(receiving|picking|shipping|inventory|returns|barcodes|labels|settings), ui/(cards|lists|modals|search|forms), hooks, api, metrics, state, offline, partners, onboarding, styles.

## UX (поведенческий дизайн)
- Списки документов: карточки, группировка Сегодня/Неделя/Старые, быстрые фильтры, SmartSearch.
- Списки товаров: карточки с прогрессом, свайпы +/-, подсказки по Фоггу.
- Сканирование: крупный CTA «Сканируйте товар», вибро, цветовые статусы, auto-progress, auto-navigation.
- Контекстная навигация: хлебные крошки, автопереходы, сохранение состояния.
- Улучшение действий: авто-добавление количества, подсветка ошибок, auto-save, full offline.
- UI-компоненты: DocumentCard, GroupedDocumentList, ItemCard, ScannerScreen, Breadcrumbs, AutoHint, PartnerPicker, SwipeableRow, ProgressStats.

## Команды (пары)
- Модуль `partners/` (PartnerContext, PartnerPickerModal, partnerStore), выбор/отображение напарника, сохранение в IndexedDB, метрики пары.

## Метрики
- `/src/metrics/` (collector, metricsTypes, timers, reporters, session).
- Рабочие: время приёмки/подбора/инвентаризации/отгрузки, SKU, корректировки, неверные сканы.
- Ошибки: % отклонений, % ошибок подбора, конфликты количества, отмены, зависания.
- UX: время поиска, клики к цели, возвраты назад, всплывающие ошибки.
- Командные: вклад A/B, распределение времени, скорость пары.
- Хранение: локально (IndexedDB), периодическая отправка `/api/metrics/report`.

## Онбординг
- `onboarding/set1|set2|set3`, selector; каждый набор 3–4 экрана: поиск документов, правильное сканирование, минимизация ошибок, ускорение работы пары.

## Конфиг
- `/src/config/server.ts`: API_URL, TIMEOUT, RETRY_POLICY, OFFLINE_QUEUE, ENABLE_METRICS. Менять без перекомпиляции.

## Маршруты
- `/`, `/onboarding`, `/documents`, `/documents/:id`, `/scanner`, `/partners`, `/settings`; редиректы: без партнёра → /partners; без документов → /documents.

## Операции (все модули)
- Экран списка, экран документа, экран сканирования, авто-подсказки, сбор метрик, оффлайн, связная навигация.

## Поведенческий дизайн (Вендел + Фогг)
- Способность: меньше трения, таблицы → карточки, вторичные действия скрыты.
- Мотивация: зелёная линия прогресса, micro-rewards, стабильная обратная связь.
- Триггеры: подсказки вовремя, автопереходы, подсказки при ошибках. Логика в `/src/behavior/`.

## A/B гипотезы
- `experiments/flags.ts`, `experiments/variants/`: карточки vs таблицы; быстрые фильтры vs обычный поиск; свайпы vs кнопки; авто-переход vs ручной; подсказки vs нет.

## Ничего не сломать
- Проверяй маршруты, избегай дублей, соблюдай архитектуру, разделяй бизнес-логику/сеть/UI, не делай монолиты.

## Прод-сборка
- PWA manifest, сервис-воркер, оптимизация изображений, code splitting, готовность к деплою.

## UX для ТСД (обязательно для всех экранов)
- Приоритет: 🥇 Next Action (что делать сейчас, самый заметный/CTA) → 🥈 Current Context (что происходит: ячейка/док/режим) → 🥉 Progress/History (что сделано, компактно). Никогда не показывай «что сделано», если не показано «что делать».
- Компактность: минимальные отступы, мелкий но читаемый текст, уменьшенные иконки/кнопки, плотное инфонаполнение. Максимум 40–50% площади под Next Action, 30–35% под контекст, 15–25% под прогресс.
- Однородность: единые паттерны стилей/логики во всех операциях, без скачков UI/UX между модулями.
- Маленькие экраны: экономия вертикали, короткие заголовки, лаконичные тексты, компактные карточки, сгруппированные действия.

### По операциям (Next / Current / Progress)
- Приёмка: «Сканируйте товар/ячейку размещения» / Документ + текущий товар / «Принято: X/Y».
- Размещение: «Сканируйте ячейку» / Товар + «Осталось: N» / «Размещено: X».
- Подбор: «Идите в ячейку …, сканируйте товар» / Заказ + Товар / «Подобрано: X/Y».
- Отгрузка: «Сканируйте короб» / Отгрузка + мест: N / «Готово: X/Y».
- Возврат/списание: «Выберите причину» / Товар + тип операции / «Списано/Возвращено: X».
- Инвентаризация: «Сканируйте ячейку/товар» / Текущая ячейка / «Ячеек: X/Y».
- Сбор ШК: «Сканируйте штрихкод» / Товар / «Добавлено: X».

### Акценты
- Next Action всегда самый заметный (цвет/размер/кнопка), вторичные спокойнее.
- Контекст короткий, без истории. Прогресс — минимальный, без подробных списков.
- Минимум пустоты, группируй действия, карточки компактные, шрифты/иконки уменьшены, но читаемы.

## Финально
- «Начни пошагово собирать проект. Выводи файлы блоками, с правильными путями, до полной рабочей версии. Каждый шаг — как старший архитектор. Продолжай, пока проект не готов.»

