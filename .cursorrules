Делаю PWA прототип мобильного клиента Cleverence - склад-15.

baseurl для api-сервера: http://localhost:9000/MobileSMARTS/api/v1/

meta-данные api читай из `$metadata.xml`

на главном экране плитки основных операций get /api/v1/DocTypes

по нажатию на плитку открывается экран со списком документов выбранного типа get /api/v1/Docs/{DocType.uni}/






# 📖 Справочник функций системы "Склад-15"

**Актуальность:** 31 октября 2025  
**Версия:** 1.0.0  
**Статус:** ✅ Полный и актуальный

---

## 📚 Содержание

- [1. Основные модули склада](#1-основные-модули-склада)
- [2. Гамбургер-меню](#2-гамбургер-меню)
- [3. Документы](#3-документы)
- [4. Партнеры и напарник](#4-партнеры-и-напарник)
- [5. Сканирование](#5-сканирование)
- [6. Оффлайн режим и синхронизация](#6-оффлайн-режим-и-синхронизация)
- [7. Настройки](#7-настройки)
- [8. Статистика и KPI](#8-статистика-и-kpi)
- [9. Диагностика](#9-диагностика)
- [10. Обратная связь](#10-обратная-связь)
- [11. О программе](#11-о-программе)
- [12. Справочники](#12-справочники)
- [13. Технические возможности](#13-технические-возможности)

---

## 1. Основные модули склада

### 1.1 📦 Приёмка (Receiving)

**Путь:** `/receiving` или `/receiving/:id`  
**Компонент:** `src/pages/Receiving.tsx`  
**База данных:** `receivingDocuments`, `receivingLines`

#### Функции:
- ✅ Приём товаров от поставщиков
- ✅ Контроль плана/факта по количеству
- ✅ Сканирование штрихкодов товаров
- ✅ Проверка соответствия накладной
- ✅ Автосохранение каждые 30 секунд
- ✅ Работа в оффлайн режиме

#### Данные документа:
- Номер поставки (`deliveryNumber`)
- Поставщик (`supplier`)
- Дата создания/обновления
- Общее количество строк
- Выполненные строки
- Примечания

#### Данные строки:
- Товар (ID, название, штрихкод)
- Количество план/факт
- Статус (pending, in_progress, completed)
- Время сканирования

#### Процесс работы:
1. Открыть документ приёмки
2. Сканировать товары
3. Система автоматически находит строку по штрихкоду
4. Увеличивает факт на +1
5. Показывает прогресс выполнения
6. Звуковой/виброотклик после сканирования

---

### 1.2 🏷️ Размещение (Placement)

**Путь:** `/placement` или `/placement/:id`  
**Компонент:** `src/pages/Placement.tsx`  
**База данных:** `placementDocuments`, `placementLines`

#### Функции:
- ✅ Размещение товаров по ячейкам хранения
- ✅ Сканирование ячейки и товара
- ✅ Контроль правильности размещения
- ✅ Привязка к документу-источнику (приёмка)
- ✅ Контроль заполнения ячеек

#### Данные документа:
- Связь с документом-источником (`sourceDocumentId`)
- Дата создания/обновления
- Статус выполнения
- Пользователь

#### Данные строки:
- Товар (ID, название, штрихкод)
- Ячейка назначения (`cellId`)
- Количество план/факт
- Статус размещения

#### Процесс работы:
1. Открыть документ размещения
2. Сканировать ячейку назначения
3. Сканировать товар
4. Система проверяет соответствие
5. Фиксирует размещение
6. Обратная связь (звук/вибро)

---

### 1.3 🚚 Подбор (Picking)

**Путь:** `/picking` или `/picking/:id`  
**Компонент:** `src/pages/Picking.tsx`  
**База данных:** `pickingDocuments`, `pickingLines`

#### Функции:
- ✅ Комплектация заказов для отгрузки
- ✅ Оптимизированный маршрут по ячейкам
- ✅ Пошаговые инструкции по маршруту
- ✅ Визуализация прогресса маршрута
- ✅ Контроль количества подбора

#### Данные документа:
- Номер заказа (`orderNumber`)
- ID заказа (`orderId`)
- Клиент (`customer`)
- Маршрут (массив ячеек)
- Статус выполнения

#### Данные строки:
- Товар (ID, название, штрихкод)
- Ячейка подбора (`cellId`)
- Количество план/факт
- Позиция в маршруте

#### Процесс работы:
1. Открыть документ подбора
2. Следовать маршруту (ячейка → товар)
3. Сканировать указанную ячейку
4. Сканировать товар
5. Контроль количества
6. Переход к следующей ячейке

#### Компонент прогресса маршрута:
```typescript
<RouteProgress 
  currentStep={currentStep}
  totalSteps={route.length}
  currentCell={route[currentStep]}
/>
```

---

### 1.4 📄 Отгрузка (Shipment)

**Путь:** `/shipment` или `/shipment/:id`  
**Компонент:** `src/pages/Shipment.tsx`  
**База данных:** `shipmentDocuments`, `shipmentLines`

#### Функции:
- ✅ Контроль отгрузки товаров клиентам
- ✅ Ввод ТТН (товарно-транспортная накладная)
- ✅ Указание перевозчика
- ✅ Финальная проверка комплектности
- ✅ Печать документов отгрузки

#### Данные документа:
- Номер заказа (`orderNumber`)
- Клиент (`customer`)
- Перевозчик (`carrier`)
- ТТН номер (`ttnNumber`)
- Дата отгрузки

#### Данные строки:
- Товар (ID, название)
- Количество отгружено
- Статус

#### Процесс работы:
1. Открыть документ отгрузки
2. Проверить комплектность
3. Ввести данные ТТН
4. Указать перевозчика
5. Подтвердить отгрузку
6. Печать документов (если настроено)

---

### 1.5 ♻️ Возврат/Списание (Return)

**Путь:** `/return` или `/return/:id`  
**Компонент:** `src/pages/Return.tsx`  
**База данных:** `returnDocuments`, `returnLines`

#### Функции:
- ✅ Оформление возвратов от клиентов
- ✅ Списание товаров (брак, утеря)
- ✅ Указание причины
- ✅ Фотофиксация (опционально)
- ✅ Автоматический расчет сумм

#### Типы операций:
- `return` — Возврат от клиента
- `writeoff` — Списание

#### Данные документа:
- Тип операции (`type`)
- Причина (`reason`)
- Дата создания
- Пользователь

#### Данные строки:
- Товар (ID, название)
- Количество
- Причина (брак, утеря, возврат)
- Фото (опционально)

#### Причины списания:
- Брак
- Утеря
- Просрочка
- Повреждение
- Другое

---

### 1.6 📊 Инвентаризация (Inventory)

**Путь:** `/inventory` или `/inventory/:id`  
**Компонент:** `src/pages/Inventory.tsx`  
**База данных:** `inventoryDocuments`, `inventoryLines`

#### Функции:
- ✅ Проведение инвентаризации склада
- ✅ Контроль расхождений план/факт
- ✅ Сканирование по ячейкам или сплошное
- ✅ Автоматический расчет отклонений
- ✅ Отчеты по расхождениям

#### Типы инвентаризации:
- `full` — Полная (весь склад)
- `partial` — Частичная (выборочные ячейки)
- `cell` — По конкретной ячейке

#### Данные документа:
- Тип инвентаризации (`type`)
- Зона/ячейки (`zones`, `cells`)
- Дата начала/завершения
- Пользователь

#### Данные строки:
- Товар (ID, название)
- Ячейка (`cellId`)
- Количество по учету (план)
- Количество фактическое
- Расхождение (±)

#### Процесс работы:
1. Открыть документ инвентаризации
2. Выбрать тип (полная/частичная)
3. Сканировать ячейку
4. Сканировать товары в ячейке
5. Фиксировать фактическое количество
6. Система автоматически выявляет расхождения
7. Формирование отчета

---

## 2. Гамбургер-меню

**Расположение:** Левый верхний угол (иконка ☰)  
**Компонент:** `src/modules/menu/HamburgerMenu.tsx`  
**Контекст:** `MenuProvider` (глобальный)

### 2.1 Основные функции

#### 🔄 Синхронизировать
- **Действие:** Принудительная синхронизация с сервером
- **Требуется:** Онлайн
- **Hook:** `useSync()`
- **Что синхронизируется:**
  - Все несинхронизированные документы
  - Очередь действий из `syncActions`
  - Изменения в документах

#### 🧩 Обновить справочники
- **Действие:** Загрузка актуальных справочников с сервера
- **Требуется:** Онлайн
- **Hook:** `useReferences()`
- **Что обновляется:**
  - Справочник товаров (`products`)
  - Справочник ячеек (`cells`)
  - Справочник сотрудников (`employees`)

### 2.2 Навигация

#### ⚙️ Настройки
- **Путь:** `/settings`
- **Подразделы:**
  - Подключение (сервер, порт, SSL)
  - Пользователь (имя, роль, отдел)
  - Поведение (звук, вибрация, голос)
  - Синхронизация (интервал, Wi-Fi only)

#### 👥 Напарник
- **Путь:** `/partner`
- **Функция:** Выбор напарника для совместной работы

#### 🧾 Мои документы
- **Путь:** `/documents`
- **Функция:** Просмотр всех документов пользователя

#### 📊 Статистика и KPI
- **Путь:** `/statistics`
- **Функция:** Отображение ключевых показателей

#### 🧠 Диагностика
- **Путь:** `/diagnostics`
- **Функция:** Проверка состояния системы

#### 💬 Обратная связь
- **Действие:** Отправка отзыва/сообщения об ошибке

#### ℹ️ О программе
- **Действие:** Информация о версии и контакты

#### 🚪 Выйти
- **Действие:** Выход из системы

### 2.3 Возможности меню

- ✅ Открытие свайпом слева направо
- ✅ Открытие кликом на ☰
- ✅ Закрытие свайпом влево
- ✅ Закрытие кликом вне меню
- ✅ Закрытие по ESC
- ✅ Анимация выезда (200ms)
- ✅ Индикация онлайн/оффлайн
- ✅ Виброотклик при взаимодействии
- ✅ Автоматическое скрытие после выбора
- ✅ Раскрывающиеся подменю
- ✅ Отключение пунктов в оффлайн режиме

---

## 3. Документы

**Путь:** `/documents`  
**Компонент:** `src/pages/Documents.tsx`  
**Сервис:** `src/services/documentService.ts`

### 3.1 Функции модуля документов

#### Просмотр всех документов
- ✅ Единый список документов из всех модулей
- ✅ Фильтрация по типу, статусу, дате
- ✅ Поиск по номеру, партнеру, примечаниям
- ✅ Сортировка по различным полям
- ✅ Группировка по модулям

#### Фильтры

**По типу документа:**
- Приёмка (`receiving`)
- Размещение (`placement`)
- Подбор (`picking`)
- Отгрузка (`shipment`)
- Возврат (`return`)
- Инвентаризация (`inventory`)

**По статусу:**
- Новый (`new`)
- В работе (`in_progress`)
- Выполнен (`completed`)
- Отменен (`cancelled`)

**По приоритету:**
- Высокий (`high`)
- Средний (`normal`)
- Низкий (`low`)

**По дате:**
- Дата с... (`dateFrom`)
- Дата по... (`dateTo`)

**По партнеру:**
- Выбор из списка

**По пользователю:**
- Мои документы
- Все документы

**По тегам:**
- Срочно
- Важно
- Проверить

#### Сортировка

**Поля сортировки:**
- Дата создания
- Дата обновления
- Номер документа
- Партнер
- Статус
- Прогресс выполнения

**Направление:**
- По возрастанию (↑)
- По убыванию (↓)

### 3.2 Карточка документа

**Отображаемая информация:**
- Тип и иконка модуля
- Номер документа
- Статус с цветовым индикатором
- Партнер (поставщик/клиент)
- Дата создания/обновления
- Прогресс выполнения (%)
- Количество строк (план/факт)
- Примечания
- Статус синхронизации

**Действия:**
- Открыть документ
- Быстрый просмотр
- Экспорт

### 3.3 API документов

```typescript
// Получить все документы
const docs = await documentService.getAllDocuments();

// Фильтровать
const filtered = documentService.filterDocuments(docs, {
  types: ['receiving', 'placement'],
  statuses: ['in_progress'],
  dateFrom: Date.now() - 7 * 24 * 60 * 60 * 1000,
  searchQuery: 'ООО Поставщик'
});

// Сортировать
const sorted = documentService.sortDocuments(filtered, {
  field: 'createdAt',
  direction: 'desc'
});

// Получить URL документа
const url = documentService.getDocumentUrl(doc); // /receiving/RCV-001

// Рассчитать процент выполнения
const progress = documentService.getCompletionPercentage(doc); // 75
```

---

## 4. Партнеры и напарник

**Путь:** `/partner`  
**Компонент:** `src/pages/PartnerManagement.tsx`  
**База данных:** `employees`, `partnerSessions`

### 4.1 Функции модуля партнеров

#### Выбор напарника
- ✅ Просмотр списка доступных сотрудников
- ✅ Фильтрация по отделу/роли
- ✅ Статус (онлайн/оффлайн/занят)
- ✅ Выбор напарника для совместной работы

#### Работа с напарником
- ✅ Создание сессии совместной работы
- ✅ Разделение задач
- ✅ Отслеживание прогресса напарника
- ✅ Завершение сессии

#### Данные сотрудника
```typescript
interface Employee {
  id: string;
  name: string;
  role: string;
  department: string;
  isActive: boolean;
  lastActiveAt: number;
}
```

#### Данные сессии
```typescript
interface PartnerSession {
  id: string;
  userId: string;
  partnerId: string;
  startedAt: number;
  endedAt?: number;
  status: 'active' | 'paused' | 'completed';
}
```

---

## 5. Сканирование

**Hook:** `src/hooks/useScanner.ts`  
**Компонент:** `src/components/ScannerInput.tsx`

### 5.1 Режимы сканирования

#### 🎹 Клавиатурный режим (по умолчанию)
- Использование: Проводные и Bluetooth-сканеры
- Принцип: Сканер работает как клавиатура
- Обнаружение: Автоматическое (по скорости ввода)
- Без настройки: Plug & Play

**Как работает:**
1. Сканер передает штрихкод как набор символов
2. Система определяет быстрый ввод
3. Автоматически обрабатывает как сканирование
4. Очищает поле после обработки

#### 📷 Режим камеры
- Использование: Встроенная или внешняя камера
- Библиотека: `html5-qrcode`
- Форматы: QR, EAN-13, UPC-A, Code128, и др.

**Настройки камеры:**
```typescript
{
  facingMode: 'environment', // Задняя камера
  fps: 10,                   // Кадров в секунду
  qrbox: { width: 250, height: 250 } // Область сканирования
}
```

#### ✍️ Ручной ввод
- Использование: Ввод штрихкода вручную
- Применение: Поврежденные/нечитаемые коды
- Валидация: Проверка формата

### 5.2 API сканирования

```typescript
const {
  isScanning,
  lastScan,
  handleScan,           // Обработка сканирования
  startCameraScanner,   // Запуск камеры
  stopScanner,          // Остановка
  manualScan,           // Ручной ввод
} = useScanner({
  mode: 'keyboard',
  continuous: true,
  onScan: (code) => console.log('Scanned:', code),
  onError: (error) => console.error(error)
});
```

### 5.3 Обратная связь при сканировании

- ✅ **Звук:** Beep при успешном сканировании
- ✅ **Вибрация:** 50ms при сканировании
- ✅ **Визуально:** Подсветка поля/анимация
- ✅ **Голос:** Название товара (опционально)

---

## 6. Оффлайн режим и синхронизация

**Hook:** `src/hooks/useOfflineStorage.ts` + `src/hooks/useSync.ts`  
**База данных:** `IndexedDB` через `Dexie.js`

### 6.1 Оффлайн режим

#### Возможности оффлайн
- ✅ Просмотр всех документов
- ✅ Работа с документами (сканирование, изменения)
- ✅ Создание новых документов
- ✅ Автоматическое сохранение в IndexedDB
- ✅ Очередь действий для синхронизации
- ✅ Индикация статуса подключения

#### Мониторинг статуса
```typescript
const { isOnline } = useOfflineStorage('module');

// Автоматическое отслеживание
navigator.onLine; // true/false
```

#### Индикатор в интерфейсе
- 🟢 **Онлайн** — зеленый индикатор
- 🔴 **Оффлайн** — красный индикатор
- Отображается в Header и меню

### 6.2 Синхронизация

#### Типы синхронизации

**Автоматическая:**
- При восстановлении подключения
- По расписанию (настраиваемый интервал)
- При выходе из документа

**Принудительная:**
- Через меню "Синхронизировать"
- Кнопка в модуле
- API вызов

#### Очередь действий

**Структура:**
```typescript
interface SyncAction {
  id?: number;
  module: string;
  action: string;      // 'create', 'update', 'delete'
  data: any;          // Данные операции
  timestamp: number;
  synced: boolean;
  error?: string;
}
```

**Процесс:**
1. Действие сохраняется в `syncActions`
2. При онлайн — автоматическая отправка
3. Пакетная обработка очереди
4. Маркировка синхронизированных
5. Повтор при ошибке

#### API синхронизации

```typescript
const { 
  isSyncing,          // Процесс синхронизации
  isOnline,           // Статус подключения
  lastSyncTime,       // Время последней синхр.
  syncError,          // Ошибка синхронизации
  pendingCount,       // Количество в очереди
  sync                // Запуск синхронизации
} = useSync({
  module: 'receiving',
  syncEndpoint: '/api/receiving/sync',
  onSyncComplete: () => console.log('✅ Synced'),
  onSyncError: (error) => console.error('❌ Error:', error)
});
```

#### Индикация синхронизации
- 🔄 Иконка вращается при синхронизации
- ⏸ Пауза если оффлайн
- ✅ Галочка при успехе
- ❌ Крестик при ошибке
- Счетчик несинхронизированных действий

### 6.3 IndexedDB структура

**Таблицы:**
- `receivingDocuments`, `receivingLines`
- `placementDocuments`, `placementLines`
- `pickingDocuments`, `pickingLines`
- `shipmentDocuments`, `shipmentLines`
- `returnDocuments`, `returnLines`
- `inventoryDocuments`, `inventoryLines`
- `syncActions` — очередь синхронизации
- `errorLogs` — логи ошибок
- `employees` — сотрудники
- `partnerSessions` — сессии напарника
- `products` — справочник товаров
- `cells` — справочник ячеек

---

## 7. Настройки

**Путь:** `/settings`  
**Компонент:** `src/pages/Settings.tsx`

### 7.1 Подключение

#### Параметры сервера
- **Адрес сервера:** URL API (`https://api.example.com`)
- **Порт:** Порт подключения (443)
- **Таймаут:** Время ожидания ответа (30 сек)
- **Использовать SSL:** Шифрование соединения (Вкл/Выкл)

### 7.2 Пользователь

#### Данные пользователя
- **Имя пользователя:** Отображаемое имя
- **Роль:** admin, manager, worker
- **Отдел:** Склад, Приемка, Отгрузка

### 7.3 Поведение

#### Обратная связь
- **Звуковые уведомления:** Вкл/Выкл
- **Вибрация:** Вкл/Выкл
- **Голосовые подсказки:** Вкл/Выкл
- **Автосканирование:** Вкл/Выкл

### 7.4 Синхронизация

#### Параметры синхронизации
- **Автосинхронизация:** Вкл/Выкл
- **Интервал:** Частота синхронизации (60 сек)
- **Только по Wi-Fi:** Экономия трафика (Вкл/Выкл)

### 7.5 Сохранение настроек

- Кнопка "Сохранить настройки"
- Подтверждение сохранения (✓ Сохранено!)
- Хранение в `localStorage`
- Синхронизация с сервером (опционально)

---

## 8. Статистика и KPI

**Путь:** `/statistics`  
**Компонент:** `src/pages/Statistics.tsx`

### 8.1 Ключевые показатели

#### KPI карточки

**📦 Приемка документов**
- Количество документов
- Тренд (↑ +12%)
- Цвет: Синий

**✅ Размещено**
- Количество документов
- Тренд
- Цвет: Зеленый

**📊 Подобрано**
- Количество документов
- Тренд
- Цвет: Желтый

**📈 Отгружено**
- Количество документов
- Тренд
- Цвет: Фиолетовый

**⏱ Среднее время**
- Время обработки (минуты)
- Тренд
- Цвет: Оранжевый

**⚠️ Ошибок**
- Количество ошибок
- Тренд
- Цвет: Красный

### 8.2 Источники данных

- Загрузка из IndexedDB
- Подсчет документов по модулям
- Расчет трендов (за период)
- Агрегация данных

### 8.3 Графики (планируется)

- График активности по часам
- График по модулям
- Сравнение периодов
- Библиотека: Chart.js (будет добавлена)

---

## 9. Диагностика

**Путь:** `/diagnostics`  
**Компонент:** `src/pages/Diagnostics.tsx`

### 9.1 Проверки системы

#### 🌐 Подключение к интернету
- **Статус:** Онлайн/Оффлайн
- **Проверка:** `navigator.onLine`
- **Индикатор:** 🟢 ОК / 🔴 Ошибка

#### 💾 База данных IndexedDB
- **Статус:** Работает/Ошибка
- **Проверка:** Открытие БД, подсчет записей
- **Информация:** Количество записей в очереди

#### 💿 Хранилище
- **Статус:** ОК/Предупреждение
- **Проверка:** `navigator.storage.estimate()`
- **Информация:** Использовано X% (Y MB)

#### 🔄 Service Worker
- **Статус:** Зарегистрирован/Не зарегистрирован
- **Проверка:** `navigator.serviceWorker.getRegistration()`

#### 📱 Устройство
- **Тип:** Мобильное/Десктоп
- **Информация:** User Agent

### 9.2 Системная информация

- **Браузер:** Chrome/Firefox/Safari
- **Язык:** ru-RU
- **Платформа:** Win32/MacOS/Linux
- **Онлайн:** Да/Нет

### 9.3 Запуск проверки

- Кнопка "Запустить проверку"
- Анимация процесса (⚙ вращение)
- Результаты с цветовыми индикаторами
- Детальное описание проблем

---

## 10. Обратная связь

**Утилиты:** `src/utils/` (sound, vibration, voice, feedback)

### 10.1 Звуковая обратная связь

**Файл:** `src/utils/sound.ts`

#### Типы звуков
- `scan` — Успешное сканирование (beep)
- `success` — Успешное действие (ding)
- `error` — Ошибка (buzz)
- `warning` — Предупреждение (alert)

#### API
```typescript
import { playSound } from '@/utils/sound';

playSound('scan');    // Beep
playSound('success'); // Ding
playSound('error');   // Buzz
```

**Управление:**
- Включение/выключение в настройках
- Регулировка громкости
- Разные звуки для разных событий

### 10.2 Виброотклик

**Файл:** `src/utils/vibration.ts`

#### Паттерны вибрации
```typescript
import { vibrate, vibratePattern } from '@/utils/vibration';

vibrate(50);                      // Короткая (50ms)
vibrate(200);                     // Длинная (200ms)
vibratePattern([100, 50, 100]);  // Паттерн
```

**Применение:**
- Сканирование: 50ms
- Успех: 100ms
- Ошибка: [100, 50, 100]
- Предупреждение: [200, 100, 200]

**Поддержка:**
- Проверка `'vibrate' in navigator`
- Автоматическое отключение если не поддерживается

### 10.3 Голосовые подсказки

**Файл:** `src/utils/voice.ts`

#### Функции
```typescript
import { speak } from '@/utils/voice';

speak('Товар отсканирован');
speak('Ошибка: товар не найден');
speak('Документ завершен');
```

**Web Speech API:**
- Использование: `window.speechSynthesis`
- Язык: Русский (ru-RU)
- Скорость: Настраиваемая

**Применение:**
- Название товара после сканирования
- Номер ячейки
- Статус выполнения
- Предупреждения

### 10.4 Комбинированная обратная связь

**Файл:** `src/utils/feedback.ts`

```typescript
import { feedback } from '@/utils/feedback';

// Все сразу: звук + вибро + голос
feedback.success('Товар добавлен');
feedback.error('Товар не найден');
feedback.warning('Проверьте количество');
feedback.scan(); // Спец. для сканирования
```

**Преимущества:**
- Единый интерфейс
- Автоматический выбор доступных методов
- Учет настроек пользователя
- Graceful degradation

---

## 11. О программе

**Путь:** `/about`  
**Компонент:** `src/pages/About.tsx`

### 11.1 Информация о приложении

#### Отображаемые данные

**Версия приложения:**
- Версия: 1.0.0
- Сборка: 2025.10.31
- Разработчик: Cleverence
- Тип: PWA

**Возможности:**
- 📦 Приёмка товаров
- 🏷️ Размещение по ячейкам
- 🚚 Комплектация заказов
- 📄 Отгрузка товаров
- ♻️ Возврат и списание
- 📊 Инвентаризация
- 🔄 Offline-First режим
- 📱 PWA установка

**Технологии:**
- React 18
- TypeScript
- Tailwind CSS
- Vite
- IndexedDB
- Dexie.js
- Framer Motion
- React Router

**Контакты:**
- Email: support@cleverence.com
- Сайт: cleverence.com
- GitHub: github.com/cleverence

### 11.2 Дополнительно

- Лицензия
- Авторские права
- Благодарности команде

---

## 12. Справочники

**Hook:** `src/hooks/useReferences.ts`

### 11.1 Справочник товаров

**Таблица:** `products`

#### Данные товара
```typescript
interface Product {
  id: string;
  name: string;
  sku: string;
  barcode: string;
  category?: string;
  unit?: string;
  weight?: number;
}
```

#### Обновление
```typescript
const { updateReferences } = useReferences();
await updateReferences(); // Загрузка с сервера
```

### 11.2 Справочник ячеек

**Таблица:** `cells`

#### Данные ячейки
```typescript
interface Cell {
  id: string;
  name: string;
  zone: string;
  type: string; // 'storage', 'picking', 'receiving'
  capacity?: number;
}
```

### 11.3 Справочник сотрудников

**Таблица:** `employees`

#### Данные сотрудника
```typescript
interface Employee {
  id: string;
  name: string;
  role: string;
  department: string;
  isActive: boolean;
  lastActiveAt: number;
}
```

---

## 13. Технические возможности

### 13.1 Hooks (React)

#### `useScanner`
**Назначение:** Работа со сканером штрихкодов  
**Файл:** `src/hooks/useScanner.ts`  
**Возможности:**
- 3 режима сканирования
- Обработка событий
- Автоматическое определение режима

#### `useOfflineStorage`
**Назначение:** Управление оффлайн хранилищем  
**Файл:** `src/hooks/useOfflineStorage.ts`  
**Возможности:**
- Добавление действий в очередь
- Мониторинг онлайн статуса
- Работа с IndexedDB

#### `useSync`
**Назначение:** Синхронизация данных  
**Файл:** `src/hooks/useSync.ts`  
**Возможности:**
- Автоматическая синхронизация
- Обработка очереди
- Периодическая синхронизация
- Обработка ошибок

#### `useReferences`
**Назначение:** Обновление справочников  
**Файл:** `src/hooks/useReferences.ts`  
**Возможности:**
- Загрузка справочников с сервера
- Обновление IndexedDB
- Отслеживание статуса

### 13.2 Services

#### `documentService`
**Файл:** `src/services/documentService.ts`  
**Функции:**
- `getAllDocuments()` — Все документы
- `filterDocuments()` — Фильтрация
- `sortDocuments()` — Сортировка
- `getDocumentUrl()` — URL документа
- `getCompletionPercentage()` — Процент выполнения

#### `api`
**Файл:** `src/services/api.ts`  
**Функции:**
- HTTP клиент (Axios)
- Автоматические токены
- Обработка ошибок
- Повторные попытки

#### `db`
**Файл:** `src/services/db.ts`  
**Функции:**
- IndexedDB через Dexie
- Схема базы данных
- Версионирование
- Индексы для быстрого поиска

### 13.3 Utils

#### Обратная связь
- `sound.ts` — Звуковые эффекты
- `vibration.ts` — Вибрация
- `voice.ts` — Голосовые подсказки
- `feedback.ts` — Комбинированная обратная связь

#### Утилиты
- `date.ts` — Работа с датами
- `export.ts` — Экспорт данных
- `loadInitialData.ts` — Загрузка тестовых данных

### 13.4 Компоненты

#### Общие
- `Layout` — Основной layout с header
- `Header` — Шапка с навигацией и меню
- `OfflineIndicator` — Индикатор оффлайн режима
- `ScannerInput` — Поле ввода для сканера

#### Модульные
- `DocumentCard` — Карточка документа
- `DocumentFilters` — Фильтры документов
- `ReceivingCard` — Карточка приёмки
- `PlacementCard` — Карточка размещения
- `PickingCard` — Карточка подбора
- `RouteProgress` — Прогресс маршрута
- `PartnerCard` — Карточка сотрудника
- `PartnerSelector` — Выбор напарника

---

## 📊 Сводная таблица функций

| Модуль | Функции | Оффлайн | Сканирование | Синхронизация |
|--------|---------|---------|--------------|---------------|
| Приёмка | ✅ Приём товаров, контроль план/факт | ✅ Да | ✅ Да | ✅ Да |
| Размещение | ✅ Размещение по ячейкам | ✅ Да | ✅ Да | ✅ Да |
| Подбор | ✅ Комплектация, маршрут | ✅ Да | ✅ Да | ✅ Да |
| Отгрузка | ✅ Контроль отгрузки, ТТН | ✅ Да | ✅ Да | ✅ Да |
| Возврат | ✅ Возврат, списание | ✅ Да | ✅ Да | ✅ Да |
| Инвентаризация | ✅ Учет, расхождения | ✅ Да | ✅ Да | ✅ Да |
| Документы | ✅ Просмотр, фильтры | ✅ Да | ❌ Нет | ✅ Да |
| Партнеры | ✅ Выбор напарника | ✅ Да | ❌ Нет | ✅ Да |
| Настройки | ✅ Конфигурация | ✅ Да | ❌ Нет | ✅ Да |
| Статистика | ✅ KPI, графики | ✅ Да | ❌ Нет | ❌ Нет |
| Диагностика | ✅ Проверка системы | ✅ Да | ❌ Нет | ❌ Нет |
| Обратная связь | ✅ Форма отзывов | ✅ Да | ❌ Нет | ✅ Да |
| О программе | ✅ Информация | ✅ Да | ❌ Нет | ❌ Нет |

---

## 🔄 История обновлений

| Дата | Версия | Изменения |
|------|--------|-----------|
| 31.10.2025 | 1.0.0 | ✅ Создание справочника. Все модули задокументированы |
| 31.10.2025 | 1.0.1 | ✅ Добавлены страницы About и Feedback. Система восстановлена |

---

## 📝 Примечания

1. **Этот документ поддерживается автоматически** при изменении системы
2. **Всегда актуален** — отражает текущее состояние
3. **Централизованный** — единый источник информации о функциях
4. **Структурированный** — удобная навигация по разделам

---

**Местоположение:** `/DOCS/SYSTEM_FUNCTIONS_REFERENCE.md`  
**Статус:** ✅ Актуальный  
**Последнее обновление:** 31 октября 2025, 11:35








# 🤖 Заметка для AI









## ⚠️ ВАЖНО: Расположение документации

**Все документационные файлы (.md) должны создаваться и храниться в папке `/DOCS`**

### Правила:
1. ✅ Всегда создавайте `.md` файлы в `/DOCS`
2. ✅ Используйте подпапки для организации (например, `/DOCS/modules/`)
3. ✅ Обновляйте `/DOCS/INDEX.md` при добавлении новых документов
4. ❌ НЕ создавайте `.md` файлы в корне проекта (кроме `README.md`)

### Исключения:
- `README.md` — остается в корне (это стандарт)
- `package.json`, `tsconfig.json` и прочие конфиги — остаются где надо

### Структура:
```
DOCS/
├── INDEX.md                    ← главный индекс
├── .AI_NOTE.md                 ← эта заметка
├── modules/                    ← документация модулей
│   └── menu/
│       └── README.md
└── [все остальные .md файлы]
```

### При создании нового документа:
1. Создайте файл в `/DOCS/`
2. Добавьте ссылку в `/DOCS/INDEX.md`
3. При необходимости обновите `/README.md`

---

**Дата создания:** 31 октября 2025  
**Причина:** Организация документации по просьбе пользователя





