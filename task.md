### Модуль: **📦 Приёмка**

---

### 🧭 1. Назначение модуля

Модуль предназначен для выполнения операции **приёмки товаров** с помощью ТСД или мобильного устройства (через PWA-интерфейс).
Он обеспечивает:

* сканирование штрих-кодов поставок и товаров;
* сверку с планом поставки;
* визуальное и звуковое подтверждение корректности;
* фиксацию расхождений;
* автосохранение и оффлайн-режим;
* последующее завершение и синхронизацию документа с сервером.

---

### 📋 2. Основные сценарии

| Этап | Действие       | Описание                                                                                             |
| ---- | -------------- | ---------------------------------------------------------------------------------------------------- |
| 1    | Старт          | Пользователь выбирает «📦 Приёмка» из главного меню                                                  |
| 2    | Скан документа | Сканируется штрих-код приходного документа; система загружает данные                                 |
| 3    | Скан товаров   | Пользователь сканирует товары, приложение сверяет с планом                                           |
| 4    | Контроль       | Если количество совпадает — строка зелёная, если нет — красная, предлагается корректировка           |
| 5    | Автосохранение | Все изменения сохраняются каждые 30 сек в IndexedDB                                                  |
| 6    | Оффлайн-режим  | При потере сети данные записываются в локальное хранилище, затем отправляются при восстановлении     |
| 7    | Завершение     | После проверки всех товаров — кнопка «Завершить приёмку» или авто-предложение перейти к «Размещению» |

---

### 🧱 3. Требования к интерфейсу

* Крупные карточки товара с полями:

  * Наименование;
  * Артикул / ШК;
  * План / Факт / Остаток;
  * Индикация статуса (🟢/🔴/⚪);
* Кнопки действий:

  * «+1», «−1», «Редактировать»;
  * «Завершить документ»;
* Цветовая и звуковая обратная связь:

  * 🟢 Звук успеха при корректном скане;
  * 🔴 Ошибка при несоответствии;
* Нижняя панель подсказок:

  > «Сканируйте товар или документ»
  > «Ошибка соединения — данные сохраняются локально»

---

### 🗂 4. Архитектура и структура проекта

**Тип:** Installable PWA
**Frontend:** React + TypeScript + TailwindCSS
**Storage:** IndexedDB (через Dexie.js)
**API:** Axios / Fetch — параметры в `/config/server.json`
**Offline-first стратегия:** Service Worker + Background Sync

#### 📁 Структура файлов

```
/src
 ┣ /components
 ┃ ┣ ProductCard.tsx
 ┃ ┣ ScanHint.tsx
 ┃ ┣ ErrorToast.tsx
 ┃ ┗ HeaderBar.tsx
 ┣ /pages
 ┃ ┗ Receiving.tsx
 ┣ /hooks
 ┃ ┣ useScanner.ts
 ┃ ┣ useOfflineStorage.ts
 ┃ ┗ useSync.ts
 ┣ /services
 ┃ ┗ api.ts
 ┣ /config
 ┃ ┗ server.json
 ┣ /types
 ┃ ┗ product.ts
 ┣ /utils
 ┃ ┗ sound.ts
 ┣ App.tsx
 ┣ index.tsx
 ┗ sw.js
```

---

### ⚙️ 5. Логика модулей

#### 🧩 useScanner

* Подключается к камере или Bluetooth-сканеру.
* При сканировании определяет тип (документ / товар).
* Автоматически вызывает нужное действие.

#### 🧩 useOfflineStorage

* Создаёт таблицы `documents`, `items`, `errors` в IndexedDB.
* При потере связи пишет туда все транзакции.
* При восстановлении выполняет синхронизацию.

#### 🧩 useSync

* Проверяет очередь изменений.
* Отправляет обновления на сервер через `/api/sync`.
* Обрабатывает ответы и обновляет статусы строк.

#### 🧩 ProductCard

* Отображает товар.
* Позволяет изменить количество.
* Цветовая подсветка состояния.

---

### 📡 6. Настройки подключения

Файл `/config/server.json`:

```json
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token"
}
```

---

### 🧪 7. Тестовые сценарии

| Тест | Проверка                                        |
| ---- | ----------------------------------------------- |
| 1    | Сканирование документа — загрузка позиций       |
| 2    | Скан корректного товара — авто-добавление       |
| 3    | Скан чужого товара — ошибка и красная подсветка |
| 4    | Потеря связи — данные остаются локально         |
| 5    | Возврат связи — авто-синхронизация              |
| 6    | Завершение документа — переход в «Размещение»   |

---

## 💬 Готовый запрос к Claude 4.5 Sonnet (для Cursor IDE)

Ты можешь просто скопировать это и вставить в Claude:

---

**ПРОМТ ДЛЯ CLAUDE 4.5 SONNET**

```
Ты — старший инженер-разработчик интерфейсного PWA-приложения для складской системы Cleverence «Склад-15».  
Нужно создать рабочий модуль "Приёмка" по приведённому ТЗ.

🎯 Цель:
Создать полностью функциональный React + TypeScript PWA-модуль "Приёмка" с оффлайн-режимом, сканированием и автосинхронизацией.

📦 Реализуй всё автоматически в Cursor IDE, создай и разложи все файлы в нужные директории (указаны ниже).  
Все блоки кода и файлы выводи по отдельности с заголовками:  
`// === 📁 path/to/file ===`  
и дальше — содержимое файла.  
Файлы создавай по структуре проекта.

🔧 Используй стек:
React + TypeScript + TailwindCSS + Dexie.js + Axios + PWA (Service Worker + Manifest).

📁 Структура:
(src/components, src/pages, src/hooks, src/services, src/config, src/types, src/utils, sw.js, manifest.json).

⚙️ Включи:
- Автосохранение в IndexedDB.
- Поддержку оффлайн-режима.
- Сканер (камерный и Bluetooth).
- Цветовые и звуковые сигналы.
- Подсказки и ошибки в нижней панели.
- Автопереход в модуль «Размещение» после завершения.

📄 API конфиг в /config/server.json:
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token"
}

🧠 Обязательно структурируй код блоками, а не одной простынёй.
💾 Убедись, что результат можно сразу запустить как рабочее PWA-приложение.

После генерации кода — выведи краткое резюме по реализованным возможностям и совет по тестированию.
```



### Модуль: **🏷️ Размещение (Placement)**

---

### 🧭 1. Назначение модуля

Модуль «Размещение» предназначен для выполнения операции перемещения товаров из зоны приёмки в ячейки хранения на складе.
Он обеспечивает:

* автоматическую загрузку списка товаров, ожидающих размещения;
* сканирование ячеек и товаров;
* проверку правильности ячеек;
* визуальные и голосовые подсказки;
* контроль ошибок и оффлайн-буферизацию;
* авто-синхронизацию после восстановления соединения.

---

### 📋 2. Основные сценарии

| Этап | Действие                 | Описание                                                                                    |
| ---- | ------------------------ | ------------------------------------------------------------------------------------------- |
| 1    | Автоматический старт     | Если модуль открыт после приёмки — загружается последний документ. Иначе выбирается вручную |
| 2    | Сканирование ячейки      | Пользователь сканирует ячейку хранения, система определяет активную локацию                 |
| 3    | Сканирование товара      | Сканируется товар, система сверяет, разрешено ли размещение в этой ячейке                   |
| 4    | Проверка соответствия    | Если ячейка верна — добавляется запись. Если нет — подсказка и отказ                        |
| 5    | Визуальное подтверждение | Карточка товара обновляется (фон 🟢/🔴, прогресс-полоса)                                    |
| 6    | Повтор шагов             | Процесс повторяется для всех товаров                                                        |
| 7    | Завершение               | После завершения — кнопка «📤 Завершить размещение» или авто-предложение вернуться в меню   |

---

### 🧱 3. Требования к интерфейсу

#### Главный экран:

* **Заголовок:** «Размещение товаров»
* **Карточки:**

  * Товар (название, артикул)
  * Текущая ячейка
  * Осталось к размещению / Уже размещено
  * Индикация статуса (🟢 — верно, 🔴 — ошибка, ⚪ — ожидание)
* **Нижняя панель подсказок:**

  > «Сканируйте ячейку, затем товар»
  > «Ячейка неверная — попробуйте другую»

#### Кнопки действий:

* «📍 Изменить ячейку»
* «📤 Завершить документ»
* «🔄 Повторная синхронизация»

#### Цветовая и звуковая обратная связь:

* 🟢 Успех: короткий сигнал и вибрация
* 🔴 Ошибка: звук + голосовая подсказка
* 🟡 Предупреждение: мягкий вибросигнал

---

### 🗂 4. Архитектура и структура проекта

**Тип:** Installable PWA
**Frontend:** React + TypeScript + TailwindCSS
**Storage:** IndexedDB (через Dexie.js)
**API:** Axios / Fetch — параметры в `/config/server.json`
**Offline-first:** Service Worker + Background Sync

#### 📁 Структура файлов

```
/src
 ┣ /components
 ┃ ┣ PlacementCard.tsx
 ┃ ┣ ScanHint.tsx
 ┃ ┣ ErrorToast.tsx
 ┃ ┗ HeaderBar.tsx
 ┣ /pages
 ┃ ┗ Placement.tsx
 ┣ /hooks
 ┃ ┣ useScanner.ts
 ┃ ┣ useOfflineStorage.ts
 ┃ ┗ useSync.ts
 ┣ /services
 ┃ ┗ api.ts
 ┣ /config
 ┃ ┗ server.json
 ┣ /types
 ┃ ┗ placement.ts
 ┣ /utils
 ┃ ┗ sound.ts
 ┣ App.tsx
 ┣ index.tsx
 ┗ sw.js
```

---

### ⚙️ 5. Логика модулей

#### 🧩 useScanner

* Сканирует ячейку и товары;
* Определяет текущий контекст: активная ячейка / ожидаемый товар;
* При ошибке выдаёт визуальное и голосовое сообщение.

#### 🧩 useOfflineStorage

* Таблицы IndexedDB:

  * `placement_docs`
  * `placement_items`
  * `errors`
* Автосохранение локальных изменений;
* Автоотправка при восстановлении соединения.

#### 🧩 PlacementCard

* Отображает карточку товара;
* Визуализирует статус (план / факт);
* Анимированная шкала выполнения.

#### 🧩 useSync

* Контролирует очередь размещений;
* Выполняет отправку на сервер `/api/placement/sync`;
* Отмечает позиции как завершённые.

---

### 📡 6. Настройки подключения

Файл `/config/server.json`:

```json
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token"
}
```

---

### 🧪 7. Тестовые сценарии

| Тест | Проверка                                             |
| ---- | ---------------------------------------------------- |
| 1    | Сканирование правильной ячейки — успешное добавление |
| 2    | Сканирование неверной ячейки — ошибка и подсказка    |
| 3    | Сканирование товара — изменение прогресса            |
| 4    | Оффлайн-режим — сохранение локально                  |
| 5    | Восстановление связи — авто-синхронизация            |
| 6    | Завершение документа — переход в меню                |

---

## 💬 ГОТОВЫЙ ЗАПРОС К CLAUDE 4.5 SONNET (для Cursor IDE)

---

**ПРОМТ ДЛЯ CLAUDE 4.5 SONNET**

```
Ты — старший инженер-разработчик интерфейсного PWA-приложения Cleverence «Склад-15».  
Создай полностью рабочий модуль **"Размещение" (Placement)**, используя предоставленное ТЗ.

🎯 Цель:
Собрать готовый React + TypeScript PWA-модуль с оффлайн-поддержкой, сканированием ячеек и товаров, авто-синхронизацией и визуальной обратной связью.

🧠 Требования:
- Автоматическая подгрузка данных после модуля "Приёмка"
- Сканирование ячеек и товаров (Bluetooth / камера)
- Проверка правильности ячейки
- Цветовые и звуковые сигналы
- Подсказки и ошибки в нижней панели
- Автосохранение в IndexedDB
- Оффлайн-режим и авто-синхронизация
- Завершение документа и возврат в меню

📦 Используй стек:
React + TypeScript + TailwindCSS + Dexie.js + Axios + PWA (Service Worker + Manifest).

📁 Создай проектную структуру:
(src/components, src/pages, src/hooks, src/services, src/config, src/types, src/utils, sw.js, manifest.json)

📄 Конфиг API:
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token"
}

⚙️ Обязательно:
- Выводи код по файлам с заголовками `// === 📁 path/to/file ===`
- Код блоками, а не единым полотном
- Убедись, что результат можно запустить как PWA сразу после генерации

После генерации — выведи краткое резюме по реализованным функциям и рекомендации по тестированию.
```





### Модуль: **🚚 Подбор (Picking)**

---

### 🧭 1. Назначение модуля

Модуль **«Подбор»** предназначен для выполнения операции комплектации заказов.
Он обеспечивает:

* автоматическую загрузку документов заказов;
* оптимизированный маршрут подбора (по ячейкам);
* сканирование ячеек и товаров с контролем;
* визуальную и голосовую обратную связь;
* автоматическое завершение при полном совпадении;
* устойчивую работу в оффлайн-режиме с последующей синхронизацией.

---

### 📋 2. Основные сценарии

| Этап | Действие                   | Описание                                                              |
| ---- | -------------------------- | --------------------------------------------------------------------- |
| 1    | Открытие документа         | Пользователь выбирает документ заказа или сканирует QR                |
| 2    | Отображение маршрута       | Система показывает оптимальную последовательность ячеек               |
| 3    | Сканирование ячейки        | Подтверждение, что оператор подошёл к правильному месту               |
| 4    | Сканирование товара        | Проверка соответствия документу и ячейке                              |
| 5    | Подтверждение              | Товар добавляется в документ, обновляется счётчик                     |
| 6    | Переход к следующей ячейке | Автоматически по маршруту, голосовая подсказка                        |
| 7    | Завершение                 | При полном совпадении — авто-завершение или кнопка “Завершить подбор” |
| 8    | Опционально                | Печать этикеток / переход к “Отгрузке”                                |

---

### 🧱 3. Требования к интерфейсу

#### Главный экран:

* **Заголовок:** «🚚 Подбор заказа №12345»
* **Информация о документе:**

  * Клиент, маршрут, количество позиций
* **Карточки товаров:**

  * Название, артикул, количество (план/факт)
  * Ячейка назначения
  * Цветовая индикация (🟢/🟡/🔴)
  * Индикация прогресса

#### Навигация:

* “Хлебные крошки”:
  `Главное меню → Подбор → Документ → Текущая ячейка`
* Быстрые действия:

  * «⏭ Следующая ячейка»
  * «📤 Завершить подбор»
  * «🖨 Печать этикеток»

#### Подсказки:

> «Подойдите к ячейке A-12 и отсканируйте товар Lavazza 250г»
> «Ошибка: товар не из документа!»
> «Товар успешно добавлен, переход к A-13»

---

### 🗂 4. Архитектура и структура проекта

**Тип:** Installable PWA
**Frontend:** React + TypeScript + TailwindCSS
**Storage:** IndexedDB (Dexie.js)
**API:** Axios / Fetch — `/config/server.json`
**Offline-first:** Service Worker + Background Sync

#### 📁 Структура файлов

```
/src
 ┣ /components
 ┃ ┣ PickingCard.tsx
 ┃ ┣ RouteProgress.tsx
 ┃ ┣ ScanHint.tsx
 ┃ ┣ ErrorToast.tsx
 ┃ ┗ HeaderBar.tsx
 ┣ /pages
 ┃ ┗ Picking.tsx
 ┣ /hooks
 ┃ ┣ useScanner.ts
 ┃ ┣ useRoute.ts
 ┃ ┣ useOfflineStorage.ts
 ┃ ┗ useSync.ts
 ┣ /services
 ┃ ┗ api.ts
 ┣ /config
 ┃ ┗ server.json
 ┣ /types
 ┃ ┗ picking.ts
 ┣ /utils
 ┃ ┣ sound.ts
 ┃ ┗ voice.ts
 ┣ App.tsx
 ┣ index.tsx
 ┗ sw.js
```

---

### ⚙️ 5. Логика модулей

#### 🧩 useScanner

* Распознаёт тип сканируемого объекта (ячейка / товар);
* При скане ячейки — активирует контекст;
* При скане товара — сверяет с ожидаемой позицией.

#### 🧩 useRoute

* Загружает маршрут ячеек;
* Отмечает выполненные точки;
* Возвращает следующую ячейку и обновляет подсказки.

#### 🧩 useOfflineStorage

* Локальное хранение документа и прогресса подбора;
* Автосохранение каждые 30 секунд;
* Буферизация действий при потере сети.

#### 🧩 useSync

* Синхронизация с сервером (`/api/picking/sync`);
* Обновление статусов документов;
* Отображение индикатора прогресса синхронизации.

#### 🧩 RouteProgress

* Визуальная шкала выполнения маршрута (ячейки);
* Отображает процент завершения.

---

### 📡 6. Настройки подключения

Файл `/config/server.json`:

```json
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token"
}
```

---

### 🧪 7. Тестовые сценарии

| Тест | Проверка                                             |
| ---- | ---------------------------------------------------- |
| 1    | Сканирование правильной ячейки — активация контекста |
| 2    | Сканирование неверного товара — голосовая ошибка     |
| 3    | Сканирование правильного товара — авто-добавление    |
| 4    | Переход к следующей ячейке — автоматический          |
| 5    | Потеря связи — сохранение в IndexedDB                |
| 6    | Восстановление — авто-синхронизация                  |
| 7    | Завершение документа — проверка статуса 100 %        |
| 8    | Возврат к меню — без потери данных                   |

---

## 💬 ГОТОВЫЙ ЗАПРОС К CLAUDE 4.5 SONNET (для Cursor IDE)

---

**ПРОМТ ДЛЯ CLAUDE 4.5 SONNET**

```
Ты — старший инженер-разработчик интерфейсного PWA-приложения Cleverence «Склад-15».  
Создай рабочий модуль **"Подбор" (Picking)** в соответствии с приведённым техническим заданием.

🎯 Цель:
Создать полностью функциональный React + TypeScript PWA-модуль для подбора заказов с оффлайн-поддержкой, маршрутизацией ячеек, сканированием и авто-синхронизацией.

🧠 Требования:
- Сканирование ячеек и товаров (Bluetooth / камера)
- Голосовые подсказки и визуальные сигналы
- Отображение маршрута ячеек и прогресса
- Автоматический переход по маршруту
- Автосохранение в IndexedDB
- Оффлайн-режим и синхронизация после восстановления
- Завершение документа при 100% совпадении
- Возможность печати этикеток (через общий компонент)

📦 Используй стек:
React + TypeScript + TailwindCSS + Dexie.js + Axios + PWA (Service Worker + Manifest).

📁 Структура проекта:
(src/components, src/pages, src/hooks, src/services, src/config, src/types, src/utils, sw.js, manifest.json)

📄 Конфиг API:
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token"
}

⚙️ Обязательно:
- Код файлов выводи блоками с заголовками `// === 📁 path/to/file ===`
- Не выводи код единым полотном — только по частям
- Убедись, что результат можно сразу запустить как PWA-приложение в Cursor IDE

После генерации — выведи краткий отчёт о реализованных функциях и советы по тестированию.
```


Прекрасно, мой повелитель 👑 — следующий модуль готов. Ниже — полное, детальное ТЗ для **модуля «Отгрузка»** и готовый, адаптированный под **Claude 4.5 Sonnet** промт для автоматической генерации рабочего PWA-модуля в **Cursor IDE**. Всё сделано в том же стиле, что и предыдущие модули: подробные сценарии, UI-требования, структура файлов, логика, оффлайн/синхрон, тесты, конфиг API и явные инструкции по выводу кода по файлам (не один большой блок).

---



### Модуль: **🧾 Отгрузка (Shipment / Dispatch)**

---

### 🧭 1. Назначение модуля

Модуль «Отгрузка» фиксирует выдачу собранных заказов со склада — контроль комплектности, печать сопроводительных документов/этикеток, фиксация номера ТТН/транспортной компании, подтверждение факта отгрузки (опционально — подпись/фото). Обеспечивает устойчивую работу в реальных условиях склада (шум, перчатки, слабый Wi-Fi).

Функции:

* загрузка и выбор документов на отгрузку (из 1С/серверного очереди или из локальных завершённых подборов);
* сканирование собранных упаковок/штрихкодов для верификации;
* проверка комплектности (план vs факт), предупреждения о расхождениях;
* частичная отгрузка и фиксация остатков;
* печать транспортных и товарных этикеток / ТТН;
* сбор доказательств (фото, подпись — опционально);
* оффлайн-буферизация и последующая синхронизация.

---

### 📋 2. Основные сценарии

| Этап | Действие                   | Описание                                                                                  |
| ---- | -------------------------- | ----------------------------------------------------------------------------------------- |
| 1    | Выбор документа            | Оператор выбирает документ отгрузки или сканирует QR/штрихкод                             |
| 2    | Проверка комплекта         | Сканирование упаковок/штрихкодов, сверка с позицией документа                             |
| 3    | Обработка расхождений      | Предупреждение, предложение действий: скорректировать, отложить позицию, пометить возврат |
| 4    | Частичная отгрузка         | Возможность отгрузить часть документа (оставить остатки)                                  |
| 5    | Подтверждение доставки     | Ввод ТТН/трек-номера, печать этикеток, сбор подписи/фото при необходимости                |
| 6    | Завершение и синхронизация | Отправка статусов на сервер, маркировка документа как «Отгружен»                          |

---

### 🧱 3. Требования к интерфейсу

#### Главный экран

* Заголовок: `Отгрузка — Документ #<номер>`
* Панель документа: Клиент, адрес (если есть), количество позиций, общий вес/кол-во мест
* Список/карточки позиций:

  * Наименование, артикул, план/факт, упаковка/серийные номера
  * Статус строки (🟢/🟡/🔴)
  * Быстрые действия: «Отложить», «Пометить как брак», «Редактировать количество»
* Центральная область: полоса прогресса по проверенным позициям
* Нижняя подсказка: «Сканируйте упаковку / введите ТТН / нажмите «Завершить отгрузку»»

#### Доп. экраны/модалки

* Модал расхождения: показать план, факт, варианты действий (корректировка/фиксировать как недостачу/запросить подтверждение)
* Модал ввода ТТН и выбора способа доставки
* Экран подтверждения с кнопкой «Подпись / Фото» (опция)
* Предпросмотр и печать ТТН/этикеток

#### UX-правила

* Крупные кликабельные зоны (≥44×44px), контрастный интерфейс
* Звуковые/вибро сигналы при успешном скане и при ошибке
* Громкие, краткие подсказки (ниже экрана) — текст + голос (опционально)
* Кнопка «Отмена последнего» и автосохранение каждые 30 с

---

### 🗂 4. Архитектура и структура проекта

**Тип:** Installable PWA
**Frontend:** React + TypeScript + TailwindCSS
**Storage:** IndexedDB (Dexie.js)
**API:** Axios / Fetch — `/config/server.json`
**Offline-first:** Service Worker + Background Sync
**Доп. компоненты:** модуль печати, модуль сбора подписи (canvas), модуль фото (камера)

#### 📁 Рекомендуемая структура файлов

```
/src
 ┣ /components
 ┃ ┣ ShipmentCard.tsx
 ┃ ┣ ShipmentLine.tsx
 ┃ ┣ DiscrepancyModal.tsx
 ┃ ┣ TtnModal.tsx
 ┃ ┣ SignaturePad.tsx
 ┃ ┗ PrintPreview.tsx
 ┣ /pages
 ┃ ┗ Shipment.tsx
 ┣ /hooks
 ┃ ┣ useScanner.ts
 ┃ ┣ useShipment.ts
 ┃ ┣ useOfflineStorage.ts
 ┃ ┗ useSync.ts
 ┣ /services
 ┃ ┗ api.ts
 ┣ /config
 ┃ ┗ server.json
 ┣ /types
 ┃ ┗ shipment.ts
 ┣ /utils
 ┃ ┣ sound.ts
 ┃ ┗ printer.ts
 ┣ App.tsx
 ┣ index.tsx
 ┗ sw.js
```

---

### ⚙ 5. Логика модулей

#### 🧩 useShipment

* Загружает документ отгрузки (API / локально);
* Хранит состояние строк (plan / fact / status);
* Обрабатывает сканы упаковок/штрихкодов, находит соответствие в документе;
* Поддерживает частичную отгрузку: уменьшает факт в документе, оставляет остаток;
* Создаёт события для печати и для отправки ТТН.

#### 🧩 useScanner

* Определяет тип скана (упаковка / серийный / документ).
* На скане упаковки ищет соответствующие строки; при совпадении — увеличивает факт, при несоответствии — вызывает DiscrepancyModal.

#### 🧩 useOfflineStorage

* Таблицы IndexedDB: `shipments`, `shipment_lines`, `shipment_actions`, `errors`
* Сохраняет все действия оператора локально при отсутствии сети

#### 🧩 useSync

* Очередь отправки: `shipment_actions`
* Пакетная отправка на `/api/shipment/sync`
* Обработка ошибок синхронизации и повторные попытки с экспоненциальной задержкой

#### 🧩 PrintPreview / printer.ts

* Формирование шаблонов этикеток/TTH
* Поддержка мобильных Bluetooth-принтеров и сетевых печатей (через API сервера или Web Bluetooth)

#### 🧩 SignaturePad

* Захват подписи касанием/стилусом; хранение в base64; прикрепление к документу

---

### 📡 6. Настройки подключения

Файл `/config/server.json` (пример):

```json
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token",
  "printer": {
    "type": "network", 
    "defaultPrinter": "Zebra-01"
  }
}
```

---

### 🧪 7. Тестовые сценарии

| № | Тест                        | Ожидаемое поведение                              |
| - | --------------------------- | ------------------------------------------------ |
| 1 | Загрузка документа          | Документ загрузился, видны все строки            |
| 2 | Скан правильной упаковки    | Факт увеличился, строка зелёная                  |
| 3 | Скан чужой упаковки         | Модал расхождения; звук ошибки                   |
| 4 | Частичная отгрузка          | Остаются корректные остатки в документе          |
| 5 | Печать ТТН                  | Предпросмотр → печать → статус «Напечатано»      |
| 6 | Подпись/фото                | Подпись/фото прикреплены к документу             |
| 7 | Оффлайн — действия          | Все действия записались локально                 |
| 8 | Синхронизация после оффлайн | Данные успешно ушли на сервер, статусы обновлены |
| 9 | Ошибка принтера             | Показать ошибку и предложить повтор              |

---

## 💬 ГОТОВЫЙ ЗАПРОС К CLAUDE 4.5 SONNET (для Cursor IDE)

Скопируй и вставь этот блок в Claude 4.5 Sonnet — и он должен **полностью сгенерировать модуль «Отгрузка»**, разбивая код на файлы и размещая их в проекте в Cursor (без ручного вмешательства). Промт строго указывает формат вывода файлов — блоками с путями.

```
Ты — старший full-stack инженер и архитектор интерфейсов.  
Нужно создать рабочий PWA-модуль **"Отгрузка" (Shipment/Dispatch)** для системы «Склад-15», используя React + TypeScript + TailwindCSS, с оффлайн-поддержкой (IndexedDB / Dexie.js), синхронизацией и печатью.

Требования (кратко):
- Поддержка сканирования упаковок/штрихкодов (камера + Bluetooth);
- Проверка соответствия «План / Факт» для строк документа;
- Обработка расхождений (модал), частичных отгрузок;
- Ввод ТТН/трек-номеров, опция подписи/фото;
- Печать ТТН/этикеток (PrintPreview + printer util);
- Оффлайн-буферизация и авто-синхронизация на `/api/shipment/sync`;
- Настройки — `/config/server.json`;
- Автосохранение каждые 30 сек, кнопка «Отмена последнего действия»;
- Голос/звуковая/вибро обратная связь и подсказки.

Структура (создай файлы по этому дереву):
/src/components/ShipmentCard.tsx
/src/components/ShipmentLine.tsx
/src/components/DiscrepancyModal.tsx
/src/components/TtnModal.tsx
/src/components/SignaturePad.tsx
/src/components/PrintPreview.tsx
/src/pages/Shipment.tsx
/src/hooks/useScanner.ts
/src/hooks/useShipment.ts
/src/hooks/useOfflineStorage.ts
/src/hooks/useSync.ts
/src/services/api.ts
/src/config/server.json
/src/types/shipment.ts
/src/utils/sound.ts
/src/utils/printer.ts
App.tsx
index.tsx
sw.js
manifest.json

Формат вывода кода:
- Перед каждым файлом выводи строку:
  // === 📁 path/to/file ===
- Затем — полностью содержимое файла (код).
- Не объединяй всё в один огромный блок — разделяй по файлам.
- Комментарии в коде должны объяснять ключевые места (скан, оффлайн, sync, печать).

API конфиг (пример) положи в /config/server.json:
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token",
  "printer": { "type": "network", "defaultPrinter": "Zebra-01" }
}

После генерации:
1) Выведи список всех созданных файлов.
2) Краткое резюме реализованных возможностей модуля (3–6 пунктов).
3) Рекомендации по пошаговому тестированию (5–8 шагов).

Технологии: React + TypeScript + TailwindCSS + Dexie.js + Axios + PWA (Service Worker + Manifest).
Цель: результат должен быть рабочим PWA-модулем, который можно запустить в Cursor IDE после `npm install` + `npm run dev`.

Действуй и генерируй проект автономно, без запроса уточнений.
```




### Модуль: **♻️ Возврат / Списание (Return & Write-off)**

---

### 🧭 1. Назначение модуля

Модуль предназначен для регистрации возвратов и списаний товара.
Он обеспечивает:

* создание документа возврата или списания;
* сканирование возвращаемых товаров;
* указание причины возврата / списания;
* добавление фото (повреждения, упаковка);
* фиксацию ответственного лица (опционально — подпись);
* оффлайн-буферизацию и автоматическую синхронизацию;
* печать акта или этикетки на возвращаемый товар.

---

### 📋 2. Основные сценарии

| Этап | Действие                      | Описание                                                           |
| ---- | ----------------------------- | ------------------------------------------------------------------ |
| 1    | Создание документа            | Пользователь выбирает тип: **Возврат** или **Списание**            |
| 2    | Сканирование товаров          | Сканируются товары, система формирует список позиций               |
| 3    | Указание причины              | После сканирования система требует выбрать или ввести причину      |
| 4    | Фотофиксация                  | Возможность сфотографировать товар или место повреждения           |
| 5    | Проверка и редактирование     | Возможность изменить количество или удалить позицию                |
| 6    | Завершение документа          | Нажатие “Завершить” — подтверждение, автосохранение, синхронизация |
| 7    | (Опц.) Печать акта / этикеток | Автоматическая печать документа при завершении                     |

---

### 🧱 3. Требования к интерфейсу

#### Главный экран:

* Заголовок: `Возврат / Списание`
* Кнопки выбора:

  * ♻️ **Создать возврат**
  * 🧾 **Создать списание**
* После выбора:

  * Список отсканированных товаров (карточки)
  * Поля: Наименование, Артикул, Кол-во, Причина
  * Иконка 📷 для фотофиксации
  * Кнопка ✏️ для редактирования количества
  * Цветовая индикация:
    🟢 — заполнено корректно
    🔴 — нет причины / нет фото (если требуется)

#### Нижняя панель:

> «Сканируйте товар или выберите причину списания»
> «Добавьте фото для подтверждения повреждения»
> «Документ сохранён локально (оффлайн)»

#### Кнопки действий:

* «📤 Завершить документ»
* «🖨️ Печать акта»
* «🔄 Повторная синхронизация»

---

### 🗂 4. Архитектура и структура проекта

**Тип:** Installable PWA
**Frontend:** React + TypeScript + TailwindCSS
**Storage:** IndexedDB (Dexie.js)
**API:** Axios / Fetch — `/config/server.json`
**Offline-first:** Service Worker + Background Sync
**Доп. возможности:** фотофиксация (через `<input type="file" capture>`), подпись (SignaturePad), автопечать акта.

#### 📁 Структура файлов

```
/src
 ┣ /components
 ┃ ┣ ReturnCard.tsx
 ┃ ┣ ReasonModal.tsx
 ┃ ┣ PhotoCapture.tsx
 ┃ ┣ SignaturePad.tsx
 ┃ ┣ PrintPreview.tsx
 ┃ ┣ ScanHint.tsx
 ┃ ┗ HeaderBar.tsx
 ┣ /pages
 ┃ ┗ ReturnWriteoff.tsx
 ┣ /hooks
 ┃ ┣ useScanner.ts
 ┃ ┣ useReturnWriteoff.ts
 ┃ ┣ useOfflineStorage.ts
 ┃ ┗ useSync.ts
 ┣ /services
 ┃ ┗ api.ts
 ┣ /config
 ┃ ┗ server.json
 ┣ /types
 ┃ ┗ returnWriteoff.ts
 ┣ /utils
 ┃ ┣ sound.ts
 ┃ ┗ printer.ts
 ┣ App.tsx
 ┣ index.tsx
 ┗ sw.js
```

---

### ⚙️ 5. Логика модулей

#### 🧩 useReturnWriteoff

* Управляет документом и позициями;
* Определяет режим: “возврат” / “списание”;
* Добавляет товары при сканировании;
* Требует указать причину (модал);
* Сохраняет фото в base64;
* Сохраняет в IndexedDB и синхронизирует при возможности.

#### 🧩 useScanner

* Обрабатывает сканы товаров;
* Проверяет дублирование (предупреждает о повторном скане);
* Если режим активен — добавляет товар в документ.

#### 🧩 useOfflineStorage

* Таблицы: `returns`, `writeoffs`, `actions`, `errors`;
* Автосохранение каждые 30 секунд;
* Буферизация всех действий при оффлайне.

#### 🧩 useSync

* Отправка на `/api/return-sync` и `/api/writeoff-sync`;
* При успешной синхронизации — очистка локальных буферов;
* Отображение индикатора отправки.

#### 🧩 PhotoCapture

* Открывает камеру телефона / сканера;
* Захватывает фото → base64 → сохраняет в IndexedDB;
* Привязывает фото к позиции.

#### 🧩 ReasonModal

* Предлагает выбрать причину:

  * “Брак / Повреждение”
  * “Срок годности истёк”
  * “Ошибка комплектации”
  * “Возврат от клиента”
  * “Другое (ввести вручную)”
* Проверка обязательности перед завершением документа.

#### 🧩 PrintPreview

* Формирует акт возврата/списания;
* Печать через подключённый принтер (Bluetooth / Network).

---

### 📡 6. Настройки подключения

Файл `/config/server.json`:

```json
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token",
  "printer": {
    "type": "bluetooth",
    "defaultPrinter": "Zebra-Mobile"
  }
}
```

---

### 🧪 7. Тестовые сценарии

| № | Тест                        | Проверка                                   |
| - | --------------------------- | ------------------------------------------ |
| 1 | Создание документа возврата | Документ создаётся и сохраняется локально  |
| 2 | Сканирование товара         | Позиция появляется в списке                |
| 3 | Повторный скан              | Появляется предупреждение о дубликате      |
| 4 | Неуказанная причина         | Система блокирует завершение               |
| 5 | Фотофиксация                | Фото сохраняется и отображается в карточке |
| 6 | Завершение документа        | Статус меняется на “Завершён”              |
| 7 | Потеря связи                | Данные сохраняются локально                |
| 8 | Восстановление связи        | Автоматическая синхронизация               |
| 9 | Печать акта                 | Акт выводится и печатается успешно         |

---

## 💬 ГОТОВЫЙ ЗАПРОС К CLAUDE 4.5 SONNET (для Cursor IDE)

---

```
Ты — старший инженер-разработчик Cleverence, создающий PWA-приложение «Склад-15».  
Нужно реализовать рабочий модуль **"Возврат / Списание" (Return & Write-off)** в соответствии с приведённым ТЗ.

🎯 Цель:
Создать React + TypeScript PWA-модуль для регистрации возвратов и списаний с поддержкой фотофиксации, причин, печати акта и оффлайн-синхронизации.

📦 Функциональность:
- Создание документа (возврат / списание)
- Сканирование товаров
- Ввод/выбор причины
- Фотофиксация (через камеру)
- Подпись (опционально)
- Печать акта
- Оффлайн-режим (IndexedDB)
- Автосохранение каждые 30 секунд
- Авто-синхронизация на `/api/return-sync` и `/api/writeoff-sync`
- Голосовые и визуальные подсказки
- Вибро/звуковая обратная связь

📁 Создай файлы по структуре:
(src/components, src/pages, src/hooks, src/services, src/config, src/types, src/utils, sw.js, manifest.json)

⚙️ Перед каждым файлом пиши:
  // === 📁 path/to/file ===  
  <код файла>

⚠️ Не выводи весь код единым полотном — каждый файл отдельным блоком.

📄 Конфигурация API (/config/server.json):
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token",
  "printer": { "type": "bluetooth", "defaultPrinter": "Zebra-Mobile" }
}

💡 Используй стек:
React + TypeScript + TailwindCSS + Dexie.js + Axios + PWA (Service Worker + Manifest).

🎤 Добавь:
- Компонент PhotoCapture (камера)
- ReasonModal (выбор причины)
- PrintPreview (акт)
- Автоматическую печать при завершении
- Звуковые и вибро-сигналы

После генерации:
1. Выведи список всех созданных файлов.
2. Краткое описание реализованных возможностей.
3. Рекомендации по тестированию (5–7 шагов).
```



### Модуль: **🧮 Инвентаризация (Inventory)**

---

### 🧭 1. Назначение модуля

Модуль предназначен для проведения инвентаризации на складе: пересчёта фактических остатков и их сравнения с учётными данными.
Он должен:

* позволять сканировать ячейки и товары;
* автоматически подсчитывать количество;
* подсвечивать расхождения;
* сохранять данные в оффлайне и синхронизировать при подключении;
* обеспечивать скорость, простоту и защиту от ошибок.

---

### 📋 2. Основные сценарии

| Этап | Действие             | Описание                                                                      |
| ---- | -------------------- | ----------------------------------------------------------------------------- |
| 1    | Создание документа   | Пользователь выбирает тип «Инвентаризация» или открывает назначенный документ |
| 2    | Сканирование ячейки  | Сканируется ячейка склада — открывается контекст для этой зоны                |
| 3    | Сканирование товаров | Все товары, найденные в ячейке, последовательно сканируются                   |
| 4    | Автоподсчёт          | Каждое сканирование увеличивает «факт», отображается прогресс                 |
| 5    | Проверка расхождений | Если план ≠ факт — подсветка и сообщение                                      |
| 6    | Повторный пересчёт   | Возможность повторного сканирования для уточнения                             |
| 7    | Завершение           | Кнопка “📤 Завершить пересчёт”, авто-сохранение и синхронизация               |

---

### 🧱 3. Требования к интерфейсу

#### Главный экран:

* Заголовок: `Инвентаризация — Документ #XXXX`
* Панель состояния:

  * текущая ячейка
  * количество товаров / пересчитано
  * процент выполнения
* Список карточек товаров:

  * Наименование, Артикул, План / Факт
  * Цвет: 🟢 совпадает / 🔴 расхождение / ⚪ не тронуто
* Нижняя панель подсказок:

  > «Сканируйте ячейку, затем товары»
  > «Расхождение найдено — требуется проверка»

#### Кнопки действий:

* «⏭ Следующая ячейка»
* «📤 Завершить документ»
* «🔄 Синхронизация»
* «🧾 Отчёт по расхождениям»

#### Визуальные и звуковые сигналы:

* 🟢 успех: короткий сигнал + вибрация
* 🔴 ошибка: предупреждение + голос “Расхождение!”
* 🟡 предупреждение: “Нет в учёте”

---

### 🗂 4. Архитектура и структура проекта

**Тип:** Installable PWA
**Frontend:** React + TypeScript + TailwindCSS
**Storage:** IndexedDB (через Dexie.js)
**API:** Axios / Fetch (URL в `/config/server.json`)
**Offline-first:** Service Worker + Background Sync
**Особенности:** оффлайн-поддержка, визуальные отчёты, умная автосинхронизация.

#### 📁 Структура файлов

```
/src
 ┣ /components
 ┃ ┣ InventoryCard.tsx
 ┃ ┣ CellHeader.tsx
 ┃ ┣ DiscrepancyAlert.tsx
 ┃ ┣ ScanHint.tsx
 ┃ ┣ SummaryModal.tsx
 ┃ ┗ HeaderBar.tsx
 ┣ /pages
 ┃ ┗ Inventory.tsx
 ┣ /hooks
 ┃ ┣ useScanner.ts
 ┃ ┣ useInventory.ts
 ┃ ┣ useOfflineStorage.ts
 ┃ ┗ useSync.ts
 ┣ /services
 ┃ ┗ api.ts
 ┣ /config
 ┃ ┗ server.json
 ┣ /types
 ┃ ┗ inventory.ts
 ┣ /utils
 ┃ ┗ sound.ts
 ┣ App.tsx
 ┣ index.tsx
 ┗ sw.js
```

---

### ⚙️ 5. Логика модулей

#### 🧩 useInventory

* Управляет состоянием документа;
* Хранит список ячеек и товаров;
* Отмечает пересчитанные позиции;
* Вычисляет процент выполнения;
* Формирует итоговый отчёт (расхождения, недостачи).

#### 🧩 useScanner

* Определяет, что отсканировано: ячейка или товар;
* При скане ячейки — открывает контекст;
* При скане товара — увеличивает счётчик факта.

#### 🧩 useOfflineStorage

* Хранит локально все документы, сканы и расхождения;
* Автосохранение каждые 30 секунд;
* Восстановление после сбоя или перезапуска.

#### 🧩 useSync

* Проверяет соединение;
* При доступности — отправляет данные на `/api/inventory/sync`;
* После успешной отправки очищает локальные буферы.

#### 🧩 DiscrepancyAlert

* Показывает всплывающее окно при расхождении (количество, ячейка);
* Позволяет подтвердить как «факт» или «ошибка учёта».

#### 🧩 SummaryModal

* Отображает сводку перед завершением:
  количество товаров, ячеек, расхождений.

---

### 📡 6. Настройки подключения

Файл `/config/server.json`:

```json
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token"
}
```

---

### 🧪 7. Тестовые сценарии

| № | Тест                 | Проверка                                         |
| - | -------------------- | ------------------------------------------------ |
| 1 | Создание документа   | Документ создаётся и сохраняется локально        |
| 2 | Скан ячейки          | Ячейка активируется, отображается заголовок      |
| 3 | Скан товара          | Увеличивается факт, карточка зеленеет            |
| 4 | Скан чужого товара   | Появляется предупреждение и звук ошибки          |
| 5 | Расхождение          | Отображается красная карточка + DiscrepancyAlert |
| 6 | Оффлайн-режим        | Данные сохраняются локально                      |
| 7 | Восстановление связи | Автоматическая синхронизация с сервером          |
| 8 | Завершение документа | Отображается SummaryModal, отчёт сохраняется     |
| 9 | Повторное открытие   | Все данные и состояния восстанавливаются         |

---

## 💬 ГОТОВЫЙ ЗАПРОС К CLAUDE 4.5 SONNET (для Cursor IDE)

---

```
Ты — старший инженер-разработчик Cleverence.  
Нужно реализовать рабочий PWA-модуль **"Инвентаризация" (Inventory)** для системы «Склад-15» по приведённому техническому заданию.

🎯 Цель:
Создать React + TypeScript PWA-модуль для проведения инвентаризации с оффлайн-режимом, подсветкой расхождений, автосохранением и автоматической синхронизацией.

📦 Функциональность:
- Создание и ведение документов инвентаризации
- Сканирование ячеек и товаров
- Автоматический подсчёт факта
- Подсветка расхождений (цвет/звук)
- Просмотр сводки перед завершением
- Автосохранение (IndexedDB) каждые 30 сек
- Оффлайн-режим и синхронизация (`/api/inventory/sync`)
- Вибро/звуковая обратная связь и голосовые подсказки
- Просмотр отчёта по расхождениям

📁 Структура проекта (создай все файлы автоматически):
/src/components/InventoryCard.tsx  
/src/components/CellHeader.tsx  
/src/components/DiscrepancyAlert.tsx  
/src/components/ScanHint.tsx  
/src/components/SummaryModal.tsx  
/src/components/HeaderBar.tsx  
/src/pages/Inventory.tsx  
/src/hooks/useScanner.ts  
/src/hooks/useInventory.ts  
/src/hooks/useOfflineStorage.ts  
/src/hooks/useSync.ts  
/src/services/api.ts  
/src/config/server.json  
/src/types/inventory.ts  
/src/utils/sound.ts  
App.tsx  
index.tsx  
sw.js  
manifest.json

⚙️ Формат вывода:
- Перед каждым файлом пиши:  
  // === 📁 path/to/file ===  
  <код>
- Не выводи всё единым блоком, только структурировано по файлам.
- Комментарии в коде поясняют ключевые функции (скан, расхождения, оффлайн, sync).

📄 Конфиг API (/config/server.json):
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token"
}

💡 Технологии:
React + TypeScript + TailwindCSS + Dexie.js + Axios + PWA (Service Worker + Manifest).

После генерации:
1️⃣ Выведи список всех созданных файлов.  
2️⃣ Краткое описание реализованных возможностей.  
3️⃣ Рекомендации по тестированию (6–8 шагов).
```

Отлично, мой повелитель 👑.
Ты мыслишь стратегически — это два “служебных” модуля, но они **критически важны** в экосистеме «Склад-15»:

* **📋 “Сбор штрихкодов”** (Barcode Collector) — лёгкий модуль для массового считывания кодов и их экспорта;
* **🖨 “Печать этикеток”** (Label Printing) — инструмент формирования и печати ярлыков, упаковочных или товарных этикеток.

Сделаем их в едином стиле: сначала — **полное ТЗ**, затем — **отдельные готовые промты** для Claude 4.5 Sonnet (Cursor IDE).

---

# 📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ

## Модуль: **📋 Сбор штрихкодов (Barcode Collector)**

---

### 🧭 1. Назначение

Модуль предназначен для **быстрого последовательного сканирования штрихкодов** (товаров, коробок, ячеек, партий и т.д.) без сопоставления с документами.
Используется для инвентаризаций, аудитов, маркировки, сверок и отладки сканеров.

---

### 📋 2. Основные сценарии

| Этап | Действие      | Описание                                                        |
| ---- | ------------- | --------------------------------------------------------------- |
| 1    | Запуск модуля | Пользователь выбирает «Сбор штрихкодов»                         |
| 2    | Сканирование  | Каждый скан добавляет строку в список                           |
| 3    | Проверка      | При дубликате — предупреждение                                  |
| 4    | Экспорт       | Возможность выгрузить данные в TXT, CSV или отправить на сервер |
| 5    | Очистка       | После сохранения можно очистить список                          |

---

### 🧱 3. Интерфейс

* Заголовок: `Сбор штрихкодов`
* Счётчик сканов (вверху)
* Список последних 10 кодов
* Индикаторы состояния:

  * 🟢 успешный скан
  * 🔴 дубликат
  * 🟡 ошибка соединения
* Кнопки:

  * «📤 Экспорт»
  * «🗑 Очистить»
* Нижняя подсказка:

  > «Сканируйте штрихкоды подряд»
  > «Экспорт доступен оффлайн и онлайн»

---

### 🗂 4. Архитектура

**Тип:** PWA
**Frontend:** React + TypeScript + TailwindCSS
**Хранилище:** IndexedDB (Dexie.js)
**Экспорт:** сохранение в локальный CSV или отправка на `/api/barcodes/upload`

#### 📁 Структура файлов

```
/src
 ┣ /components
 ┃ ┣ BarcodeList.tsx
 ┃ ┣ Counter.tsx
 ┃ ┣ ScanHint.tsx
 ┃ ┗ HeaderBar.tsx
 ┣ /pages
 ┃ ┗ BarcodeCollector.tsx
 ┣ /hooks
 ┃ ┣ useScanner.ts
 ┃ ┣ useBarcodes.ts
 ┃ ┗ useExport.ts
 ┣ /services/api.ts
 ┣ /config/server.json
 ┣ /types/barcode.ts
 ┣ /utils/file.ts
 ┣ App.tsx
 ┣ index.tsx
 ┗ sw.js
```

---

### 🧪 5. Тестовые сценарии

| № | Тест            | Проверка                               |
| - | --------------- | -------------------------------------- |
| 1 | Первый скан     | Добавляется строка                     |
| 2 | Повторный скан  | Показывается предупреждение            |
| 3 | 100+ сканов     | Список и счётчик работают без лагов    |
| 4 | Оффлайн экспорт | Файл сохраняется локально              |
| 5 | Онлайн экспорт  | Отправляется на `/api/barcodes/upload` |
| 6 | Очистка списка  | Все данные удаляются из IndexedDB      |

---

## 💬 ПРОМТ ДЛЯ CLAUDE 4.5 SONNET — 📋 СБОР ШТРИХКОДОВ

```
Ты — инженер Cleverence.  
Создай модуль **"Сбор штрихкодов" (Barcode Collector)** для PWA-приложения «Склад-15».

🎯 Цель:
Быстрое последовательное сканирование и экспорт штрихкодов.

📦 Функциональность:
- Сканирование кодов (Bluetooth/камера)
- Предотвращение дубликатов
- Счётчик количества
- Список последних 10 сканов
- Экспорт в CSV / Upload на `/api/barcodes/upload`
- Автосохранение в IndexedDB
- Оффлайн поддержка
- Звуковая/визуальная обратная связь
- Кнопка очистки списка

📁 Структура проекта:
(src/components, src/pages, src/hooks, src/services, src/config, src/types, src/utils, sw.js, manifest.json)

⚙️ Формат вывода:
Перед каждым файлом указывай:
  // === 📁 path/to/file ===  
  <код>
Не выводи всё одним блоком.

После генерации:
1. Выведи список всех файлов.  
2. Краткое описание функций.  
3. Пошаговое руководство по тестированию.
```

---


## Модуль: **🖨️ Печать этикеток (Label Printing)**

---

### 🧭 1. Назначение

Модуль обеспечивает **формирование и печать этикеток** для товаров, коробок, ячеек или документов.
Может использоваться отдельно или вызываться из других модулей (Приёмка, Подбор, Возврат и т.д.).

---

### 📋 2. Основные сценарии

| Этап | Действие             | Описание                                                      |
| ---- | -------------------- | ------------------------------------------------------------- |
| 1    | Выбор источника      | Пользователь выбирает: вручную / из документа / по скану      |
| 2    | Формирование шаблона | Выбирается шаблон этикетки (товарная, ячейка, коробка и т.п.) |
| 3    | Настройка данных     | Ввод или подтягивание данных (название, штрихкод, дата, цена) |
| 4    | Предпросмотр         | Визуализация макета перед печатью                             |
| 5    | Печать               | Отправка на принтер (Bluetooth / Network / Server)            |
| 6    | Повторная печать     | Возможность повторить последнюю задачу                        |

---

### 🧱 3. Интерфейс

* Заголовок: `Печать этикеток`
* Поля:

  * Шаблон (dropdown)
  * Количество копий
  * Принтер (dropdown)
  * Переменные (название, дата, штрихкод и т.д.)
* Кнопки:

  * «🖨️ Печать»
  * «👁️ Предпросмотр»
  * «📂 Загрузить шаблон»
* Область предпросмотра — миниатюра шаблона.
* Нижняя подсказка:

  > «Выберите шаблон и источник данных»
  > «Печать возможна оффлайн при доступном Bluetooth-принтере»

---

### 🗂 4. Архитектура

**Тип:** PWA
**Frontend:** React + TypeScript + TailwindCSS
**Хранилище:** IndexedDB (кэш шаблонов)
**Печать:** Bluetooth, Network или API `/api/print`

#### 📁 Структура файлов

```
/src
 ┣ /components
 ┃ ┣ LabelPreview.tsx
 ┃ ┣ LabelForm.tsx
 ┃ ┣ TemplateSelect.tsx
 ┃ ┣ PrinterSelect.tsx
 ┃ ┣ PrintHistory.tsx
 ┃ ┗ HeaderBar.tsx
 ┣ /pages
 ┃ ┗ LabelPrinting.tsx
 ┣ /hooks
 ┃ ┣ usePrinter.ts
 ┃ ┣ useTemplates.ts
 ┃ ┣ useOfflineStorage.ts
 ┗ ┣ useSync.ts
 ┣ /services/api.ts
 ┣ /config/server.json
 ┣ /types/label.ts
 ┣ /utils/printer.ts
 ┣ App.tsx
 ┣ index.tsx
 ┗ sw.js
```

---

### ⚙️ 5. Логика модулей

#### 🧩 usePrinter

* Определяет доступные принтеры (Bluetooth, Network);
* Отправляет данные на выбранный принтер;
* При ошибке повторяет попытку / сохраняет задачу для фоновой печати.

#### 🧩 useTemplates

* Хранит список шаблонов в IndexedDB;
* Позволяет добавлять / удалять / обновлять шаблоны;
* Применяет значения переменных перед печатью.

#### 🧩 LabelPreview

* Отображает визуальный макет этикетки;
* Обновляется при изменении данных.

#### 🧩 PrintHistory

* Сохраняет историю последних печатей;
* Позволяет повторить задачу.

---

### 🧪 6. Тестовые сценарии

| № | Тест             | Проверка                                        |
| - | ---------------- | ----------------------------------------------- |
| 1 | Загрузка шаблона | Шаблон сохранён локально                        |
| 2 | Предпросмотр     | Отображается корректно                          |
| 3 | Выбор принтера   | Принтер определяется автоматически              |
| 4 | Печать онлайн    | Отправка на сервер успешна                      |
| 5 | Печать оффлайн   | Задача сохраняется и выполняется при соединении |
| 6 | Повторная печать | Повтор успешен                                  |
| 7 | Ошибка принтера  | Показ предупреждения и лог в истории            |

---

## 💬 ПРОМТ ДЛЯ CLAUDE 4.5 SONNET — 🖨️ ПЕЧАТЬ ЭТИКЕТОК

```
Ты — инженер Cleverence.  
Создай модуль **"Печать этикеток" (Label Printing)** для PWA-приложения «Склад-15» по приведённому ТЗ.

🎯 Цель:
Создать React + TypeScript PWA-модуль с оффлайн-поддержкой, выбором шаблонов и Bluetooth/Network-печатью.

📦 Функциональность:
- Выбор шаблона и источника данных
- Настройка переменных (название, штрихкод, дата, цена)
- Предпросмотр
- Печать (через Bluetooth / сеть / API `/api/print`)
- История печати и повторная печать
- Кэширование шаблонов в IndexedDB
- Автоотправка задач после восстановления соединения
- Звуковая и визуальная обратная связь

📁 Создай файлы:
(src/components, src/pages, src/hooks, src/services, src/config, src/types, src/utils, sw.js, manifest.json)

⚙️ Формат вывода:
Перед каждым файлом:
  // === 📁 path/to/file ===  
  <код>

📄 Конфиг API:
{
  "apiBaseUrl": "https://warehouse.company.com/api",
  "syncIntervalSec": 60,
  "authMode": "token",
  "printer": { "type": "bluetooth", "defaultPrinter": "Zebra-Mobile" }
}

💡 Технологии:
React + TypeScript + TailwindCSS + Dexie.js + Axios + PWA (Service Worker + Manifest).

После генерации:
1. Список созданных файлов.  
2. Краткое резюме функций.  
3. Рекомендации по тестированию.
```
