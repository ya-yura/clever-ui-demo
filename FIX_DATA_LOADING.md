# 🔧 Инструкция по исправлению загрузки данных

## Проблема
Данные не отображаются в приложении после обновления.

## ✅ Решение (выполните по порядку)

### Шаг 1: Очистите localStorage
1. Откройте http://localhost:3000 в браузере
2. Нажмите **F12** (откроется DevTools)
3. Перейдите во вкладку **Console**
4. Выполните команду:
```javascript
localStorage.removeItem('initial_data_loaded');
console.log('✅ Flag removed');
```

### Шаг 2: Жестко перезагрузите страницу
- Нажмите **Ctrl + Shift + R** (Windows/Linux)
- Или **Cmd + Shift + R** (Mac)
- Это перезагрузит страницу без кэша

### Шаг 3: Проверьте консоль
В Console должны появиться сообщения:
```
🔄 Loading initial data from JSON files...
✅ Receiving data loaded: 5 documents
✅ Placement data loaded: 4 documents
✅ Picking data loaded: 5 documents
✅ Shipment data loaded: 4 documents
✅ Return data loaded: 5 documents
✅ Inventory data loaded: 4 documents
🎉 All initial data loaded successfully!
📊 Total: 27 documents, 278 lines
```

### Шаг 4: Проверьте IndexedDB
1. В DevTools перейдите во вкладку **Application**
2. В левой панели найдите **IndexedDB** → **WarehouseDB**
3. Разверните и посмотрите таблицы:
   - `receivingDocuments` - должно быть 5 записей
   - `placementDocuments` - должно быть 4 записи
   - и т.д.

### Шаг 5: Проверьте главную страницу
- На главной странице на каждой карточке модуля должны быть бейджи с числами
- Приёмка: **5**
- Размещение: **4**  
- Подбор: **5**
- Отгрузка: **4**
- Возврат: **5**
- Инвентаризация: **4**

---

## 🐛 Если всё равно не работает

### Вариант А: Полная очистка
Выполните в Console:
```javascript
// Очистить всё
localStorage.clear();
indexedDB.deleteDatabase('WarehouseDB');
location.reload();
```

### Вариант Б: Проверьте ошибки
1. Посмотрите в Console на красные сообщения об ошибках
2. Если видите ошибки типа "Cannot read property...", сделайте скриншот
3. Если видите "404 Not Found", проблема с путями к файлам

### Вариант В: Используйте приватное окно
1. Закройте все вкладки с приложением
2. Откройте **новое приватное окно** (Ctrl+Shift+N в Chrome)
3. Перейдите на http://localhost:3000
4. Откройте Console (F12) и проверьте сообщения

---

## 📝 Что должно работать после исправления

### Главная страница:
- ✅ 6 карточек модулей
- ✅ Бейджи с числами документов
- ✅ Статус "PWA Установлено", "Онлайн"

### Модуль Приёмка:
- ✅ Список из 5 документов
- ✅ Документы с именами поставщиков
- ✅ Статусы: "Завершен", "В работе", "Ожидает"
- ✅ При клике на документ - открывается детальный вид

### Детальный вид документа:
- ✅ Заголовок с номером (например, RCV-001)
- ✅ Прогресс-бар
- ✅ Список товаров
- ✅ Количества (план/факт)

---

## 🆘 Альтернативное решение (если ничего не помогло)

Выполните эти команды в терминале:

```bash
# Остановите сервер (Ctrl+C)

# Очистите кэш npm
npm cache clean --force

# Переустановите зависимости
rm -rf node_modules
rm package-lock.json
npm install

# Запустите заново
npm run dev
```

Потом откройте в браузере и выполните Шаг 1-5 из инструкции выше.

---

**Важно:** Обязательно сообщите, какие сообщения вы видите в Console после выполнения Шагов 1-3!

