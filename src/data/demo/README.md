# 📦 Демо-данные для Склад-15

Эта папка содержит полный набор демо-данных для демонстрации всех возможностей приложения.

## 📊 Структура данных

### Типы документов (`doctypes.json`)
- **PrihodNaSklad** - Приход на склад (Приёмка)
- **PodborZakaza** - Подбор заказа
- **Otgruzka** - Отгрузка
- **Inventarizaciya** - Инвентаризация
- **RazmeshhenieVYachejki** - Размещение в ячейки
- **Vozvrat** - Возврат/Списание

### Документы (`documents.json`)
Содержит по 4-5 документов каждого типа с разными статусами:
- ✅ `completed` - завершённые
- 🔄 `in_progress` - в процессе
- ⏸️ `pending` - ожидают выполнения

**Всего: 27 документов**

### Справочники

#### Товары (`products.json`)
35 товаров разных категорий:
- Напитки (кофе, чай, соки, вода)
- Бакалея (крупы, макароны, мука)
- Молочная продукция
- Хлебобулочные изделия
- Кондитерские изделия
- Консервы
- Соусы
- Снеки

#### Ячейки хранения (`cells.json`)
18 ячеек в 5 зонах:
- **Зона A** - стандартные ячейки (6 шт)
- **Зона B** - большие ячейки (4 шт)
- **Зона C** - bulk-ячейки для сыпучих (3 шт)
- **Зона D** - холодильные ячейки (3 шт)
- **Зона E** - морозильные ячейки (2 шт)

#### Сотрудники (`employees.json`)
10 сотрудников с разными ролями:
- Кладовщики
- Комплектовщики
- Приемщики
- Грузчики
- Старший кладовщик

#### Контрагенты (`partners.json`)
10 контрагентов:
- 5 поставщиков
- 5 клиентов (покупателей)

#### Склады (`warehouses.json`)
3 склада:
- Основной склад
- Запасной склад
- Холодильный склад

## 🔄 Автоматический fallback

Система автоматически использует демо-данные в следующих случаях:

1. **API сервер недоступен** - при невозможности подключения к серверу
2. **Пустой ответ API** - когда сервер не возвращает данные
3. **Ошибки кэша** - при проблемах с локальным хранилищем

### Порядок попыток загрузки данных:

```
1. Проверка флага demo_mode в localStorage
   ↓ (если не установлен)
2. Попытка загрузить из кэша (если актуален)
   ↓ (если кэш пуст/устарел)
3. Запрос к API серверу
   ↓ (если API недоступен)
4. Попытка использовать устаревший кэш
   ↓ (если кэш пуст)
5. **FALLBACK → Демо-данные** ✅
```

## 🛠️ Регенерация демо-данных

Если нужно обновить демо-данные из исходных файлов (`receiving.json`, `picking.json` и т.д.):

```bash
node scripts/generate-demo-data.cjs
```

Скрипт автоматически:
- Читает данные из `src/data/*.json`
- Конвертирует в формат OData
- Сохраняет в `src/data/demo/documents.json`

## 📝 Формат данных

Все данные соответствуют спецификации OData и имеют структуру:

```json
{
  "@odata.context": "http://localhost:9000/MobileSMARTS/api/v1/$metadata#...",
  "value": [...]
}
```

## 🎯 Использование в коде

Демо-данные загружаются через `demoDataService`:

```typescript
import { demoDataService } from '@/services/demoDataService';

// Получить типы документов
const docTypes = demoDataService.getDocTypes();

// Получить документы по типу
const docs = demoDataService.getDocuments('PrihodNaSklad');

// Получить товары
const products = demoDataService.getProducts();

// Получить все документы
const allDocs = demoDataService.getAllDocuments();
```

## ✅ Проверка данных

Статистика доступна через:

```typescript
const stats = demoDataService.getStats();
console.log(stats);
// {
//   docTypesCount: 6,
//   totalDocuments: 27,
//   byType: { ... },
//   productsCount: 35,
//   cellsCount: 18,
//   partnersCount: 10,
//   employeesCount: 10,
//   warehousesCount: 3
// }
```

## 🚀 Результат

Приложение **всегда** работает, даже без подключения к серверу, демонстрируя все возможности:
- Приёмка товаров
- Размещение в ячейки
- Подбор заказов
- Отгрузка
- Инвентаризация
- Возвраты и списания

**Демо-данные полностью восстановлены и готовы к использованию! ✨**
