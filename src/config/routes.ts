/**
 * ⚠️ КРИТИЧНО: НЕ ИЗМЕНЯТЬ ЗНАЧЕНИЯ КОНСТАНТ! ⚠️
 * 
 * Этот файл содержит константы маршрутов для всех операций системы "Склад-15".
 * Эти константы используются в НЕСКОЛЬКИХ программах для привязки к кнопкам операций.
 * 
 * ИЗМЕНЕНИЕ ЭТИХ ЗНАЧЕНИЙ НАРУШИТ РАБОТУ ИНТЕГРАЦИИ!
 * 
 * Дата фиксации: 12 ноября 2025
 * Версия: 1.0.0 (FROZEN)
 * Статус: 🔒 LOCKED - НЕ ИЗМЕНЯТЬ
 */

// ==================== ОСНОВНЫЕ ОПЕРАЦИИ (ГЛАВНЫЙ ЭКРАН) ====================

/**
 * Маршруты для основных складских операций.
 * Эти маршруты используются кнопками на главном экране.
 * 
 * Формат: `/docs/{UniqueDocTypeId}`
 * где {UniqueDocTypeId} - уникальный идентификатор типа документа из API
 */
export const OPERATION_ROUTES = {
  /**
   * Приход на склад (Приёмка товаров)
   * Операция: Приём товаров от поставщиков с проверкой план/факт
   * Маршрут: /docs/PrihodNaSklad
   */
  RECEIVING: '/docs/PrihodNaSklad',

  /**
   * Подбор заказа (Комплектация)
   * Операция: Сборка заказа по маршруту ячеек для отгрузки
   * Маршрут: /docs/PodborZakaza
   */
  PICKING: '/docs/PodborZakaza',

  /**
   * Отгрузка
   * Операция: Проверка комплектности и оформление отгрузки клиенту
   * Маршрут: /docs/Otgruzka
   */
  SHIPMENT: '/docs/Otgruzka',

  /**
   * Инвентаризация
   * Операция: Пересчет остатков и фиксация расхождений
   * Маршрут: /docs/Inventarizaciya
   */
  INVENTORY: '/docs/Inventarizaciya',

  /**
   * Размещение в ячейки
   * Операция: Размещение принятого товара по ячейкам хранения
   * Маршрут: /docs/RazmeshhenieVYachejki
   */
  PLACEMENT: '/docs/RazmeshhenieVYachejki',

  /**
   * Возврат/Списание
   * Операция: Оформление возврата или списания с указанием причины
   * Маршрут: /docs/Vozvrat
   */
  RETURN: '/docs/Vozvrat',

  /**
   * Перемещение
   * Операция: Перемещение товара между ячейками или зонами
   * Маршрут: /docs/Peremeshenie
   */
  MOVEMENT: '/docs/Peremeshenie',

  /**
   * Маркировка
   * Операция: Нанесение или перепечатка этикеток и кодов
   * Маршрут: /docs/Markirovka
   */
  LABELING: '/docs/Markirovka',
} as const;

// ==================== LEGACY МАРШРУТЫ (СТАРЫЕ МОДУЛИ) ====================

/**
 * Устаревшие прямые маршруты к модулям.
 * ⚠️ Сохранены для обратной совместимости, но рекомендуется использовать OPERATION_ROUTES
 * 
 * @deprecated Используйте OPERATION_ROUTES вместо этих маршрутов
 */
export const LEGACY_MODULE_ROUTES = {
  /**
   * @deprecated Используйте OPERATION_ROUTES.RECEIVING
   */
  RECEIVING: '/receiving',
  RECEIVING_DETAIL: '/receiving/:id',

  /**
   * @deprecated Используйте OPERATION_ROUTES.PLACEMENT
   */
  PLACEMENT: '/placement',
  PLACEMENT_DETAIL: '/placement/:id',

  /**
   * @deprecated Используйте OPERATION_ROUTES.PICKING
   */
  PICKING: '/picking',
  PICKING_DETAIL: '/picking/:id',

  /**
   * @deprecated Используйте OPERATION_ROUTES.SHIPMENT
   */
  SHIPMENT: '/shipment',
  SHIPMENT_DETAIL: '/shipment/:id',

  /**
   * @deprecated Используйте OPERATION_ROUTES.RETURN
   */
  RETURN: '/return',
  RETURN_DETAIL: '/return/:id',

  /**
   * @deprecated Используйте OPERATION_ROUTES.INVENTORY
   */
  INVENTORY: '/inventory',
  INVENTORY_DETAIL: '/inventory/:id',
} as const;

// ==================== СИСТЕМНЫЕ МАРШРУТЫ ====================

/**
 * Маршруты системных страниц и служебных функций.
 * Эти маршруты не связаны с конкретными операциями,
 * но используются в меню и навигации.
 */
export const SYSTEM_ROUTES = {
  /**
   * Главная страница (список всех операций)
   */
  HOME: '/',

  /**
   * Все документы (универсальный просмотр)
   */
  DOCUMENTS: '/documents',

  /**
   * Документы по типу (динамический маршрут)
   * Параметр: docTypeUni - уникальный идентификатор типа документа
   */
  DOCUMENTS_BY_TYPE: '/docs/:docTypeUni',

  /**
   * Детали документа (динамический маршрут)
   * Параметры: docTypeUni, docId
   */
  DOCUMENT_DETAILS: '/docs/:docTypeUni/:docId',

  /**
   * Управление напарником
   */
  PARTNER: '/partner',

  /**
   * Настройки приложения
   */
  SETTINGS: '/settings',

  /**
   * Статистика и KPI
   */
  STATISTICS: '/statistics',

  /**
   * Диагностика системы
   */
  DIAGNOSTICS: '/diagnostics',

  /**
   * О программе
   */
  ABOUT: '/about',

  /**
   * Обратная связь
   */
  FEEDBACK: '/feedback',

  /**
   * Тест звуков (для отладки)
   */
  SOUND_TEST: '/sound-test',

  /**
   * Showcase дизайн-системы (компоненты, токены, примеры)
   */
  DESIGN_SYSTEM: '/design-system',

  /**
   * Кастомный интерфейс (динамическая сборка)
   */
  CUSTOM_INTERFACE: '/custom-interface',
} as const;

// ==================== ПУБЛИЧНЫЕ МАРШРУТЫ (БЕЗ АВТОРИЗАЦИИ) ====================

/**
 * Маршруты, доступные без авторизации.
 */
export const PUBLIC_ROUTES = {
  /**
   * Первоначальная настройка подключения к серверу
   */
  SETUP: '/setup',

  /**
   * Страница входа в систему
   */
  LOGIN: '/login',
} as const;

// ==================== ТИПЫ ДЛЯ TYPESCRIPT ====================

/**
 * Тип для маршрута операции
 */
export type OperationRoute = typeof OPERATION_ROUTES[keyof typeof OPERATION_ROUTES];

/**
 * Тип для системного маршрута
 */
export type SystemRoute = typeof SYSTEM_ROUTES[keyof typeof SYSTEM_ROUTES];

/**
 * Тип для публичного маршрута
 */
export type PublicRoute = typeof PUBLIC_ROUTES[keyof typeof PUBLIC_ROUTES];

/**
 * Любой валидный маршрут в приложении
 */
export type AppRoute = OperationRoute | SystemRoute | PublicRoute | string;

// ==================== ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ ====================

/**
 * Построить маршрут к документу конкретного типа
 * 
 * @param docTypeUni - Уникальный идентификатор типа документа
 * @returns Маршрут к списку документов этого типа
 * 
 * @example
 * ```ts
 * getDocTypeRoute('PrihodNaSklad') // => '/docs/PrihodNaSklad'
 * ```
 */
export function getDocTypeRoute(docTypeUni: string): string {
  return `/docs/${docTypeUni}`;
}

/**
 * Построить маршрут к конкретному документу
 * 
 * @param docTypeUni - Уникальный идентификатор типа документа
 * @param docId - Идентификатор документа
 * @returns Маршрут к детальной странице документа
 * 
 * @example
 * ```ts
 * getDocumentRoute('PrihodNaSklad', 'RCV-001') // => '/docs/PrihodNaSklad/RCV-001'
 * ```
 */
export function getDocumentRoute(docTypeUni: string, docId: string): string {
  return `/docs/${docTypeUni}/${docId}`;
}

/**
 * Проверить, является ли маршрут маршрутом операции
 * 
 * @param route - Проверяемый маршрут
 * @returns true, если маршрут является маршрутом операции
 * 
 * @example
 * ```ts
 * isOperationRoute('/docs/PrihodNaSklad') // => true
 * isOperationRoute('/settings') // => false
 * ```
 */
export function isOperationRoute(route: string): boolean {
  return Object.values(OPERATION_ROUTES).includes(route as OperationRoute);
}

/**
 * Получить название операции по маршруту
 * 
 * @param route - Маршрут операции
 * @returns Название операции или null, если маршрут не найден
 * 
 * @example
 * ```ts
 * getOperationNameByRoute('/docs/PrihodNaSklad') // => 'RECEIVING'
 * getOperationNameByRoute('/settings') // => null
 * ```
 */
export function getOperationNameByRoute(route: string): keyof typeof OPERATION_ROUTES | null {
  const entry = Object.entries(OPERATION_ROUTES).find(([_, value]) => value === route);
  return entry ? (entry[0] as keyof typeof OPERATION_ROUTES) : null;
}

// ==================== ЭКСПОРТ ПО УМОЛЧАНИЮ ====================

/**
 * Все маршруты приложения, сгруппированные по категориям
 */
export const APP_ROUTES = {
  /**
   * Основные операции (используются на главном экране)
   */
  operations: OPERATION_ROUTES,

  /**
   * Системные страницы
   */
  system: SYSTEM_ROUTES,

  /**
   * Публичные страницы (без авторизации)
   */
  public: PUBLIC_ROUTES,

  /**
   * Устаревшие маршруты (для обратной совместимости)
   * @deprecated
   */
  legacy: LEGACY_MODULE_ROUTES,
} as const;

export default APP_ROUTES;

// ==================== ДОКУМЕНТАЦИЯ ДЛЯ ИНТЕГРАЦИИ ====================

/**
 * РУКОВОДСТВО ПО ИНТЕГРАЦИИ
 * =========================
 * 
 * Для других программ, использующих эти маршруты:
 * 
 * 1. ИМПОРТ КОНСТАНТ
 * ------------------
 * ```typescript
 * import { OPERATION_ROUTES } from './config/routes';
 * 
 * // Использование
 * const receivingUrl = OPERATION_ROUTES.RECEIVING; // '/docs/PrihodNaSklad'
 * ```
 * 
 * 2. ПРИВЯЗКА К КНОПКАМ
 * ---------------------
 * ```typescript
 * const buttons = [
 *   { label: 'Приход', route: OPERATION_ROUTES.RECEIVING },
 *   { label: 'Подбор', route: OPERATION_ROUTES.PICKING },
 *   { label: 'Отгрузка', route: OPERATION_ROUTES.SHIPMENT },
 *   // и т.д.
 * ];
 * ```
 * 
 * 3. НАВИГАЦИЯ
 * ------------
 * ```typescript
 * // React Router
 * navigate(OPERATION_ROUTES.RECEIVING);
 * 
 * // Vanilla JS
 * window.location.href = OPERATION_ROUTES.RECEIVING;
 * ```
 * 
 * 4. ДИНАМИЧЕСКИЕ МАРШРУТЫ
 * ------------------------
 * ```typescript
 * // Для произвольного типа документа
 * const route = getDocTypeRoute('CustomDocType'); // '/docs/CustomDocType'
 * 
 * // Для конкретного документа
 * const route = getDocumentRoute('PrihodNaSklad', 'DOC-123'); 
 * // '/docs/PrihodNaSklad/DOC-123'
 * ```
 * 
 * 5. ПРОВЕРКА МАРШРУТОВ
 * ---------------------
 * ```typescript
 * if (isOperationRoute(currentRoute)) {
 *   console.log('Это маршрут операции');
 * }
 * 
 * const operationName = getOperationNameByRoute(currentRoute);
 * if (operationName) {
 *   console.log('Операция:', operationName); // 'RECEIVING', 'PICKING', etc.
 * }
 * ```
 * 
 * 6. ВАЖНО!
 * ---------
 * - НЕ изменяйте значения констант OPERATION_ROUTES
 * - НЕ добавляйте новые операции без согласования
 * - НЕ удаляйте существующие операции
 * - При необходимости изменений - обновите ВСЕ интегрированные программы
 * 
 * 7. ВЕРСИОНИРОВАНИЕ
 * ------------------
 * Текущая версия: 1.0.0 (FROZEN с 12 ноября 2025)
 * 
 * При критических изменениях:
 * - Создайте новый файл routes.v2.ts
 * - Сохраните этот файл для обратной совместимости
 * - Обновите все интеграции постепенно
 */




