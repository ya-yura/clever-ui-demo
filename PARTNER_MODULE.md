# 🤝 Модуль "Работа с напарником" — Документация

## 🎯 Назначение

Модуль **"Работа с напарником"** обеспечивает функциональность совместной работы двух сотрудников на складе. Позволяет выбирать напарника, отслеживать совместные сессии работы и собирать статистику эффективности команды.

---

## 🏗️ Архитектура модуля

### Структура файлов

```
src/
├── types/
│   └── partner.ts                   # Типы для сотрудников и сессий
├── services/
│   ├── db.ts                        # Обновлена для хранения данных
│   └── partnerService.ts            # Сервис управления напарниками
├── components/
│   └── partner/
│       ├── PartnerCard.tsx          # Карточка сотрудника
│       ├── PartnerStatus.tsx        # Статус текущей сессии
│       ├── PartnerSelector.tsx      # Выбор напарника
│       └── index.ts                 # Экспорты
├── pages/
│   └── PartnerManagement.tsx        # Главная страница модуля
├── data/
│   └── employees.json               # Тестовые данные сотрудников
└── utils/
    └── loadInitialData.ts           # Загрузка тестовых данных
```

---

## 📦 Основные компоненты

### 1. **PartnerService** (`src/services/partnerService.ts`)

Сервис для управления сотрудниками и сессиями совместной работы.

#### Основные методы:

```typescript
// Получить всех сотрудников
await partnerService.getAllEmployees();

// Получить активных сотрудников
await partnerService.getActiveEmployees();

// Фильтрация сотрудников
await partnerService.filterEmployees(filter);

// Начать сессию с напарником
await partnerService.startSession(userId, partnerId, workType);

// Управление сессией
await partnerService.pauseSession(sessionId);
await partnerService.resumeSession(sessionId);
await partnerService.endSession(sessionId);

// Статистика
await partnerService.getPartnerStats(userId, partnerId);
await partnerService.getRecentPartners(userId);
```

### 2. **PartnerCard** (`src/components/partner/PartnerCard.tsx`)

Карточка отображения сотрудника в списке.

**Отображает:**
- Аватар с инициалами
- Имя и табельный номер
- Роль и отдел
- Статистику совместной работы (опционально)
- Время последней активности
- Статус (активен/неактивен)

### 3. **PartnerStatus** (`src/components/partner/PartnerStatus.tsx`)

Компонент отображения текущей сессии совместной работы.

**Функции:**
- Показывает информацию о напарнике
- Отображает длительность сессии
- Показывает статистику (документы, строки, товары)
- Кнопки управления (пауза, возобновление, завершение)

### 4. **PartnerSelector** (`src/components/partner/PartnerSelector.tsx`)

Компонент для выбора напарника.

**Возможности:**
- Поиск по имени, табельному, роли
- Переключение между недавними и всеми сотрудниками
- Отображение статистики недавних напарников
- Визуальное выделение выбранного сотрудника

### 5. **PartnerManagement** (`src/pages/PartnerManagement.tsx`)

Главная страница модуля.

**Функциональность:**
- Отображение текущей активной сессии
- Выбор нового напарника
- Управление сессией
- Быстрый доступ к документам и модулям

---

## 🎨 Интерфейс пользователя

### Главная страница

**Карточка на Home.tsx:**
```
┌─────────────────────────────────────┐
│ 🤝 Работа с напарником         ✓   │
│ Выберите напарника для              │
│ совместной работы                   │
└─────────────────────────────────────┘
```

### Выбор напарника

**Экран выбора:**
```
┌─────────────────────────────────────┐
│ Выбор напарника                 ✕   │
│                                     │
│ 🔍 [Поиск...]                       │
│                                     │
│ [Недавние (3)] [Все (10)]           │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ ИИ  Иванов Иван       #001      │ │
│ │     Кладовщик • Склад основной  │ │
│ │     ━━━━━━━━━━━━                │ │
│ │     Сессий: 5  Часов: 12  90%   │ │
│ └─────────────────────────────────┘ │
│                                     │
│ Выбран: Иванов Иван                 │
│ [Отмена] [✓ Начать работу]          │
└─────────────────────────────────────┘
```

### Активная сессия

**Статус работы:**
```
┌─────────────────────────────────────┐
│ 🤝 Работаете с напарником           │
│    Иванов Иван            2ч 30мин  │
│                                     │
│ 📦 Приёмка                           │
│                                     │
│ Документов: 3  Строк: 45  Товаров: 120 │
│                                     │
│ [⏸️ Пауза]  [✓ Завершить]            │
└─────────────────────────────────────┘
```

---

## 🗄️ База данных

### Таблицы IndexedDB

#### employees
```
id: string (primary key)
name: string
badge: string
role: string
department: string
phone: string
isActive: boolean
createdAt: number
lastActiveAt: number
```

#### partnerSessions
```
id: string (primary key)
userId: string
partnerId: string
startedAt: number
endedAt: number
status: SessionStatus
documentsCompleted: number
linesProcessed: number
itemsProcessed: number
workType: WorkType
documentId: string
```

---

## 📊 Типы данных

### Employee
```typescript
interface Employee {
  id: string;
  name: string;
  badge?: string;
  role?: string;
  department?: string;
  phone?: string;
  photo?: string;
  isActive: boolean;
  createdAt: number;
  lastActiveAt?: number;
}
```

### PartnerSession
```typescript
interface PartnerSession {
  id: string;
  userId: string;
  partnerId: string;
  startedAt: number;
  endedAt?: number;
  status: SessionStatus;
  documentsCompleted: number;
  linesProcessed: number;
  itemsProcessed: number;
  workType?: WorkType;
  documentId?: string;
  notes?: string;
}
```

### SessionStatus
```typescript
type SessionStatus = 
  | 'active'      // Работают
  | 'paused'      // На паузе
  | 'completed'   // Завершена
  | 'cancelled';  // Отменена
```

### WorkType
```typescript
type WorkType = 
  | 'receiving'   // Приёмка
  | 'placement'   // Размещение
  | 'picking'     // Подбор
  | 'shipment'    // Отгрузка
  | 'inventory'   // Инвентаризация
  | 'return'      // Возврат
  | 'general';    // Общие работы
```

---

## 🔄 Рабочий процесс

### 1. Выбор напарника
```
Пользователь → Главная → "Работа с напарником"
           ↓
     Список сотрудников
           ↓
   Выбор из списка
           ↓
    Подтверждение
           ↓
   Создание сессии
```

### 2. Совместная работа
```
Активная сессия
       ↓
Работа с документами
       ↓
Автообновление статистики
       ↓
Завершение сессии
```

### 3. Статистика
```
Сессия завершена
       ↓
Данные сохранены в БД
       ↓
Доступны в "Недавние напарники"
       ↓
Рассчитывается совместимость
```

---

## 🎯 Основные возможности

### ✅ Управление сотрудниками
- Просмотр списка всех сотрудников
- Фильтрация по активности
- Поиск по имени, табельному, роли
- Отображение информации о сотруднике

### ✅ Сессии совместной работы
- Создание новой сессии
- Пауза и возобновление
- Завершение с сохранением статистики
- Автоматическое отслеживание времени

### ✅ Статистика
- История работы с каждым напарником
- Общее время работы вместе
- Количество обработанных документов
- Показатель совместимости (0-100%)

### ✅ Интеграция
- Связь с модулями документов
- Быстрый доступ к работе
- Сохранение контекста (тип работы, документ)

---

## 🚀 Использование

### Программный доступ

```typescript
import { partnerService } from '@/services/partnerService';

// Начать работу с напарником
const session = await partnerService.startSession(
  'user-001',    // ID текущего пользователя
  'user-002',    // ID напарника
  'receiving',   // Тип работы (опционально)
  'doc-123'      // ID документа (опционально)
);

// Обновить статистику
await partnerService.updateSessionStats(session.id, {
  documentsCompleted: 5,
  linesProcessed: 120,
  itemsProcessed: 350
});

// Завершить сессию
await partnerService.endSession(session.id);

// Получить статистику
const stats = await partnerService.getPartnerStats('user-001', 'user-002');
console.log(`Совместимость: ${stats.compatibility}%`);
```

### Использование в компонентах

```typescript
import { PartnerSelector } from '@/components/partner';

<PartnerSelector
  currentUserId="user-001"
  onSelect={(partner) => {
    console.log('Выбран:', partner.name);
    // Начать работу
  }}
  showRecentPartners={true}
/>
```

---

## 📈 Показатель совместимости

Совместимость рассчитывается на основе:
- Количества завершённых документов
- Времени совместной работы
- Эффективности (документов в час)

**Формула:**
```
documentsPerHour = documentsCompleted / totalHours
compatibility = min(100, round(documentsPerHour * 10))
```

**Интерпретация:**
- **0-30%** — Низкая совместимость
- **31-60%** — Средняя совместимость
- **61-85%** — Хорошая совместимость
- **86-100%** — Отличная совместимость

---

## 🔧 Настройка

### Изменение текущего пользователя

В `src/pages/PartnerManagement.tsx`:
```typescript
// Замените на реальную систему аутентификации
const CURRENT_USER_ID = 'user-001';
```

### Добавление сотрудников

Добавьте в `src/data/employees.json` или через API:
```json
{
  "id": "user-011",
  "name": "Новый Сотрудник",
  "badge": "011",
  "role": "Комплектовщик",
  "department": "Склад основной",
  "isActive": true,
  "createdAt": 1730000000000
}
```

---

## 🎓 Сценарии использования

### Сценарий 1: Приёмка товара вдвоём
```
1. Иванов открывает "Работа с напарником"
2. Выбирает Петрова из списка
3. Начинает сессию
4. Переходит в "Приёмка"
5. Работают вместе над документом
6. Завершают сессию
7. Статистика сохраняется
```

### Сценарий 2: Подбор крупного заказа
```
1. Сидорова выбирает напарника
2. Указывает тип работы "Подбор"
3. Активная сессия отображается
4. Работают над заказом
5. Делают паузу на обед
6. Возобновляют работу
7. Завершают после выполнения
```

### Сценарий 3: Инвентаризация склада
```
1. Козлов и Смирнова работают вместе
2. Начинают сессию "Инвентаризация"
3. Проходят зоны склада
4. Система отслеживает прогресс
5. Завершают в конце смены
6. Данные доступны для анализа
```

---

## 🧪 Тестовые данные

Включены 10 сотрудников:
- Иванов Иван (#001) — Кладовщик
- Петров Петр (#002) — Комплектовщик
- Сидорова Анна (#003) — Комплектовщик
- Козлов Михаил (#004) — Кладовщик
- Смирнова Ольга (#005) — Старший кладовщик
- Васильев Алексей (#006) — Комплектовщик
- Морозова Екатерина (#007) — Приемщик
- Новиков Дмитрий (#008) — Грузчик
- Павлова Мария (#009) — Комплектовщик
- Федоров Сергей (#010) — Неактивен

---

## ✅ Готовность модуля

- [x] Типы созданы
- [x] База данных обновлена
- [x] Сервис реализован
- [x] Компоненты работают
- [x] Страница готова
- [x] Интеграция выполнена
- [x] Тестовые данные добавлены
- [x] Документация написана

---

## 🎉 Модуль готов к использованию!

**Путь:** Главная → Работа с напарником → 🤝 Выбор

---

**Дата создания:** 29 октября 2025  
**Статус:** ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ

