# 📋 Модуль списка документов — Документация

## 🎯 Назначение

Модуль **"Все документы"** предоставляет единый интерфейс для просмотра, поиска и фильтрации всех документов складского приложения "Склад-15" в одном месте.

### Поддерживаемые типы документов:
- 📦 **Приёмка** (Receiving)
- 📍 **Размещение** (Placement)
- 🛒 **Подбор** (Picking)
- 🚚 **Отгрузка** (Shipment)
- ↩️ **Возврат/Списание** (Return)
- 📊 **Инвентаризация** (Inventory)

---

## 🏗️ Архитектура модуля

### Структура файлов

```
src/
├── types/
│   └── document.ts              # Типы для универсальных документов
├── services/
│   └── documentService.ts       # Сервис работы с документами
├── components/
│   └── documents/
│       ├── DocumentFilters.tsx  # Компонент фильтров и поиска
│       ├── DocumentCard.tsx     # Карточка документа
│       └── DocumentList.tsx     # Виртуализированный список
└── pages/
    └── Documents.tsx            # Главная страница модуля
```

---

## 📦 Основные компоненты

### 1. **DocumentService** (`src/services/documentService.ts`)

Сервис для работы со всеми типами документов.

#### Основные методы:

```typescript
// Получить все документы из всех модулей
await documentService.getAllDocuments();

// Фильтрация документов
documentService.filterDocuments(documents, filter);

// Сортировка документов
documentService.sortDocuments(documents, sort);

// Получить URL документа
documentService.getDocumentUrl(document);

// Вычислить процент выполнения
documentService.getCompletionPercentage(document);
```

### 2. **DocumentFilters** (`src/components/documents/DocumentFilters.tsx`)

Компонент для фильтрации и поиска документов.

**Возможности:**
- 🔍 Полнотекстовый поиск по номеру, контрагенту, примечаниям
- 📑 Фильтрация по типу документа
- 🎯 Фильтрация по статусу
- 🔄 Сортировка по различным полям
- ↻ Сброс всех фильтров

### 3. **DocumentCard** (`src/components/documents/DocumentCard.tsx`)

Карточка отображения документа в списке.

**Отображает:**
- Тип и иконка документа
- Номер документа и статус
- Контрагент (поставщик/клиент)
- Прогресс выполнения (визуальная полоса)
- Статистика (строк, количество)
- Дополнительная информация (маршрут, ТС, причина и т.д.)
- Даты создания/обновления
- Примечания

### 4. **DocumentList** (`src/components/documents/DocumentList.tsx`)

Виртуализированный список документов для обработки больших объёмов данных.

**Оптимизация:**
- Виртуализация на основе IntersectionObserver
- Начальная загрузка: 20 документов
- Подгрузка по скроллу: по 20 документов
- Поддержка до 10 000+ документов без потери производительности

### 5. **Documents Page** (`src/pages/Documents.tsx`)

Главная страница модуля, объединяющая все компоненты.

**Функции:**
- Загрузка всех документов при открытии
- Применение фильтров и сортировки
- Обновление списка документов
- Статистика по документам в футере

---

## 🎨 Интерфейс пользователя

### Главная страница

**Хедер:**
- Заголовок "📋 Документы"
- Кнопка обновления списка

**Панель фильтров:**
- Строка поиска
- Кнопка открытия расширенных фильтров
- Счётчик отображаемых/всего документов

**Расширенные фильтры:**
- Типы документов (кнопки выбора)
- Статусы (кнопки выбора)
- Сортировка (выбор поля и направления)
- Кнопка сброса фильтров

**Список документов:**
- Виртуализированный список карточек
- Infinite scroll (подгрузка при прокрутке)
- Индикатор загрузки
- Сообщение об отсутствии документов

**Футер со статистикой:**
- Всего документов
- В работе
- Завершено
- Новые (черновики)

---

## 🔧 Использование

### Добавление карточки на главную страницу

В `src/pages/Home.tsx` уже добавлена карточка:

```tsx
<button onClick={() => navigate('/documents')}>
  Все документы ({stats.total})
</button>
```

### Маршрутизация

В `src/App.tsx` добавлен маршрут:

```tsx
<Route path="documents" element={<Documents />} />
```

### Навигация к конкретному документу

При клике на карточку документа происходит автоматический переход на страницу этого документа:

```typescript
// Автоматически определяется URL в зависимости от типа
// Например:
// /receiving/{id}
// /picking/{id}
// /shipment/{id}
```

---

## 🎯 Фильтры и поиск

### Поисковый запрос

Поиск работает по следующим полям:
- Номер документа (`number`)
- Внешний ID (`externalId`)
- Название контрагента (`partnerName`)
- Примечания (`notes`)

### Фильтрация по типам

Можно выбрать один или несколько типов документов:
- Приёмка
- Размещение
- Подбор
- Отгрузка
- Возврат
- Инвентаризация

### Фильтрация по статусам

Доступные статусы:
- Черновик
- В работе
- Завершён
- Отменён
- Синхронизирован
- Ошибка

### Сортировка

Доступные поля сортировки:
- Дата создания
- Дата обновления
- Номер документа
- Статус

Направление: возрастание (↑) / убывание (↓)

---

## 🚀 Производительность

### Оптимизация для больших объёмов

Модуль оптимизирован для работы с большими объёмами документов (до 10 000+):

1. **Виртуализация списка:**
   - Рендерится только видимая часть списка
   - Подгрузка по требованию (lazy loading)
   - Использование IntersectionObserver

2. **Мемоизация фильтров:**
   - `useMemo` для пересчёта только при изменении данных
   - Минимизация лишних ререндеров

3. **Индексы IndexedDB:**
   - Быстрая выборка по `status`, `createdAt`, `updatedAt`
   - Оптимизированные запросы к базе данных

4. **Кэширование:**
   - Результаты запросов кэшируются в памяти
   - Обновление только при явном запросе

---

## 📱 Адаптивность

Модуль полностью адаптирован для работы на складских терминалах данных (TSD):

- ✅ Большие кликабельные области
- ✅ Оптимизация для сенсорного ввода
- ✅ Читаемость на маленьких экранах
- ✅ Работа в оффлайн-режиме (PWA)
- ✅ Быстрый отклик интерфейса

---

## 🔄 Синхронизация

Документы автоматически синхронизируются с сервером:

- Загрузка документов из IndexedDB (оффлайн)
- Отображение статуса синхронизации
- Автоматическое обновление при восстановлении сети

---

## 🎨 Цветовая схема

Каждый тип документа имеет свой цвет для быстрого визуального распознавания:

| Тип документа | Цвет |
|---------------|------|
| Приёмка | 🔵 Синий |
| Размещение | 🟣 Фиолетовый |
| Подбор | 🟠 Оранжевый |
| Отгрузка | 🟢 Зелёный |
| Возврат | 🔴 Красный |
| Инвентаризация | 🟡 Жёлтый |

Статусы также имеют цветовую кодировку для быстрого понимания состояния документа.

---

## 🛠️ Расширение функциональности

### Добавление нового фильтра

1. Добавить поле в интерфейс `DocumentFilter` (`src/types/document.ts`)
2. Обновить UI в `DocumentFilters.tsx`
3. Добавить логику фильтрации в `documentService.ts`

### Добавление нового поля в карточку

1. Добавить поле в `UniversalDocument` (`src/types/document.ts`)
2. Обновить маппинг в `documentService.ts`
3. Отобразить в `DocumentCard.tsx`

---

## 📊 Статистика и аналитика

В футере страницы отображается быстрая статистика:

- **Всего документов** — общее количество
- **В работе** — документы со статусом `in_progress`
- **Завершено** — документы со статусом `completed`
- **Новые** — документы со статусом `draft`

---

## 🐛 Обработка ошибок

Модуль включает обработку ошибок:

- Отображение сообщения об ошибке при сбое загрузки
- Возможность повторной попытки загрузки
- Логирование ошибок в консоль для отладки

---

## ✅ Чек-лист внедрения

- [x] Типы для универсальных документов
- [x] Сервис работы с документами
- [x] Компонент фильтров и поиска
- [x] Компонент карточки документа
- [x] Виртуализированный список
- [x] Главная страница модуля
- [x] Маршрутизация
- [x] Интеграция с главной страницей
- [x] Оптимизация производительности
- [x] Адаптивный дизайн

---

## 🎓 Примеры использования

### Программный доступ к документам

```typescript
import { documentService } from '@/services/documentService';

// Получить все документы
const allDocs = await documentService.getAllDocuments();

// Фильтрация
const filtered = documentService.filterDocuments(allDocs, {
  types: ['receiving', 'shipment'],
  statuses: ['in_progress'],
  searchQuery: 'ООО Поставщик'
});

// Сортировка
const sorted = documentService.sortDocuments(filtered, {
  field: 'createdAt',
  direction: 'desc'
});
```

---

## 📞 Поддержка

Для вопросов и предложений по улучшению модуля обращайтесь к команде разработки.

---

**Модуль готов к использованию! 🚀**

